Result of following commands:

cp -a bash-completion-2.11 bash-completion-2.11.old

find bash-completion-2.11/completions/ -type f -exec sed -i 's/ grep / ggrep /g' {} \;
find bash-completion-2.11/completions/ -type f -exec sed -i 's/ awk / nawk /g' {} \;
find bash-completion-2.11/completions/ -type f -exec sed -i 's/$(awk /$(nawk /g' {} \;
find bash-completion-2.11/completions/ -type f -exec sed -i 's/ sed / gsed /g' {} \;

find bash-completion-2.11/bash_completion -type f -exec sed -i 's/ grep / ggrep /g' {} \;
find bash-completion-2.11/bash_completion -type f -exec sed -i 's/ awk / nawk /g' {} \;
find bash-completion-2.11/bash_completion -type f -exec sed -i 's/$(awk /$(nawk /g' {} \;
find bash-completion-2.11/bash_completion -type f -exec sed -i 's/ sed / gsed /g' {} \;

gdiff -pruN bash-completion-2.11.old bash-completion-2.11 | less

And check the result.

diff -pruN bash-completion-2.11.old/bash_completion bash-completion-2.11/bash_completion
--- bash-completion-2.11.old/bash_completion	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/bash_completion	2021-03-31 13:41:21.942243526 +0000
@@ -918,7 +918,7 @@ _mac_addresses()
     COMPREPLY+=($(
         {
             LC_ALL=C ifconfig -a || ip link show
-        } 2>/dev/null | command sed -ne \
+        } 2>/dev/null | command gsed -ne \
             "s/.*[[:space:]]HWaddr[[:space:]]\{1,\}\($re\)[[:space:]].*/\1/p" -ne \
             "s/.*[[:space:]]HWaddr[[:space:]]\{1,\}\($re\)[[:space:]]*$/\1/p" -ne \
             "s|.*[[:space:]]\(link/\)\{0,1\}ether[[:space:]]\{1,\}\($re\)[[:space:]].*|\2|p" -ne \
@@ -928,12 +928,12 @@ _mac_addresses()
     # ARP cache
     COMPREPLY+=($({
         arp -an || ip neigh show
-    } 2>/dev/null | command sed -ne \
+    } 2>/dev/null | command gsed -ne \
         "s/.*[[:space:]]\($re\)[[:space:]].*/\1/p" -ne \
         "s/.*[[:space:]]\($re\)[[:space:]]*$/\1/p"))
 
     # /etc/ethers
-    COMPREPLY+=($(command sed -ne \
+    COMPREPLY+=($(command gsed -ne \
         "s/^[[:space:]]*\($re\)[[:space:]].*/\1/p" /etc/ethers 2>/dev/null))
 
     COMPREPLY=($(compgen -W '${COMPREPLY[@]}' -- "$cur"))
@@ -946,24 +946,24 @@ _configured_interfaces()
 {
     if [[ -f /etc/debian_version ]]; then
         # Debian system
-        COMPREPLY=($(compgen -W "$(command sed -ne 's|^iface \([^ ]\{1,\}\).*$|\1|p' \
+        COMPREPLY=($(compgen -W "$(command gsed -ne 's|^iface \([^ ]\{1,\}\).*$|\1|p' \
             /etc/network/interfaces /etc/network/interfaces.d/* 2>/dev/null)" \
             -- "$cur"))
     elif [[ -f /etc/SuSE-release ]]; then
         # SuSE system
         COMPREPLY=($(compgen -W "$(printf '%s\n' \
             /etc/sysconfig/network/ifcfg-* |
-            command sed -ne 's|.*ifcfg-\([^*].*\)$|\1|p')" -- "$cur"))
+            command gsed -ne 's|.*ifcfg-\([^*].*\)$|\1|p')" -- "$cur"))
     elif [[ -f /etc/pld-release ]]; then
         # PLD Linux
         COMPREPLY=($(compgen -W "$(command ls -B \
             /etc/sysconfig/interfaces |
-            command sed -ne 's|.*ifcfg-\([^*].*\)$|\1|p')" -- "$cur"))
+            command gsed -ne 's|.*ifcfg-\([^*].*\)$|\1|p')" -- "$cur"))
     else
         # Assume Red Hat
         COMPREPLY=($(compgen -W "$(printf '%s\n' \
             /etc/sysconfig/network-scripts/ifcfg-* |
-            command sed -ne 's|.*ifcfg-\([^*].*\)$|\1|p')" -- "$cur"))
+            command gsed -ne 's|.*ifcfg-\([^*].*\)$|\1|p')" -- "$cur"))
     fi
 }
 
@@ -984,7 +984,7 @@ _ip_addresses()
     local addrs=$({
         LC_ALL=C ifconfig -a || ip addr show
     } 2>/dev/null |
-        command sed -e 's/[[:space:]]addr:/ /' -ne \
+        command gsed -e 's/[[:space:]]addr:/ /' -ne \
             "s|.*inet${n}[[:space:]]\{1,\}\([^[:space:]/]*\).*|\1|p")
     COMPREPLY+=($(compgen -W "$addrs" -- "${cur-}"))
 }
@@ -1012,7 +1012,7 @@ _available_interfaces()
         else
             ifconfig -a || ip link show
         fi
-    } 2>/dev/null | awk \
+    } 2>/dev/null | nawk \
         '/^[^ \t]/ { if ($1 ~ /^[0-9]+:/) { print $2 } else { print $1 } }'))
 
     COMPREPLY=($(compgen -W '${COMPREPLY[@]/%[[:punct:]]/}' -- "$cur"))
@@ -1099,17 +1099,17 @@ if [[ $OSTYPE == *@(solaris|aix)* ]]; th
     # This function completes on process IDs.
     _pids()
     {
-        COMPREPLY=($(compgen -W '$(command ps -efo pid | command sed 1d)' -- "$cur"))
+        COMPREPLY=($(compgen -W '$(command ps -efo pid | command gsed 1d)' -- "$cur"))
     }
 
     _pgids()
     {
-        COMPREPLY=($(compgen -W '$(command ps -efo pgid | command sed 1d)' -- "$cur"))
+        COMPREPLY=($(compgen -W '$(command ps -efo pgid | command gsed 1d)' -- "$cur"))
     }
     _pnames()
     {
         COMPREPLY=($(compgen -X '<defunct>' -W '$(command ps -efo comm | \
-        command sed -e 1d -e "s:.*/::" -e "s/^-//" | sort -u)' -- "$cur"))
+        command gsed -e 1d -e "s:.*/::" -e "s/^-//" | sort -u)' -- "$cur"))
     }
 else
     _pids()
@@ -1125,7 +1125,7 @@ else
     {
         local -a procs
         if [[ ${1-} == -s ]]; then
-            procs=($(command ps axo comm | command sed -e 1d))
+            procs=($(command ps axo comm | command gsed -e 1d))
         else
             local line i=-1 ifs=$IFS
             IFS=$'\n'
@@ -1230,7 +1230,7 @@ _services()
         systemctl list-units --full --all ||
             systemctl list-unit-files
     } 2>/dev/null |
-        awk '$1 ~ /\.service$/ { sub("\\.service$", "", $1); print $1 }'))
+        nawk '$1 ~ /\.service$/ { sub("\\.service$", "", $1); print $1 }'))
 
     if [[ -x /sbin/upstart-udev-bridge ]]; then
         COMPREPLY+=($(initctl list 2>/dev/null | cut -d' ' -f1))
@@ -1257,7 +1257,7 @@ _service()
     else
         local sysvdirs
         _sysvdirs
-        COMPREPLY=($(compgen -W '`command sed -e "y/|/ /" \
+        COMPREPLY=($(compgen -W '`command gsed -e "y/|/ /" \
             -ne "s/^.*\(U\|msg_u\)sage.*{\(.*\)}.*$/\2/p" \
             ${sysvdirs[0]}/${prev##*/} 2>/dev/null` start stop' -- "$cur"))
     fi
@@ -1278,7 +1278,7 @@ _modules()
     local modpath
     modpath=/lib/modules/$1
     COMPREPLY=($(compgen -W "$(command ls -RL $modpath 2>/dev/null |
-        command sed -ne 's/^\(.*\)\.k\{0,1\}o\(\.[gx]z\)\{0,1\}$/\1/p')" -- "$cur"))
+        command gsed -ne 's/^\(.*\)\.k\{0,1\}o\(\.[gx]z\)\{0,1\}$/\1/p')" -- "$cur"))
 }
 
 # This function completes on installed modules
@@ -1286,7 +1286,7 @@ _modules()
 _installed_modules()
 {
     COMPREPLY=($(compgen -W "$(PATH="$PATH:/sbin" lsmod |
-        awk '{if (NR != 1) print $1}')" -- "$1"))
+        nawk '{if (NR != 1) print $1}')" -- "$1"))
 }
 
 # This function completes on user or user:group format; as for chown and cpio.
@@ -1385,13 +1385,13 @@ _fstypes()
     if [[ -e /proc/filesystems ]]; then
         # Linux
         fss="$(cut -d$'\t' -f2 /proc/filesystems)
-             $(awk '! /\*/ { print $NF }' /etc/filesystems 2>/dev/null)"
+             $(nawk '! /\*/ { print $NF }' /etc/filesystems 2>/dev/null)"
     else
         # Generic
-        fss="$(awk '/^[ \t]*[^#]/ { print $3 }' /etc/fstab 2>/dev/null)
-             $(awk '/^[ \t]*[^#]/ { print $3 }' /etc/mnttab 2>/dev/null)
-             $(awk '/^[ \t]*[^#]/ { print $4 }' /etc/vfstab 2>/dev/null)
-             $(awk '{ print $1 }' /etc/dfs/fstypes 2>/dev/null)
+        fss="$(nawk '/^[ \t]*[^#]/ { print $3 }' /etc/fstab 2>/dev/null)
+             $(nawk '/^[ \t]*[^#]/ { print $3 }' /etc/mnttab 2>/dev/null)
+             $(nawk '/^[ \t]*[^#]/ { print $4 }' /etc/vfstab 2>/dev/null)
+             $(nawk '{ print $1 }' /etc/dfs/fstypes 2>/dev/null)
              $([[ -d /etc/fs ]] && command ls /etc/fs)"
     fi
 
@@ -1459,7 +1459,7 @@ _count_args()
 _pci_ids()
 {
     COMPREPLY+=($(compgen -W \
-        "$(PATH="$PATH:/sbin" lspci -n | awk '{print $3}')" -- "$cur"))
+        "$(PATH="$PATH:/sbin" lspci -n | nawk '{print $3}')" -- "$cur"))
 }
 
 # This function completes on USB IDs
@@ -1467,7 +1467,7 @@ _pci_ids()
 _usb_ids()
 {
     COMPREPLY+=($(compgen -W \
-        "$(PATH="$PATH:/sbin" lsusb | awk '{print $6}')" -- "$cur"))
+        "$(PATH="$PATH:/sbin" lsusb | nawk '{print $6}')" -- "$cur"))
 }
 
 # CD device names
@@ -1486,12 +1486,12 @@ _dvd_devices()
 _terms()
 {
     COMPREPLY+=($(compgen -W "$({
-        command sed -ne 's/^\([^[:space:]#|]\{2,\}\)|.*/\1/p' /etc/termcap
+        command gsed -ne 's/^\([^[:space:]#|]\{2,\}\)|.*/\1/p' /etc/termcap
         {
             toe -a || toe
-        } | awk '{ print $1 }'
+        } | nawk '{ print $1 }'
         find /{etc,lib,usr/lib,usr/share}/terminfo/? -type f -maxdepth 1 |
-            awk -F/ '{ print $NF }'
+            nawk -F/ '{ print $NF }'
     } 2>/dev/null)" -- "$cur"))
 }
 
@@ -1499,7 +1499,7 @@ _bashcomp_try_faketty()
 {
     if type unbuffer &>/dev/null; then
         unbuffer -p "$@"
-    elif script --version 2>&1 | command grep -qF util-linux; then
+    elif script --version 2>&1 | command ggrep -qF util-linux; then
         # BSD and Solaris "script" do not seem to have required features
         script -qaefc "$*" /dev/null
     else
@@ -1555,7 +1555,7 @@ _included_ssh_config_files()
 
     local reset=$(shopt -po noglob)
     set -o noglob
-    local included=($(command sed -ne 's/^[[:blank:]]*[Ii][Nn][Cc][Ll][Uu][Dd][Ee][[:blank:]]\(.*\)$/\1/p' "${configfile}"))
+    local included=($(command gsed -ne 's/^[[:blank:]]*[Ii][Nn][Cc][Ll][Uu][Dd][Ee][[:blank:]]\(.*\)$/\1/p' "${configfile}"))
     $reset
 
     [[ ${included-} ]] || return
@@ -1666,7 +1666,7 @@ _known_hosts_real()
         # TODO(?): try to make known hosts files with more than one consecutive
         #          spaces in their name work (watch out for ~ expansion
         #          breakage! Alioth#311595)
-        tmpkh=($(awk 'sub("^[ \t]*([Gg][Ll][Oo][Bb][Aa][Ll]|[Uu][Ss][Ee][Rr])[Kk][Nn][Oo][Ww][Nn][Hh][Oo][Ss][Tt][Ss][Ff][Ii][Ll][Ee][ \t]+", "") { print $0 }' "${config[@]}" | sort -u))
+        tmpkh=($(nawk 'sub("^[ \t]*([Gg][Ll][Oo][Bb][Aa][Ll]|[Uu][Ss][Ee][Rr])[Kk][Nn][Oo][Ww][Nn][Hh][Oo][Ss][Tt][Ss][Ff][Ii][Ll][Ee][ \t]+", "") { print $0 }' "${config[@]}" | sort -u))
         IFS=$ifs
     fi
     if ((${#tmpkh[@]} != 0)); then
@@ -1750,7 +1750,7 @@ _known_hosts_real()
 
     # append any available aliases from ssh config files
     if [[ ${#config[@]} -gt 0 && -v aliases ]]; then
-        local -a hosts=($(command sed -ne 's/^[[:blank:]]*[Hh][Oo][Ss][Tt][[:blank:]]\(.*\)$/\1/p' "${config[@]}"))
+        local -a hosts=($(command gsed -ne 's/^[[:blank:]]*[Hh][Oo][Ss][Tt][[:blank:]]\(.*\)$/\1/p' "${config[@]}"))
         if ((${#hosts[@]} != 0)); then
             COMPREPLY+=($(compgen -P "$prefix" \
                 -S "$suffix" -W '${hosts[@]%%[*?%]*}' -X '\!*' -- "$cur"))
@@ -1767,13 +1767,13 @@ _known_hosts_real()
         # time ago, so...
         COMPREPLY+=($(compgen -P "$prefix" -S "$suffix" -W \
             "$(avahi-browse -cpr _workstation._tcp 2>/dev/null |
-                awk -F';' '/^=/ { print $7 }' | sort -u)" -- "$cur"))
+                nawk -F';' '/^=/ { print $7 }' | sort -u)" -- "$cur"))
     fi
 
     # Add hosts reported by ruptime.
     if type ruptime &>/dev/null; then
         COMPREPLY+=($(compgen -W \
-            "$(ruptime 2>/dev/null | awk '!/^ruptime:/ { print $1 }')" \
+            "$(ruptime 2>/dev/null | nawk '!/^ruptime:/ { print $1 }')" \
             -- "$cur"))
     fi
 
@@ -1999,7 +1999,7 @@ _longopt()
             return
             ;;
         --+([-a-z0-9_]))
-            local argtype=$(LC_ALL=C $1 --help 2>&1 | command sed -ne \
+            local argtype=$(LC_ALL=C $1 --help 2>&1 | command gsed -ne \
                 "s|.*$prev\[\{0,1\}=[<[]\{0,1\}\([-A-Za-z0-9_]\{1,\}\).*|\1|p")
             case ${argtype,,} in
                 *dir*)
@@ -2031,11 +2031,11 @@ _longopt()
     fi
 }
 # makeinfo and texi2dvi are defined elsewhere.
-complete -F _longopt a2ps awk base64 bash bc bison cat chroot colordiff cp \
+complete -F _longopt a2ps nawk base64 bash bc bison cat chroot colordiff cp \
     csplit cut date df diff dir du enscript env expand fmt fold gperf \
-    grep grub head irb ld ldd less ln ls m4 md5sum mkdir mkfifo mknod \
+    ggrep grub head irb ld ldd less ln ls m4 md5sum mkdir mkfifo mknod \
     mv netstat nl nm objcopy objdump od paste pr ptx readelf rm rmdir \
-    sed seq sha{,1,224,256,384,512}sum shar sort split strip sum tac tail tee \
+    gsed seq sha{,1,224,256,384,512}sum shar sort split strip sum tac tail tee \
     texindex touch tr uname unexpand uniq units vdir wc who
 
 declare -Ag _xspecs
diff -pruN bash-completion-2.11.old/completions/_adb bash-completion-2.11/completions/_adb
--- bash-completion-2.11.old/completions/_adb	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/_adb	2021-03-31 13:40:32.742446430 +0000
@@ -6,8 +6,8 @@
 _adb_command_usage()
 {
     COMPREPLY=($(compgen -W \
-        '$("$1" help 2>&1 | command grep "^ *\(adb \)\? *$2 " \
-            | command sed -e "s/[]|[]/\n/g" | _parse_help -)' -- "$cur"))
+        '$("$1" help 2>&1 | command ggrep "^ *\(adb \)\? *$2 " \
+            | command gsed -e "s/[]|[]/\n/g" | _parse_help -)' -- "$cur"))
 }
 
 _adb()
@@ -39,7 +39,7 @@ _adb()
             tmp+=($(compgen -W '$(_parse_help "$1" help)' -- "$cur"))
         fi
         if [[ ! $cur || $cur != -* ]]; then
-            tmp+=($($1 help 2>&1 | awk '$1 == "adb" { print $2 }'))
+            tmp+=($($1 help 2>&1 | nawk '$1 == "adb" { print $2 }'))
             tmp+=(devices connect disconnect sideload)
         fi
         COMPREPLY=($(compgen -W '${tmp[@]}' -- "$cur"))
@@ -56,7 +56,7 @@ _adb()
             ;;
         forward)
             COMPREPLY=($(compgen -W \
-                '$("$1" help 2>&1 | command sed -ne "s/^ *adb  *forward  *-/-/p" | \
+                '$("$1" help 2>&1 | command gsed -ne "s/^ *adb  *forward  *-/-/p" | \
                     _parse_help -)' -- "$cur"))
             ;;
         reboot)
diff -pruN bash-completion-2.11.old/completions/_mock bash-completion-2.11/completions/_mock
--- bash-completion-2.11.old/completions/_mock	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/_mock	2021-03-31 13:40:33.186118929 +0000
@@ -45,7 +45,7 @@ _mock()
             # This would actually depend on what the target root
             # can be used to build for...
             COMPREPLY=($(compgen -W "$(command rpm --showrc |
-                command sed -ne 's/^\s*compatible\s\s*archs\s*:\s*\(.*\)/\1/i p')" \
+                command gsed -ne 's/^\s*compatible\s\s*archs\s*:\s*\(.*\)/\1/i p')" \
                 -- "$cur"))
             return
             ;;
diff -pruN bash-completion-2.11.old/completions/_modules bash-completion-2.11/completions/_modules
--- bash-completion-2.11.old/completions/_modules	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/_modules	2021-03-31 13:40:32.934027505 +0000
@@ -23,13 +23,13 @@
 
 _module_list()
 {
-    local modules="$(command sed 's/:/ /g' <<<$LOADEDMODULES | sort)"
+    local modules="$(command gsed 's/:/ /g' <<<$LOADEDMODULES | sort)"
     compgen -W "$modules" -- $1
 }
 
 _module_path()
 {
-    local modules="$(command sed 's/:/ /g' <<<$MODULEPATH | sort)"
+    local modules="$(command gsed 's/:/ /g' <<<$MODULEPATH | sort)"
     compgen -W "$modules" -- $1
 }
 
@@ -37,8 +37,8 @@ _module_avail()
 {
     local modules="$(
         module avail 2>&1 |
-            command grep -E -v '^(-|$)' |
-            xargs printf '%s\n' | command sed -e 's/(default)//g' | sort
+            command ggrep -E -v '^(-|$)' |
+            xargs printf '%s\n' | command gsed -e 's/(default)//g' | sort
     )"
 
     compgen -W "$modules" -- $1
@@ -54,8 +54,8 @@ _module()
         # First parameter on line -- we expect it to be a mode selection
 
         local options
-        options="$(module help 2>&1 | command grep -E '^[[:space:]]*\+' |
-            awk '{print $2}' | command sed -e 's/|/ /g' | sort)"
+        options="$(module help 2>&1 | command ggrep -E '^[[:space:]]*\+' |
+            nawk '{print $2}' | command gsed -e 's/|/ /g' | sort)"
 
         COMPREPLY=($(compgen -W "$options" -- "$cur"))
 
diff -pruN bash-completion-2.11.old/completions/_mount bash-completion-2.11/completions/_mount
--- bash-completion-2.11.old/completions/_mount	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/_mount	2021-03-31 13:40:33.178444717 +0000
@@ -34,7 +34,7 @@ _mount()
         for sm in "$(type -P showmount)" {,/usr}/{,s}bin/showmount; do
             [[ -x $sm ]] || continue
             COMPREPLY=($(compgen -W "$("$sm" -e ${cur%%:*} |
-                awk 'NR>1 {print $1}')" -- "${cur#*:}"))
+                nawk 'NR>1 {print $1}')" -- "${cur#*:}"))
             return
         done
     fi
@@ -45,19 +45,19 @@ _mount()
         if [[ -n $host ]]; then
             COMPREPLY=($(compgen -P "//$host" -W \
                 "$(smbclient -d 0 -NL $host 2>/dev/null |
-                    command sed -ne '/^[[:blank:]]*Sharename/,/^$/p' |
-                    command sed -ne '3,$s|^[^A-Za-z]*\([^[:blank:]]*\).*$|/\1|p')" \
+                    command gsed -ne '/^[[:blank:]]*Sharename/,/^$/p' |
+                    command gsed -ne '3,$s|^[^A-Za-z]*\([^[:blank:]]*\).*$|/\1|p')" \
                 -- "${cur#//$host}"))
         fi
     elif [[ -r /etc/vfstab ]]; then
         # Solaris
-        COMPREPLY=($(compgen -W "$(awk '! /^[ \t]*#/ {if ($3 ~ /\//) print $3}' /etc/vfstab)" -- "$cur"))
+        COMPREPLY=($(compgen -W "$(nawk '! /^[ \t]*#/ {if ($3 ~ /\//) print $3}' /etc/vfstab)" -- "$cur"))
     elif [[ ! -e /etc/fstab ]]; then
         # probably Cygwin
-        COMPREPLY=($(compgen -W "$($1 | awk '! /^[ \t]*#/ {if ($3 ~ /\//) print $3}')" -- "$cur"))
+        COMPREPLY=($(compgen -W "$($1 | nawk '! /^[ \t]*#/ {if ($3 ~ /\//) print $3}')" -- "$cur"))
     else
         # probably BSD
-        COMPREPLY=($(compgen -W "$(awk '! /^[ \t]*#/ {if ($2 ~ /\//) print $2}' /etc/fstab)" -- "$cur"))
+        COMPREPLY=($(compgen -W "$(nawk '! /^[ \t]*#/ {if ($2 ~ /\//) print $2}' /etc/fstab)" -- "$cur"))
     fi
 } &&
     complete -F _mount -o default -o dirnames mount
diff -pruN bash-completion-2.11.old/completions/_mount.linux bash-completion-2.11/completions/_mount.linux
--- bash-completion-2.11.old/completions/_mount.linux	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/_mount.linux	2021-03-31 13:40:32.460523794 +0000
@@ -228,7 +228,7 @@ _mount()
         for sm in "$(type -P showmount)" {,/usr}/{,s}bin/showmount; do
             [[ -x $sm ]] || continue
             COMPREPLY=($(compgen -W "$("$sm" -e ${cur%%:*} |
-                awk 'NR>1 {print $1}')" -- "${cur#*:}"))
+                nawk 'NR>1 {print $1}')" -- "${cur#*:}"))
             return
         done
     fi
@@ -239,8 +239,8 @@ _mount()
         if [[ -n $host ]]; then
             COMPREPLY=($(compgen -P "//$host" -W \
                 "$(smbclient -d 0 -NL $host 2>/dev/null |
-                    command sed -ne '/^[[:blank:]]*Sharename/,/^$/p' |
-                    command sed -ne '3,$s|^[^A-Za-z]*\([^[:blank:]]*\).*$|/\1|p')" \
+                    command gsed -ne '/^[[:blank:]]*Sharename/,/^$/p' |
+                    command gsed -ne '3,$s|^[^A-Za-z]*\([^[:blank:]]*\).*$|/\1|p')" \
                 -- "${cur#//$host}"))
         fi
     fi
diff -pruN bash-completion-2.11.old/completions/_nmcli bash-completion-2.11/completions/_nmcli
--- bash-completion-2.11.old/completions/_nmcli	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/_nmcli	2021-03-31 13:40:32.834639055 +0000
@@ -12,26 +12,26 @@ _nmcli_con_id()
 {
     local IFS=$'\n'
     COMPREPLY=($(compgen -W "$(nmcli con list 2>/dev/null |
-        tail -n +2 | awk -F ' {2,}' '{print $1 }')" -- "$cur"))
+        tail -n +2 | nawk -F ' {2,}' '{print $1 }')" -- "$cur"))
 }
 
 _nmcli_con_uuid()
 {
     COMPREPLY=($(compgen -W "$(nmcli con list 2>/dev/null |
-        tail -n +2 | awk -F ' {2,}' '{print $2}')" -- "$cur"))
+        tail -n +2 | nawk -F ' {2,}' '{print $2}')" -- "$cur"))
 }
 
 _nmcli_ap_ssid()
 {
     local IFS=$'\n'
     COMPREPLY=($(compgen -W "$(nmcli dev wifi list 2>/dev/null |
-        tail -n +2 | awk -F ' {2,}' '{print $1}')" -- "$cur"))
+        tail -n +2 | nawk -F ' {2,}' '{print $1}')" -- "$cur"))
 }
 
 _nmcli_ab_bssid()
 {
     COMPREPLY=($(compgen -W "$(nmcli dev wifi list 2>/dev/null |
-        tail -n +2 | awk -F ' {2,}' '{print $2}')" -- "$cur"))
+        tail -n +2 | nawk -F ' {2,}' '{print $2}')" -- "$cur"))
 }
 
 _nmcli()
diff -pruN bash-completion-2.11.old/completions/_rfkill bash-completion-2.11/completions/_rfkill
--- bash-completion-2.11.old/completions/_rfkill	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/_rfkill	2021-03-31 13:40:32.706529918 +0000
@@ -18,7 +18,7 @@ _rfkill()
                 ;;
             2)
                 if [[ $prev == block || $prev == unblock ]]; then
-                    COMPREPLY=($(compgen -W "$($1 list | awk -F: \
+                    COMPREPLY=($(compgen -W "$($1 list | nawk -F: \
                         '/^[0-9]/ {print $1}') all wifi bluetooth uwb wimax \
                         wwan gps" -- "$cur"))
                 fi
diff -pruN bash-completion-2.11.old/completions/_udevadm bash-completion-2.11/completions/_udevadm
--- bash-completion-2.11.old/completions/_udevadm	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/_udevadm	2021-03-31 13:40:32.994699574 +0000
@@ -60,7 +60,7 @@ _udevadm()
                 ;;
             *)
                 COMPREPLY=($(compgen -W "$("$1" --help 2>/dev/null |
-                    awk '/^[ \t]/ { print $1 }')" -- "$cur"))
+                    nawk '/^[ \t]/ { print $1 }')" -- "$cur"))
                 ;;
         esac
         return
diff -pruN bash-completion-2.11.old/completions/_xm bash-completion-2.11/completions/_xm
--- bash-completion-2.11.old/completions/_xm	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/_xm	2021-03-31 13:40:32.434157388 +0000
@@ -7,13 +7,13 @@
 _xen_domain_names()
 {
     COMPREPLY=($(compgen -W "$(xm list 2>/dev/null |
-        awk '!/Name|Domain-0/ { print $1 }')" -- "$cur"))
+        nawk '!/Name|Domain-0/ { print $1 }')" -- "$cur"))
 }
 
 _xen_domain_ids()
 {
     COMPREPLY=($(compgen -W "$(xm list 2>/dev/null |
-        awk '!/Name|Domain-0/ { print $2 }')" -- "$cur"))
+        nawk '!/Name|Domain-0/ { print $2 }')" -- "$cur"))
 }
 
 _xm()
@@ -152,7 +152,7 @@ _xm()
                             ;;
                         3)
                             COMPREPLY=($(compgen -W "$(xm block-list $prev \
-                                2>/dev/null | awk '!/Vdev/ { print $1 }')" \
+                                2>/dev/null | nawk '!/Vdev/ { print $1 }')" \
                                 -- "$cur"))
                             ;;
                     esac
@@ -177,7 +177,7 @@ _xm()
                             ;;
                         3)
                             COMPREPLY=($(compgen -W "$(xm network-list $prev \
-                                2>/dev/null | awk '!/Idx/ { print $1 }')" \
+                                2>/dev/null | nawk '!/Idx/ { print $1 }')" \
                                 -- "$cur"))
                             ;;
                     esac
diff -pruN bash-completion-2.11.old/completions/_yum bash-completion-2.11/completions/_yum
--- bash-completion-2.11.old/completions/_yum	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/_yum	2021-03-31 13:40:32.738907567 +0000
@@ -9,12 +9,12 @@ _yum_list()
         # Try to strip in between headings like "Available Packages"
         # This will obviously only work for English :P
         COMPREPLY=($(yum -d 0 -C list $1 "$cur*" 2>/dev/null |
-            command sed -ne '/^Available /d' -e '/^Installed /d' -e '/^Updated /d' \
+            command gsed -ne '/^Available /d' -e '/^Installed /d' -e '/^Updated /d' \
                 -e 's/[[:space:]].*//p'))
     else
         # Drop first line (e.g. "Updated Packages")
         COMPREPLY=($(yum -d 0 -C list $1 "$cur*" 2>/dev/null |
-            command sed -ne 1d -e 's/[[:space:]].*//p'))
+            command gsed -ne 1d -e 's/[[:space:]].*//p'))
     fi
 }
 
@@ -24,13 +24,13 @@ _yum_repolist()
     # http://yum.baseurl.org/ticket/83
     # Drop first ("repo id      repo name") and last ("repolist: ...") rows
     yum --noplugins -C repolist $1 2>/dev/null |
-        command sed -ne '/^repo\s\s*id/d' -e '/^repolist:/d' -e 's/[[:space:]].*//p'
+        command gsed -ne '/^repo\s\s*id/d' -e '/^repolist:/d' -e 's/[[:space:]].*//p'
 }
 
 _yum_plugins()
 {
     command ls /usr/lib/yum-plugins/*.py{,c,o} 2>/dev/null |
-        command sed -ne 's|.*/\([^./]*\)\.py[co]\{0,1\}$|\1|p' | sort -u
+        command gsed -ne 's|.*/\([^./]*\)\.py[co]\{0,1\}$|\1|p' | sort -u
 }
 
 _yum()
diff -pruN bash-completion-2.11.old/completions/2to3 bash-completion-2.11/completions/2to3
--- bash-completion-2.11.old/completions/2to3	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/2to3	2021-03-31 13:40:32.851872233 +0000
@@ -11,7 +11,7 @@ _2to3()
             ;;
         -f | --fix | -x | --nofix)
             COMPREPLY=($(compgen -W \
-                "$($1 --list-fixes 2>/dev/null | command sed -e 1d)" -- "$cur"))
+                "$($1 --list-fixes 2>/dev/null | command gsed -e 1d)" -- "$cur"))
             return
             ;;
         -j | --processes)
diff -pruN bash-completion-2.11.old/completions/7z bash-completion-2.11/completions/7z
--- bash-completion-2.11.old/completions/7z	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/7z	2021-03-31 13:40:32.552075381 +0000
@@ -115,7 +115,7 @@ _7z()
         if [[ ${words[1]} == d ]]; then
             local IFS=$'\n'
             COMPREPLY=($(compgen -W "$(printf '%s\n' "$($1 l ${words[2]} \
-                -slt 2>/dev/null | command sed -n '/^Path =/s/^Path = \(.*\)$/\1/p' \
+                -slt 2>/dev/null | command gsed -n '/^Path =/s/^Path = \(.*\)$/\1/p' \
                 2>/dev/null | tail -n+2)")" -- "$cur"))
             compopt -o filenames
         else
diff -pruN bash-completion-2.11.old/completions/abook bash-completion-2.11/completions/abook
--- bash-completion-2.11.old/completions/abook	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/abook	2021-03-31 13:40:33.197117976 +0000
@@ -23,12 +23,12 @@ _abook()
     case $prev in
         --informat)
             COMPREPLY=($(compgen -W "$($1 --formats |
-                command sed -n -e 's/^'$'\t''\([a-z]*\).*/\1/p' -e '/^$/q')" \
+                command gsed -n -e 's/^'$'\t''\([a-z]*\).*/\1/p' -e '/^$/q')" \
                 -- "$cur"))
             ;;
         --outformat)
             COMPREPLY=($(compgen -W "$($1 --formats |
-                command sed -n -e '/^$/,$s/^'$'\t''\([a-z]*\).*/\1/p')" \
+                command gsed -n -e '/^$/,$s/^'$'\t''\([a-z]*\).*/\1/p')" \
                 -- "$cur"))
             ;;
         --infile)
diff -pruN bash-completion-2.11.old/completions/alias bash-completion-2.11/completions/alias
--- bash-completion-2.11.old/completions/alias	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/alias	2021-03-31 13:40:32.401583917 +0000
@@ -10,7 +10,7 @@ _alias()
             COMPREPLY=($(compgen -A alias -- "$cur"))
             ;;
         *=)
-            COMPREPLY=("$(alias ${cur%=} 2>/dev/null | command sed \
+            COMPREPLY=("$(alias ${cur%=} 2>/dev/null | command gsed \
                 -e 's|^alias '"$cur"'\(.*\)$|\1|')")
             ;;
     esac
diff -pruN bash-completion-2.11.old/completions/apache2ctl bash-completion-2.11/completions/apache2ctl
--- bash-completion-2.11.old/completions/apache2ctl	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/apache2ctl	2021-03-31 13:40:32.608502926 +0000
@@ -6,7 +6,7 @@ _apache2ctl()
     _init_completion || return
 
     local APWORDS
-    APWORDS=$($1 2>&1 >/dev/null | awk 'NR<2 { print $3; exit }' |
+    APWORDS=$($1 2>&1 >/dev/null | nawk 'NR<2 { print $3; exit }' |
         tr "|" " ")
 
     COMPREPLY=($(compgen -W "$APWORDS" -- "$cur"))
diff -pruN bash-completion-2.11.old/completions/appdata-validate bash-completion-2.11/completions/appdata-validate
--- bash-completion-2.11.old/completions/appdata-validate	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/appdata-validate	2021-03-31 13:40:33.184173263 +0000
@@ -11,7 +11,7 @@ _appdata_validate()
             ;;
         --output-format)
             COMPREPLY=($(compgen -W "$($1 --help |
-                command sed -ne 's/--output-format.*\[\(.*\)\]/\1/' -e 's/|/ /gp')" \
+                command gsed -ne 's/--output-format.*\[\(.*\)\]/\1/' -e 's/|/ /gp')" \
                 -- "$cur"))
             return
             ;;
diff -pruN bash-completion-2.11.old/completions/apt-cache bash-completion-2.11/completions/apt-cache
--- bash-completion-2.11.old/completions/apt-cache	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/apt-cache	2021-03-31 13:40:32.509022580 +0000
@@ -10,7 +10,7 @@ _apt_cache_packages()
 _apt_cache_sources()
 {
     compgen -W "$(apt-cache dumpavail |
-        awk '$1 == "Source:" { print $2 }' | sort -u)" -- "$1"
+        nawk '$1 == "Source:" { print $2 }' | sort -u)" -- "$1"
 }
 
 # List APT source packages
diff -pruN bash-completion-2.11.old/completions/apt-get bash-completion-2.11/completions/apt-get
--- bash-completion-2.11.old/completions/apt-get	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/apt-get	2021-03-31 13:40:32.371610427 +0000
@@ -29,7 +29,7 @@ _apt_get()
             source)
                 COMPREPLY=($(_xfunc apt-cache _apt_cache_packages)
                 $(compgen -W "$(apt-cache dumpavail |
-                    awk '$1 == "Source:" { print $2 }' | sort -u)" -- "$cur"))
+                    nawk '$1 == "Source:" { print $2 }' | sort -u)" -- "$cur"))
                 ;;
             install)
                 if [[ $cur == */* ]]; then
@@ -69,7 +69,7 @@ _apt_get()
             return
             ;;
         --target-release | --default-release | -!(-*)t)
-            COMPREPLY=($(compgen -W "$(apt-cache policy | command sed -ne \
+            COMPREPLY=($(compgen -W "$(apt-cache policy | command gsed -ne \
                 's/^ *release.*[ ,]o=\(Debian\|Ubuntu\),a=\(\w*\).*/\2/p')" \
                 -- "$cur"))
             return
diff -pruN bash-completion-2.11.old/completions/aptitude bash-completion-2.11/completions/aptitude
--- bash-completion-2.11.old/completions/aptitude	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/aptitude	2021-03-31 13:40:32.782200554 +0000
@@ -8,8 +8,8 @@ _have grep-status && {
 } || {
     _comp_dpkg_hold_packages()
     {
-        command grep -B 2 'hold' /var/lib/dpkg/status |
-            awk "/Package: $1/ { print \$2 }"
+        command ggrep -B 2 'hold' /var/lib/dpkg/status |
+            nawk "/Package: $1/ { print \$2 }"
     }
 }
 
@@ -65,14 +65,14 @@ _aptitude()
             ;;
         --target-release | --default-release | -!(-*)t)
             COMPREPLY=($(apt-cache policy |
-                command grep "release.o=Debian,a=$cur" |
-                command sed -e "s/.*a=\(\w*\).*/\1/" | uniq 2>/dev/null))
+                command ggrep "release.o=Debian,a=$cur" |
+                command gsed -e "s/.*a=\(\w*\).*/\1/" | uniq 2>/dev/null))
             return
             ;;
     esac
 
     if [[ $cur == -* ]]; then
-        local opts=" $($1 --help 2>&1 | command sed -e \
+        local opts=" $($1 --help 2>&1 | command gsed -e \
             's/--with(out)-recommends/--without-recommends\n--with-recommends/' |
             _parse_help - | tr '\n' ' ') "
 
diff -pruN bash-completion-2.11.old/completions/arp bash-completion-2.11/completions/arp
--- bash-completion-2.11.old/completions/arp	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/arp	2021-03-31 13:40:33.069660759 +0000
@@ -35,7 +35,7 @@ _arp()
     _count_args "" "@(--device|--protocol|--file|--hw-type|-!(-*)[iApfHt])"
     case $args in
         1)
-            local ips=$("$1" -an | command sed -ne \
+            local ips=$("$1" -an | command gsed -ne \
                 's/.*(\([0-9]\{1,3\}\(\.[0-9]\{1,3\}\)\{3\}\)).*/\1/p')
             COMPREPLY=($(compgen -W '$ips' -- "$cur"))
             ;;
diff -pruN bash-completion-2.11.old/completions/aspell bash-completion-2.11/completions/aspell
--- bash-completion-2.11.old/completions/aspell	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/aspell	2021-03-31 13:40:32.621977019 +0000
@@ -33,7 +33,7 @@ _aspell()
             ;;
         --mode)
             COMPREPLY=($(compgen -W "$($1 modes 2>/dev/null |
-                awk '{ print $1 }')" -- "$cur"))
+                nawk '{ print $1 }')" -- "$cur"))
             return
             ;;
         --sug-mode)
@@ -51,7 +51,7 @@ _aspell()
             ;;
         --add-filter | --rem-filter)
             COMPREPLY=($(compgen -W "$($1 filters 2>/dev/null |
-                awk '{ print $1 }')" -- "$cur"))
+                nawk '{ print $1 }')" -- "$cur"))
             return
             ;;
     esac
diff -pruN bash-completion-2.11.old/completions/beadm bash-completion-2.11/completions/beadm
--- bash-completion-2.11.old/completions/beadm	2020-01-11 19:58:32.000000000 +0000
+++ bash-completion-2.11/completions/beadm	2021-03-31 13:40:33.050871638 +0000
@@ -138,7 +138,7 @@ _beadm()
                           # entered BE name, so find snapshot for the particuler BE
                           if [[ "${prev}" != "rollback" ]]
                           then
-                            be_and_snapshots=( $(for K in echo "${!be_and_snapshots[@]}"; do echo ${be_and_snapshots[$K]} | grep "^${prev}@" | sed -e 's,.*@,,'; done) )
+                            be_and_snapshots=( $(for K in echo "${!be_and_snapshots[@]}"; do echo ${be_and_snapshots[$K]} | ggrep "^${prev}@" | gsed -e 's,.*@,,'; done) )
                           fi
                           ;;
                       esac
@@ -188,7 +188,7 @@ _beadm()
               local pprev="${COMP_WORDS[COMP_CWORD-2]}"
               local realcur="${cur##*=}"
               local zfsprop="${pprev%%@*}"; zfsprop="${zfsprop##*,}"
-              local zfsvals=( $(zfs get 2>&1 | nawk '$1 == "'${zfsprop}'" && $2 == "YES" { $1=""; $2=""; $3=""; print }' | sed -e 's,|,,g') )
+              local zfsvals=( $(zfs get 2>&1 | nawk '$1 == "'${zfsprop}'" && $2 == "YES" { $1=""; $2=""; $3=""; print }' | gsed -e 's,|,,g') )
               compopt -o nospace
               compopt -o nosort
               COMPREPLY=( $(compgen -W "${zfsvals[*]}" -- "${realcur}") )
@@ -206,7 +206,7 @@ _beadm()
                   # ZFS property values
                   local realcur=${cur##*=}
                   local zfsprop=${prev%%@*}; zfsprop=${zfsprop##*,}
-                  local zfsvals=( $(zfs get 2>&1 | nawk '$1 == "'${zfsprop}'" && $2 == "YES" { $1=""; $2=""; $3=""; print }' | sed -e 's,|,,g') )
+                  local zfsvals=( $(zfs get 2>&1 | nawk '$1 == "'${zfsprop}'" && $2 == "YES" { $1=""; $2=""; $3=""; print }' | gsed -e 's,|,,g') )
                   compopt -o nospace
                   compopt -o nosort
                   COMPREPLY=( $(compgen -W "${zfsvals[*]}" -- "${realcur}") )
diff -pruN bash-completion-2.11.old/completions/bk bash-completion-2.11/completions/bk
--- bash-completion-2.11.old/completions/bk	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/bk	2021-03-31 13:40:32.966718418 +0000
@@ -7,7 +7,7 @@ _bk()
     _init_completion || return
 
     local BKCMDS="$(bk help topics 2>/dev/null |
-        awk '/^  bk/ { print $2 }' | xargs printf '%s ')"
+        nawk '/^  bk/ { print $2 }' | xargs printf '%s ')"
 
     COMPREPLY=($(compgen -W "$BKCMDS" -- "$cur"))
     _filedir
diff -pruN bash-completion-2.11.old/completions/brctl bash-completion-2.11/completions/brctl
--- bash-completion-2.11.old/completions/brctl	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/brctl	2021-03-31 13:40:32.469822693 +0000
@@ -19,7 +19,7 @@ _brctl()
 
                 *)
                     COMPREPLY=($(compgen -W "$($1 show |
-                        awk 'NR>1 {print $1}')" -- "$cur"))
+                        nawk 'NR>1 {print $1}')" -- "$cur"))
                     ;;
             esac
             ;;
diff -pruN bash-completion-2.11.old/completions/carton bash-completion-2.11/completions/carton
--- bash-completion-2.11.old/completions/carton	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/carton	2021-03-31 13:40:32.561945972 +0000
@@ -3,7 +3,7 @@
 _carton_commands()
 {
     local cmds=$("${1:-carton}" usage 2>&1 |
-        command sed -ne '/.*command.* is one of/{n;p;q;}')
+        command gsed -ne '/.*command.* is one of/{n;p;q;}')
     COMPREPLY+=($(IFS="$IFS," compgen -W "$cmds" -- "$cur"))
 }
 
diff -pruN bash-completion-2.11.old/completions/ccache bash-completion-2.11/completions/ccache
--- bash-completion-2.11.old/completions/ccache	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/ccache	2021-03-31 13:40:32.661325711 +0000
@@ -21,7 +21,7 @@ _ccache()
         --set-config | -!(-*)o)
             if [[ $cur != *=* ]]; then
                 COMPREPLY=($(compgen -S = -W "$($1 -p 2>/dev/null |
-                    awk '$3 = "=" { print $2 }')" -- "$cur"))
+                    nawk '$3 = "=" { print $2 }')" -- "$cur"))
                 [[ ${COMPREPLY-} == *= ]] && compopt -o nospace
             fi
             return
diff -pruN bash-completion-2.11.old/completions/ccze bash-completion-2.11/completions/ccze
--- bash-completion-2.11.old/completions/ccze	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/ccze	2021-03-31 13:40:32.422704038 +0000
@@ -28,7 +28,7 @@ _ccze()
             ;;
         --plugin | -!(-*)p)
             COMPREPLY=($(compgen -W '$("$1" --list-plugins | command \
-                sed -ne "s/^\([a-z0-9]\{1,\}\)[[:space:]]\{1,\}|.*/\1/p")' \
+                gsed -ne "s/^\([a-z0-9]\{1,\}\)[[:space:]]\{1,\}|.*/\1/p")' \
                 -- "$cur"))
             return
             ;;
diff -pruN bash-completion-2.11.old/completions/cfrun bash-completion-2.11/completions/cfrun
--- bash-completion-2.11.old/completions/cfrun	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/cfrun	2021-03-31 13:40:32.353785171 +0000
@@ -33,7 +33,7 @@ _cfrun()
                 done
                 [[ ! -f $hostfile ]] && return
 
-                COMPREPLY=($(compgen -W "$(command grep -v \
+                COMPREPLY=($(compgen -W "$(command ggrep -v \
                     -E '(=|^$|^#)' $hostfile)" -- "$cur"))
             fi
             ;;
diff -pruN bash-completion-2.11.old/completions/chronyc bash-completion-2.11/completions/chronyc
--- bash-completion-2.11.old/completions/chronyc	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/chronyc	2021-03-31 13:40:32.315359968 +0000
@@ -4,7 +4,7 @@ _chronyc_command_args()
 {
     local -a args
     args=($(compgen -W "$($1 help 2>/dev/null |
-        awk '/^'$prev'\s[^ []/ { gsub("\\|", " ", $2); print $2 }')"))
+        nawk '/^'$prev'\s[^ []/ { gsub("\\|", " ", $2); print $2 }')"))
     case $args in
         \<address\>) _known_hosts_real -- "$cur" ;;
         \<*) ;;
@@ -41,7 +41,7 @@ _chronyc()
     case $args in
         0)
             COMPREPLY=($(compgen -W "$($1 help 2>/dev/null |
-                awk '!/(^ |: *$)/ { sub("\\|", " ", $1); print $1 }')" \
+                nawk '!/(^ |: *$)/ { sub("\\|", " ", $1); print $1 }')" \
                 -- "$cur"))
             ;;
         1)
diff -pruN bash-completion-2.11.old/completions/complete bash-completion-2.11/completions/complete
--- bash-completion-2.11.old/completions/complete	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/complete	2021-03-31 13:40:33.002978426 +0000
@@ -29,7 +29,7 @@ _complete()
             return
             ;;
         -*p | -*r)
-            COMPREPLY=($(complete -p | command sed -e 's|.* ||'))
+            COMPREPLY=($(complete -p | command gsed -e 's|.* ||'))
             COMPREPLY=($(compgen -W '${COMPREPLY[@]}' -- "$cur"))
             return
             ;;
diff -pruN bash-completion-2.11.old/completions/configure bash-completion-2.11/completions/configure
--- bash-completion-2.11.old/completions/configure	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/configure	2021-03-31 13:40:32.688582460 +0000
@@ -28,8 +28,8 @@ _configure()
 
     if [[ ${COMP_CONFIGURE_HINTS-} ]]; then
         COMPREPLY=($(compgen -W "$($1 --help 2>&1 |
-            awk '/^  --[A-Za-z]/ { print $1; \
-            if ($2 ~ /--[A-Za-z]/) print $2 }' | command sed -e 's/[[,].*//g')" \
+            nawk '/^  --[A-Za-z]/ { print $1; \
+            if ($2 ~ /--[A-Za-z]/) print $2 }' | command gsed -e 's/[[,].*//g')" \
             -- "$cur"))
         [[ ${COMPREPLY-} == *=* ]] && compopt -o nospace
     else
diff -pruN bash-completion-2.11.old/completions/convert bash-completion-2.11/completions/convert
--- bash-completion-2.11.old/completions/convert	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/convert	2021-03-31 13:40:32.836584106 +0000
@@ -50,7 +50,7 @@ _ImageMagick()
             return
             ;;
         -format)
-            COMPREPLY=($(compgen -W "$(convert -list format | awk \
+            COMPREPLY=($(compgen -W "$(convert -list format | nawk \
                 '/ [r-][w-][+-] / { sub("[*]$","",$1); print tolower($1) }')" \
                 -- "$cur"))
             return
diff -pruN bash-completion-2.11.old/completions/cpan2dist bash-completion-2.11/completions/cpan2dist
--- bash-completion-2.11.old/completions/cpan2dist	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/cpan2dist	2021-03-31 13:40:32.518039999 +0000
@@ -29,7 +29,7 @@ _cpan2dist()
                 packagelist="$dir/02packages.details.txt.gz"
         done
         [[ -v packagelist ]] && COMPREPLY=($(zgrep "^${cur//-/::}" \
-            $packagelist 2>/dev/null | awk '{print $1}' | command sed -e 's/::/-/g'))
+            $packagelist 2>/dev/null | nawk '{print $1}' | command gsed -e 's/::/-/g'))
     fi
 } &&
     complete -F _cpan2dist -o default cpan2dist
diff -pruN bash-completion-2.11.old/completions/cvs bash-completion-2.11/completions/cvs
--- bash-completion-2.11.old/completions/cvs	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/cvs	2021-03-31 13:40:32.445935555 +0000
@@ -22,7 +22,7 @@ _cvs_modules()
 
 _cvs_commands()
 {
-    cvs --help-commands 2>&1 | awk '/^(     *|\t)/ { print $1 }'
+    cvs --help-commands 2>&1 | nawk '/^(     *|\t)/ { print $1 }'
 }
 
 _cvs_command_options()
@@ -39,7 +39,7 @@ _cvs_roots()
 {
     local -a cvsroots
     [[ -v CVSROOT ]] && cvsroots=("$CVSROOT")
-    [[ -r ~/.cvspass ]] && cvsroots+=($(awk '{ print $2 }' ~/.cvspass))
+    [[ -r ~/.cvspass ]] && cvsroots+=($(nawk '{ print $2 }' ~/.cvspass))
     [[ -r CVS/Root ]] && mapfile -tO ${#cvsroots[@]} cvsroots <CVS/Root
     COMPREPLY=($(compgen -W '${cvsroots[@]}' -- "$cur"))
     __ltrim_colon_completions "$cur"
@@ -225,7 +225,7 @@ _cvs()
             if [[ $cur != -* ]]; then
                 [[ ! -v cvsroot ]] && cvsroot=${CVSROOT-}
                 COMPREPLY=($(cvs -d "$cvsroot" co -c 2>/dev/null |
-                    awk '{print $1}'))
+                    nawk '{print $1}'))
                 COMPREPLY=($(compgen -W '${COMPREPLY[@]}' -- "$cur"))
             else
                 _cvs_command_options "$1" $mode
@@ -252,9 +252,9 @@ _cvs()
                     # far, but other changes (something other than
                     # changed/removed/new) may be missing
                     changed=($(cvs -q diff --brief 2>&1 |
-                        command sed -ne 's/^Files [^ ]* and \([^ ]*\) differ$/\1/p'))
+                        command gsed -ne 's/^Files [^ ]* and \([^ ]*\) differ$/\1/p'))
                     newremoved=($(cvs -q diff --brief 2>&1 |
-                        command sed -ne 's/^cvs diff: \([^ ]*\) .*, no comparison available$/\1/p'))
+                        command gsed -ne 's/^cvs diff: \([^ ]*\) .*, no comparison available$/\1/p'))
                     COMPREPLY=($(compgen -W '${changed[@]:-} \
                         ${newremoved[@]:-}' -- "$cur"))
                 else
@@ -303,7 +303,7 @@ _cvs()
 
             if [[ $cur != -* ]]; then
                 [[ ! -v cvsroot ]] && cvsroot=${CVSROOT-}
-                COMPREPLY=($(cvs -d "$cvsroot" co -c | awk '{print $1}'))
+                COMPREPLY=($(cvs -d "$cvsroot" co -c | nawk '{print $1}'))
                 COMPREPLY=($(compgen -W '${COMPREPLY[@]}' -- "$cur"))
             else
                 _cvs_command_options "$1" $mode
diff -pruN bash-completion-2.11.old/completions/cvsps bash-completion-2.11/completions/cvsps
--- bash-completion-2.11.old/completions/cvsps	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/cvsps	2021-03-31 13:40:32.960158617 +0000
@@ -11,22 +11,22 @@ _cvsps()
             ;;
         -s)
             COMPREPLY=($(compgen -W "$($1 2>/dev/null |
-                awk '/^PatchSet:?[ \t]/ { print $2 }')" -- "$cur"))
+                nawk '/^PatchSet:?[ \t]/ { print $2 }')" -- "$cur"))
             return
             ;;
         -a)
             COMPREPLY=($(compgen -W "$($1 2>/dev/null |
-                awk '/^Author:[ \t]/ { print $2 }')" -- "$cur"))
+                nawk '/^Author:[ \t]/ { print $2 }')" -- "$cur"))
             return
             ;;
         -b)
             COMPREPLY=($(compgen -W "$($1 2>/dev/null |
-                awk '/^Branch:[ \t]/ { print $2 }')" -- "$cur"))
+                nawk '/^Branch:[ \t]/ { print $2 }')" -- "$cur"))
             return
             ;;
         -r)
             COMPREPLY=($(compgen -W "$($1 2>/dev/null |
-                awk '/^Tag:[ \t]+[^(]/ { print $2 }')" -- "$cur"))
+                nawk '/^Tag:[ \t]+[^(]/ { print $2 }')" -- "$cur"))
             return
             ;;
         -p)
diff -pruN bash-completion-2.11.old/completions/dict bash-completion-2.11/completions/dict
--- bash-completion-2.11.old/completions/dict	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/dict	2021-03-31 13:40:33.021794018 +0000
@@ -2,7 +2,7 @@
 
 _dictdata()
 {
-    dict $host $port $1 2>/dev/null | command sed -ne \
+    dict $host $port $1 2>/dev/null | command gsed -ne \
         's/^[[:blank:]]\{1,\}\([^[:blank:]]*\).*$/\1/p'
 }
 
@@ -49,11 +49,11 @@ _dict()
     local dictfile=/usr/share/dict/words
     if [[ -r $dictfile ]]; then
         # Dictfile may be too large for practical compgen -W usage, so narrow
-        # it down with grep if $cur looks like something that's safe to embed
+        # it down with ggrep if $cur looks like something that's safe to embed
         # in a pattern instead.
         if [[ $cur == +([-A-Za-z0-9/.]) ]]; then
             COMPREPLY=($(compgen -W \
-                '$(command grep "^${cur//./\\.}" $dictfile)' -- "$cur"))
+                '$(command ggrep "^${cur//./\\.}" $dictfile)' -- "$cur"))
         else
             COMPREPLY=($(compgen -W '$(cat $dictfile)' -- "$cur"))
         fi
diff -pruN bash-completion-2.11.old/completions/dladm bash-completion-2.11/completions/dladm
--- bash-completion-2.11.old/completions/dladm	2020-01-11 19:58:32.000000000 +0000
+++ bash-completion-2.11/completions/dladm	2021-03-31 13:40:32.647107933 +0000
@@ -10,11 +10,11 @@ _dladm()
     prev="${COMP_WORDS[COMP_CWORD-1]}"
 
     if [[ ${prev} == 'dladm' ]]; then
-      local cmds="$(dladm 2>&1 | awk '/^ / { print $1 }')"
+      local cmds="$(dladm 2>&1 | nawk '/^ / { print $1 }')"
       COMPREPLY=( $(compgen -W "${cmds}" -- ${cur}) )
     elif [[ ${prev} =~ "-z" ]]; then
       # Some illumos OS distributions have zone-aware dladm. Treat -z as a zone name option
-      local zones="$(zoneadm list -c | grep -v '^global$')"
+      local zones="$(zoneadm list -c | ggrep -v '^global$')"
       COMPREPLY=( $(compgen -W "${zones}" -- ${cur}) )
     elif [[ ${prev} =~ 'delete-vnic' ]]; then
       # Redirect stderr to /dev/null not to polute console, e.g.:
diff -pruN bash-completion-2.11.old/completions/dmypy bash-completion-2.11/completions/dmypy
--- bash-completion-2.11.old/completions/dmypy	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/dmypy	2021-03-31 13:40:32.397330581 +0000
@@ -37,8 +37,8 @@ _dmypy()
 
     if [[ ! -v cmd ]]; then
         local cmds=$($1 --help 2>&1 |
-            command sed -ne '/positional arguments/{p;n;p;q}' |
-            command sed -ne 's/{\(.*\)}/\1/p')
+            command gsed -ne '/positional arguments/{p;n;p;q}' |
+            command gsed -ne 's/{\(.*\)}/\1/p')
         COMPREPLY=($(IFS="," compgen -W '$cmds' -- "$cur"))
         return
     fi
diff -pruN bash-completion-2.11.old/completions/dnssec-keygen bash-completion-2.11/completions/dnssec-keygen
--- bash-completion-2.11.old/completions/dnssec-keygen	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/dnssec-keygen	2021-03-31 13:40:32.733470787 +0000
@@ -3,9 +3,9 @@
 _dnssec_keygen_optarg()
 {
     local args=$("$1" -h 2>&1 |
-        command sed -e 's/|/ /g' -e 's/(.*//' \
+        command gsed -e 's/|/ /g' -e 's/(.*//' \
             -ne '/^[[:space:]]*'$2'/,/^[[:space:]]*[(-]/p' |
-        command sed -e 's/^[[:space:]]*'$2'.*://' -e '/^[[:space:]]*-/d')
+        command gsed -e 's/^[[:space:]]*'$2'.*://' -e '/^[[:space:]]*-/d')
     COMPREPLY+=($(compgen -W '$args' -- "$cur"))
 }
 
@@ -39,7 +39,7 @@ _dnssec_keygen()
 
     if [[ $cur == -* ]]; then
         COMPREPLY=($(compgen -W '$(_parse_help "$1" | \
-            command sed -e "s/:\$//")' -- "$cur"))
+            command gsed -e "s/:\$//")' -- "$cur"))
         return
     fi
 } &&
diff -pruN bash-completion-2.11.old/completions/dot bash-completion-2.11/completions/dot
--- bash-completion-2.11.old/completions/dot	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/dot	2021-03-31 13:40:33.090466780 +0000
@@ -13,13 +13,13 @@ _dot()
             ;;
         -T*)
             local langs=($("$1" -TNON_EXISTENT 2>&1 |
-                command sed -ne 's/.*one of://p'))
+                command gsed -ne 's/.*one of://p'))
             COMPREPLY=($(compgen -P -T -W '${langs[@]}' -- "${cur#-T}"))
             return
             ;;
         -K*)
             local layouts=($("$1" -KNON_EXISTENT 2>&1 |
-                command sed -ne 's/.*one of://p'))
+                command gsed -ne 's/.*one of://p'))
             COMPREPLY=($(compgen -P -K -W '${layouts[@]}' -- "${cur#-K}"))
             return
             ;;
diff -pruN bash-completion-2.11.old/completions/dpkg bash-completion-2.11/completions/dpkg
--- bash-completion-2.11.old/completions/dpkg	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/dpkg	2021-03-31 13:40:32.670575611 +0000
@@ -8,11 +8,11 @@ _have grep-status && {
 } || {
     _comp_dpkg_installed_packages()
     {
-        command grep -A 1 "Package: $1" /var/lib/dpkg/status 2>/dev/null |
-            command grep -B 1 -Ee "ok installed|half-installed|unpacked| \
+        command ggrep -A 1 "Package: $1" /var/lib/dpkg/status 2>/dev/null |
+            command ggrep -B 1 -Ee "ok installed|half-installed|unpacked| \
             half-configured" \
                 -Ee "^Essential: yes" |
-            awk "/Package: $1/ { print \$2 }" 2>/dev/null
+            nawk "/Package: $1/ { print \$2 }" 2>/dev/null
     }
 }
 
@@ -24,11 +24,11 @@ _have grep-status && {
 } || {
     _comp_dpkg_purgeable_packages()
     {
-        command grep -A 1 "Package: $1" /var/lib/dpkg/status 2>/dev/null |
-            command grep -B 1 -Ee "ok installed|half-installed|unpacked| \
+        command ggrep -A 1 "Package: $1" /var/lib/dpkg/status 2>/dev/null |
+            command ggrep -B 1 -Ee "ok installed|half-installed|unpacked| \
             half-configured|config-files" \
                 -Ee "^Essential: yes" |
-            awk "/Package: $1/ { print \$2 }" 2>/dev/null
+            nawk "/Package: $1/ { print \$2 }" 2>/dev/null
     }
 }
 
diff -pruN bash-completion-2.11.old/completions/ebtables bash-completion-2.11/completions/ebtables
--- bash-completion-2.11.old/completions/ebtables	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/ebtables	2021-03-31 13:40:32.652883012 +0000
@@ -14,7 +14,7 @@ _ebtables()
     case $prev in
         -!(-*)[AIDPFXLZ])
             COMPREPLY=($(compgen -W '`"$1" $table -L 2>/dev/null | \
-                command sed -ne "$chain"`' -- "$cur"))
+                command gsed -ne "$chain"`' -- "$cur"))
             ;;
         -!(-*)t)
             COMPREPLY=($(compgen -W 'nat filter broute' -- "$cur"))
@@ -23,19 +23,19 @@ _ebtables()
             if [[ $table == "-t filter" || -z $table ]]; then
                 COMPREPLY=($(compgen -W '$targets
                 $("$1" $table -L 2>/dev/null | \
-                  command sed -n -e "s/INPUT\|OUTPUT\|FORWARD//" \
+                  command gsed -n -e "s/INPUT\|OUTPUT\|FORWARD//" \
                                  -e "$chain")' \
                     -- "$cur"))
             elif [[ $table == "-t nat" ]]; then
                 COMPREPLY=($(compgen -W '$targets
                 $("$1" $table -L 2>/dev/null | \
-                  command sed -n -e "s/OUTPUT|PREROUTING|POSTROUTING//" \
+                  command gsed -n -e "s/OUTPUT|PREROUTING|POSTROUTING//" \
                                  -e "$chain")' \
                     -- "$cur"))
             elif [[ $table == "-t broute" ]]; then
                 COMPREPLY=($(compgen -W 'ACCEPT DROP
                 $("$1" $table -L 2>/dev/null | \
-                  command sed -n -e "s/BROUTING//" -e "$chain")' \
+                  command gsed -n -e "s/BROUTING//" -e "$chain")' \
                     -- "$cur"))
             fi
             ;;
diff -pruN bash-completion-2.11.old/completions/fbgs bash-completion-2.11/completions/fbgs
--- bash-completion-2.11.old/completions/fbgs	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/fbgs	2021-03-31 13:40:32.428016138 +0000
@@ -12,7 +12,7 @@ _fbgs()
             return
             ;;
         -m | --mode)
-            COMPREPLY=($(compgen -W '$(command sed \
+            COMPREPLY=($(compgen -W '$(command gsed \
                 -n "/^mode/{s/^mode \{1,\}\"\([^\"]\{1,\}\)\"/\1/g;p}" \
                 /etc/fb.modes 2>/dev/null)' -- "$cur"))
             return
diff -pruN bash-completion-2.11.old/completions/fbi bash-completion-2.11/completions/fbi
--- bash-completion-2.11.old/completions/fbi	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/fbi	2021-03-31 13:40:32.677752029 +0000
@@ -20,7 +20,7 @@ _fbi()
             return
             ;;
         -m | --mode)
-            COMPREPLY=($(compgen -W '$(command sed \
+            COMPREPLY=($(compgen -W '$(command gsed \
                 -n "/^mode/{s/^mode \{1,\}\"\([^\"]\{1,\}\)\"/\1/g;p}" \
                 /etc/fb.modes 2>/dev/null)' -- "$cur"))
             return
diff -pruN bash-completion-2.11.old/completions/function bash-completion-2.11/completions/function
--- bash-completion-2.11.old/completions/function	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/function	2021-03-31 13:40:33.092166602 +0000
@@ -29,7 +29,7 @@ _function()
     elif ((cword == 1)); then
         COMPREPLY=($(compgen -A function -- "$cur"))
     else
-        COMPREPLY=("() $(type -- ${words[1]} | command sed -e 1,2d)")
+        COMPREPLY=("() $(type -- ${words[1]} | command gsed -e 1,2d)")
     fi
 } &&
     complete -F _function function declare typeset
diff -pruN bash-completion-2.11.old/completions/fusermount bash-completion-2.11/completions/fusermount
--- bash-completion-2.11.old/completions/fusermount	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/fusermount	2021-03-31 13:40:32.840989426 +0000
@@ -10,7 +10,7 @@ _fusermount()
             return
             ;;
         -*u)
-            COMPREPLY=($(compgen -W "$(awk \
+            COMPREPLY=($(compgen -W "$(nawk \
                 '{ if ($3 ~ /^fuse(\.|$)/) print $2 }' /etc/mtab \
                 2>/dev/null)" -- "$cur"))
             return
diff -pruN bash-completion-2.11.old/completions/gcc bash-completion-2.11/completions/gcc
--- bash-completion-2.11.old/completions/gcc	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/gcc	2021-03-31 13:40:32.306333196 +0000
@@ -12,7 +12,7 @@ _gcc()
             local cc=$($1 -print-prog-name=cc1 2>/dev/null)
             [[ $cc ]] || return
             COMPREPLY=($(compgen -W "$($cc --help 2>/dev/null | tr '\t' ' ' |
-                command sed -e '/^  *-/!d' -e 's/ *-\([^][ <>]*\).*/-\1/')" \
+                command gsed -e '/^  *-/!d' -e 's/ *-\([^][ <>]*\).*/-\1/')" \
                 -- "$cur"))
             [[ ${COMPREPLY-} == *= ]] && compopt -o nospace
         else
@@ -58,16 +58,16 @@ _gcc()
     complete -F _gcc gcc{,-5,-6,-7,-8} g++{,-5,-6,-7,-8} g77 g95 \
         gccgo{,-5,-6,-7,-8} gcj gfortran{,-5,-6,-7,-8} gpc &&
     {
-        cc --version 2>/dev/null | command grep -q GCC ||
+        cc --version 2>/dev/null | command ggrep -q GCC ||
             [[ $(_realcommand cc) == *gcc* ]] &&
             complete -F _gcc cc || complete -F _minimal cc
-        c++ --version 2>/dev/null | command grep -q GCC ||
+        c++ --version 2>/dev/null | command ggrep -q GCC ||
             [[ $(_realcommand c++) == *g++* ]] &&
             complete -F _gcc c++ || complete -F _minimal c++
-        f77 --version 2>/dev/null | command grep -q GCC ||
+        f77 --version 2>/dev/null | command ggrep -q GCC ||
             [[ $(_realcommand f77) == *gfortran* ]] &&
             complete -F _gcc f77 || complete -F _minimal f77
-        f95 --version 2>/dev/null | command grep -q GCC ||
+        f95 --version 2>/dev/null | command ggrep -q GCC ||
             [[ $(_realcommand f95) == *gfortran* ]] &&
             complete -F _gcc f95 || complete -F _minimal f95
     }
diff -pruN bash-completion-2.11.old/completions/gdb bash-completion-2.11/completions/gdb
--- bash-completion-2.11.old/completions/gdb	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/gdb	2021-03-31 13:40:32.812382384 +0000
@@ -27,7 +27,7 @@ _gdb()
             # names manually.
             IFS=":"
             local path_array=($(
-                command sed -e 's/:\{2,\}/:/g' -e 's/^://' -e 's/:$//' <<<"$PATH"
+                command gsed -e 's/:\{2,\}/:/g' -e 's/^://' -e 's/:$//' <<<"$PATH"
             ))
             IFS=$'\n'
             COMPREPLY=($(compgen -d -W '$(find "${path_array[@]}" . \
@@ -36,7 +36,7 @@ _gdb()
         fi
     elif ((cword == 2)); then
         COMPREPLY=($(compgen -W "$(command ps axo comm,pid |
-            awk '{if ($1 ~ /^'"${prev##*/}"'/) print $2}')" -- "$cur"))
+            nawk '{if ($1 ~ /^'"${prev##*/}"'/) print $2}')" -- "$cur"))
         compopt -o filenames
         COMPREPLY+=($(compgen -f -X '!?(*/)core?(.?*)' -o plusdirs \
             -- "$cur"))
diff -pruN bash-completion-2.11.old/completions/getconf bash-completion-2.11/completions/getconf
--- bash-completion-2.11.old/completions/getconf	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/getconf	2021-03-31 13:40:33.207141240 +0000
@@ -12,7 +12,7 @@ _getconf()
             ;;
         -v)
             COMPREPLY=($(compgen -W \
-                '$("$1" -a 2>/dev/null | awk "{ print \$1 }")' -- \
+                '$("$1" -a 2>/dev/null | nawk "{ print \$1 }")' -- \
                 "${cur:-POSIX_V}"))
             return
             ;;
@@ -24,7 +24,7 @@ _getconf()
         COMPREPLY=($(compgen -W '-a -v' -- "$cur"))
     else
         COMPREPLY=($(compgen -W \
-            '$("$1" -a 2>/dev/null | awk "{ print \$1 }")' -- "$cur"))
+            '$("$1" -a 2>/dev/null | nawk "{ print \$1 }")' -- "$cur"))
     fi
 } &&
     complete -F _getconf getconf
diff -pruN bash-completion-2.11.old/completions/getent bash-completion-2.11/completions/getent
--- bash-completion-2.11.old/completions/getent	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/getent	2021-03-31 13:40:32.413694170 +0000
@@ -43,7 +43,7 @@ _getent()
             ;;
         protocols | networks | ahosts | ahostsv4 | ahostsv6 | rpc)
             COMPREPLY=($(compgen -W "$($1 $db |
-                awk '{ print $1 }')" -- "$cur"))
+                nawk '{ print $1 }')" -- "$cur"))
             return
             ;;
         aliases | shadow | gshadow)
diff -pruN bash-completion-2.11.old/completions/gm bash-completion-2.11/completions/gm
--- bash-completion-2.11.old/completions/gm	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/gm	2021-03-31 13:40:32.940151796 +0000
@@ -3,7 +3,7 @@
 _gm_commands()
 {
     COMPREPLY+=($(compgen -W '$("$1" help |
-        awk "/^ +[^ ]+ +- / { print \$1 }")' -- "$cur"))
+        nawk "/^ +[^ ]+ +- / { print \$1 }")' -- "$cur"))
 }
 
 _gm()
diff -pruN bash-completion-2.11.old/completions/gnokii bash-completion-2.11/completions/gnokii
--- bash-completion-2.11.old/completions/gnokii	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/gnokii	2021-03-31 13:40:32.801363834 +0000
@@ -25,7 +25,7 @@ _gnokii()
             done
             [[ ! -f $config_file ]] && return
             COMPREPLY=($(compgen -W \
-                "$(command sed -n 's/^\[phone_\(.*\)\]/\1/p' $config_file)" \
+                "$(command gsed -n 's/^\[phone_\(.*\)\]/\1/p' $config_file)" \
                 -- "$cur"))
             return
             ;;
@@ -224,7 +224,7 @@ _gnokii()
     # safer to use LANG=C
     local all_cmd="$(LANG=C _parse_help $1 "--help all")"
     # these 2 below are allowed in combination with others
-    local main_cmd=$(command grep -v -- '--config\|--phone' <<<"$all_cmd")
+    local main_cmd=$(command ggrep -v -- '--config\|--phone' <<<"$all_cmd")
 
     # don't provide main command completions if one is
     # already on the command line
diff -pruN bash-completion-2.11.old/completions/gpg bash-completion-2.11/completions/gpg
--- bash-completion-2.11.old/completions/gpg	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/gpg	2021-03-31 13:40:32.910502065 +0000
@@ -12,16 +12,16 @@ _gpg()
             ;;
         --export | --sign-key | --lsign-key | --nrsign-key | --nrlsign-key | --edit-key)
             # return list of public keys
-            COMPREPLY=($(compgen -W "$($1 --list-keys 2>/dev/null | command sed -ne \
+            COMPREPLY=($(compgen -W "$($1 --list-keys 2>/dev/null | command gsed -ne \
                 's@^pub.*/\([^ ]*\).*$@\1@p' -ne \
                 's@^.*\(<\([^>]*\)>\).*$@\2@p')" -- "$cur"))
             return
             ;;
         --recipient | -!(-*)r)
-            COMPREPLY=($(compgen -W "$($1 --list-keys 2>/dev/null | command sed -ne \
+            COMPREPLY=($(compgen -W "$($1 --list-keys 2>/dev/null | command gsed -ne \
                 's@^.*<\([^>]*\)>.*$@\1@p')" -- "$cur"))
             if [[ -e ~/.gnupg/gpg.conf ]]; then
-                COMPREPLY+=($(compgen -W "$(command sed -ne \
+                COMPREPLY+=($(compgen -W "$(command gsed -ne \
                     's@^[ \t]*group[ \t][ \t]*\([^=]*\).*$@\1@p' \
                     ~/.gnupg/gpg.conf)" -- "$cur"))
             fi
diff -pruN bash-completion-2.11.old/completions/gpg2 bash-completion-2.11/completions/gpg2
--- bash-completion-2.11.old/completions/gpg2	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/gpg2	2021-03-31 13:40:32.554014927 +0000
@@ -16,16 +16,16 @@ _gpg2()
             ;;
         --export | --sign-key | --lsign-key | --nrsign-key | --nrlsign-key | --edit-key)
             # return list of public keys
-            COMPREPLY=($(compgen -W "$($1 --list-keys 2>/dev/null | command sed -ne \
+            COMPREPLY=($(compgen -W "$($1 --list-keys 2>/dev/null | command gsed -ne \
                 's@^pub.*/\([^ ]*\).*$@\1@p' -ne \
                 's@^.*\(<\([^>]*\)>\).*$@\2@p')" -- "$cur"))
             return
             ;;
         --recipient | -!(-*)r)
             COMPREPLY=($(compgen -W "$($1 --list-keys 2>/dev/null |
-                command sed -ne 's@^.*<\([^>]*\)>.*$@\1@p')" -- "$cur"))
+                command gsed -ne 's@^.*<\([^>]*\)>.*$@\1@p')" -- "$cur"))
             if [[ -e ~/.gnupg/gpg.conf ]]; then
-                COMPREPLY+=($(compgen -W "$(command sed -ne \
+                COMPREPLY+=($(compgen -W "$(command gsed -ne \
                     's@^[ \t]*group[ \t][ \t]*\([^=]*\).*$@\1@p' \
                     ~/.gnupg/gpg.conf)" -- "$cur"))
             fi
diff -pruN bash-completion-2.11.old/completions/gphoto2 bash-completion-2.11/completions/gphoto2
--- bash-completion-2.11.old/completions/gphoto2	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/gphoto2	2021-03-31 13:40:32.843181674 +0000
@@ -24,14 +24,14 @@ _gphoto2()
             ;;
         --port)
             COMPREPLY=($(compgen -W "$($1 --list-ports 2>/dev/null |
-                awk 'NR>3 { print $1 }')" -- "$cur"))
+                nawk 'NR>3 { print $1 }')" -- "$cur"))
             __ltrim_colon_completions "$cur"
             return
             ;;
         --camera)
             local IFS=$'\n'
             COMPREPLY=($(compgen -W "$($1 --list-cameras 2>/dev/null |
-                awk -F'"' 'NR>2 { print $2 }')" -- "$cur"))
+                nawk -F'"' 'NR>2 { print $2 }')" -- "$cur"))
             return
             ;;
         --get-config | --set-config | --set-config-index | --set-config-value)
diff -pruN bash-completion-2.11.old/completions/gssdp-discover bash-completion-2.11/completions/gssdp-discover
--- bash-completion-2.11.old/completions/gssdp-discover	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/gssdp-discover	2021-03-31 13:40:32.601347632 +0000
@@ -15,7 +15,7 @@ _gssdp_discover()
             ;;
         --message-type | -m)
             local types=$($1 --help 2>&1 |
-                command sed -ne 's/^.*--message-type=.*(\([^)]*\))$/\1/p')
+                command gsed -ne 's/^.*--message-type=.*(\([^)]*\))$/\1/p')
             COMPREPLY=($(
                 IFS+=,
                 compgen -W "$types" -- "$cur"
diff -pruN bash-completion-2.11.old/completions/hcitool bash-completion-2.11/completions/hcitool
--- bash-completion-2.11.old/completions/hcitool	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/hcitool	2021-03-31 13:40:32.531106975 +0000
@@ -4,14 +4,14 @@ _bluetooth_addresses()
 {
     if [[ -n ${COMP_BLUETOOTH_SCAN:-} ]]; then
         COMPREPLY+=($(compgen -W "$(hcitool scan |
-            awk '/^\t/{print $1}')" -- "$cur"))
+            nawk '/^\t/{print $1}')" -- "$cur"))
     fi
 }
 
 _bluetooth_devices()
 {
     COMPREPLY+=($(compgen -W "$(hcitool dev |
-        awk '/^\t/{print $1}')" -- "$cur"))
+        nawk '/^\t/{print $1}')" -- "$cur"))
 }
 
 _bluetooth_services()
diff -pruN bash-completion-2.11.old/completions/iconv bash-completion-2.11/completions/iconv
--- bash-completion-2.11.old/completions/iconv	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/iconv	2021-03-31 13:40:32.928560565 +0000
@@ -3,7 +3,7 @@
 _iconv_charsets()
 {
     COMPREPLY+=($(compgen -X ... -W '$(${1:-iconv} -l | \
-        command sed -e "s@/*\$@@" -e "s/[,()]//g")' -- "$cur"))
+        command gsed -e "s@/*\$@@" -e "s/[,()]//g")' -- "$cur"))
 }
 
 _iconv()
diff -pruN bash-completion-2.11.old/completions/ifstat bash-completion-2.11/completions/ifstat
--- bash-completion-2.11.old/completions/ifstat	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/ifstat	2021-03-31 13:40:32.920233629 +0000
@@ -20,9 +20,9 @@ _ifstat()
             if ! {
                 "$1" --help 2>&1 || :
             } |
-                command grep -q -- '-d.*--scan'; then
+                command ggrep -q -- '-d.*--scan'; then
                 COMPREPLY=($(compgen -W '$("$1" -v | command \
-                    sed -e "s/[,.]//g" -ne "s/^.*drivers://p")' -- "$cur"))
+                    gsed -e "s/[,.]//g" -ne "s/^.*drivers://p")' -- "$cur"))
             fi
             return
             ;;
@@ -32,7 +32,7 @@ _ifstat()
             if ! {
                 "$1" --help 2>&1 || :
             } |
-                command grep -q -- '-s.*--noupdate'; then
+                command ggrep -q -- '-s.*--noupdate'; then
                 _known_hosts_real -- "$cur"
                 return
             fi
@@ -43,12 +43,12 @@ _ifstat()
             ! {
                 "$1" --help 2>&1 || :
             } |
-                command grep -q -- '-t.*--interval' || return
+                command ggrep -q -- '-t.*--interval' || return
             ;;
         --extended | -!(-*)x)
             # iproute2: parse xstat types
             COMPREPLY=($(compgen -W '$("$1" -x nonexistent-xstat 2>&1 |
-                awk "found { print \$1 } /supported xstats:/ { found=1 }")' \
+                nawk "found { print \$1 } /supported xstats:/ { found=1 }")' \
                 -- "$cur"))
             return
             ;;
diff -pruN bash-completion-2.11.old/completions/influx bash-completion-2.11/completions/influx
--- bash-completion-2.11.old/completions/influx	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/influx	2021-03-31 13:40:32.317259527 +0000
@@ -14,7 +14,7 @@ _influx()
             return
             ;;
         -format | -precision | -consistency)
-            local args=$($1 --help 2>&1 | awk "\$1 == \"$prev\" { print \$2 }")
+            local args=$($1 --help 2>&1 | nawk "\$1 == \"$prev\" { print \$2 }")
             COMPREPLY=($(
                 IFS+="\"'|"
                 compgen -W "$args" -- "$cur"
diff -pruN bash-completion-2.11.old/completions/inotifywait bash-completion-2.11/completions/inotifywait
--- bash-completion-2.11.old/completions/inotifywait	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/inotifywait	2021-03-31 13:40:32.484560371 +0000
@@ -6,7 +6,7 @@ _inotifywait_events()
     # tab. Word following the tab is event name, others are line
     # wrapped explanations.
     COMPREPLY+=($(compgen -W "$($1 --help 2>/dev/null |
-        command sed -e '/^Events:/,/^[^'$'\t'']/!d' \
+        command gsed -e '/^Events:/,/^[^'$'\t'']/!d' \
             -ne 's/^'$'\t''\([^ '$'\t'']\{1,\}\)[ '$'\t''].*/\1/p')" \
         -- "$cur"))
 }
diff -pruN bash-completion-2.11.old/completions/invoke-rc.d bash-completion-2.11/completions/invoke-rc.d
--- bash-completion-2.11.old/completions/invoke-rc.d	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/invoke-rc.d	2021-03-31 13:40:32.865179908 +0000
@@ -20,12 +20,12 @@ _invoke_rc_d()
     if [[ $cword -eq 1 || $prev == --* ]]; then
         valid_options=($(
             tr " " "\n" <<<"${words[*]} ${options[*]}" |
-                command sed -ne "/$(command sed 's/ /\\|/g' <<<"${options[*]}")/p" |
+                command gsed -ne "/$(command gsed 's/ /\\|/g' <<<"${options[*]}")/p" |
                 sort | uniq -u
         ))
         COMPREPLY=($(compgen -W '${valid_options[@]} ${services[@]}' -- "$cur"))
     elif [[ -x $sysvdir/$prev ]]; then
-        COMPREPLY=($(compgen -W '`command sed -e "y/|/ /" \
+        COMPREPLY=($(compgen -W '`command gsed -e "y/|/ /" \
             -ne "s/^.*Usage:[ ]*[^ ]*[ ]*{*\([^}\"]*\).*$/\1/p" \
             $sysvdir/$prev`' -- "$cur"))
     else
diff -pruN bash-completion-2.11.old/completions/ip bash-completion-2.11/completions/ip
--- bash-completion-2.11.old/completions/ip	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/ip	2021-03-31 13:40:32.838727158 +0000
@@ -3,7 +3,7 @@
 _iproute2_etc()
 {
     COMPREPLY+=($(compgen -W \
-        "$(awk '!/#/ { print $2 }' /etc/iproute2/$1 2>/dev/null)" \
+        "$(nawk '!/#/ { print $2 }' /etc/iproute2/$1 2>/dev/null)" \
         -- "$cur"))
 }
 
@@ -48,7 +48,7 @@ _ip()
                 COMPREPLY=($(compgen -W "$c" -- "$cur"))
                 ;;
             *)
-                COMPREPLY=($(compgen -W "help $($1 help 2>&1 | command sed -e \
+                COMPREPLY=($(compgen -W "help $($1 help 2>&1 | command gsed -e \
                     '/OBJECT := /,/}/!d' -e \
                     's/.*{//' -e \
                     's/}.*//' -e \
@@ -191,7 +191,7 @@ _ip()
                     ;;
                 a | add | d | del | change | append | r | replace | monitor)
                     if [[ $prev == via ]]; then
-                        COMPREPLY=($(compgen -W "$($1 r | command sed -ne \
+                        COMPREPLY=($(compgen -W "$($1 r | command gsed -ne \
                             's/.*via \([0-9.]*\).*/\1/p')" -- "$cur"))
                     elif [[ $prev == "$subcmd" ]]; then
                         COMPREPLY=($(compgen -W "table default \
diff -pruN bash-completion-2.11.old/completions/iperf bash-completion-2.11/completions/iperf
--- bash-completion-2.11.old/completions/iperf	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/iperf	2021-03-31 13:40:32.606694380 +0000
@@ -51,10 +51,10 @@ _iperf()
     for i in "${words[@]}"; do
         case $i in
             -s | --server)
-                filter='command sed -e /^Client.specific/,/^\(Server.specific.*\)\?$/d'
+                filter='command gsed -e /^Client.specific/,/^\(Server.specific.*\)\?$/d'
                 ;;
             -c | --client)
-                filter='command sed -e /^Server.specific/,/^\(Client.specific.*\)\?$/d'
+                filter='command gsed -e /^Server.specific/,/^\(Client.specific.*\)\?$/d'
                 ;;
         esac
     done
diff -pruN bash-completion-2.11.old/completions/ipmitool bash-completion-2.11/completions/ipmitool
--- bash-completion-2.11.old/completions/ipmitool	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/ipmitool	2021-03-31 13:40:32.679574327 +0000
@@ -3,7 +3,7 @@
 _ipmitool_singleline_help()
 {
     COMPREPLY=($(compgen -W "$($1 $2 2>&1 |
-        command sed -ne 's/[,\r]//g' -e 's/^.*[Cc]ommands://p')" -- "$cur"))
+        command gsed -ne 's/[,\r]//g' -e 's/^.*[Cc]ommands://p')" -- "$cur"))
 }
 
 _ipmitool()
@@ -18,14 +18,14 @@ _ipmitool()
         -*d)
             COMPREPLY=($(compgen -W "$(
                 command ls -d /dev/ipmi* /dev/ipmi/* /dev/ipmidev/* \
-                    2>/dev/null | command sed -ne 's/^[^0-9]*\([0-9]\{1,\}\)/\1/p'
+                    2>/dev/null | command gsed -ne 's/^[^0-9]*\([0-9]\{1,\}\)/\1/p'
             )" \
                 -- "$cur"))
             return
             ;;
         -*I)
             COMPREPLY=($(compgen -W "$($1 -h 2>&1 |
-                command sed -e '/^Interfaces:/,/^[[:space:]]*$/!d' \
+                command gsed -e '/^Interfaces:/,/^[[:space:]]*$/!d' \
                     -ne 's/^[[:space:]]\{1,\}\([^[:space:]]\{1,\}\).*/\1/p')" \
                 -- "$cur"))
             return
@@ -53,7 +53,7 @@ _ipmitool()
             ;;
         -*o)
             COMPREPLY=($(compgen -W "$($1 -o list 2>&1 |
-                awk '/^[ \t]+/ { print $1 }') list" -- "$cur"))
+                nawk '/^[ \t]+/ { print $1 }') list" -- "$cur"))
             return
             ;;
     esac
diff -pruN bash-completion-2.11.old/completions/iptables bash-completion-2.11/completions/iptables
--- bash-completion-2.11.old/completions/iptables	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/iptables	2021-03-31 13:40:32.672299733 +0000
@@ -13,7 +13,7 @@ _iptables()
     case $prev in
         -*[AIDRPFXLZ])
             COMPREPLY=($(compgen -W '`"$1" $table -nL 2>/dev/null | \
-                command sed -ne "s/^Chain \([^ ]\{1,\}\).*$/\1/p"`' -- "$cur"))
+                command gsed -ne "s/^Chain \([^ ]\{1,\}\).*$/\1/p"`' -- "$cur"))
             ;;
         -*t)
             COMPREPLY=($(compgen -W 'nat filter mangle' -- "$cur"))
@@ -21,17 +21,17 @@ _iptables()
         -j)
             if [[ $table == "-t filter" || -z $table ]]; then
                 COMPREPLY=($(compgen -W 'ACCEPT DROP LOG ULOG REJECT
-                `"$1" $table -nL 2>/dev/null | command sed -ne "$chain" \
+                `"$1" $table -nL 2>/dev/null | command gsed -ne "$chain" \
                 -e "s/INPUT|OUTPUT|FORWARD|PREROUTING|POSTROUTING//"`' -- \
                     "$cur"))
             elif [[ $table == "-t nat" ]]; then
                 COMPREPLY=($(compgen -W 'ACCEPT DROP LOG ULOG REJECT MIRROR SNAT
                 DNAT MASQUERADE `"$1" $table -nL 2>/dev/null | \
-                command sed -ne "$chain" -e "s/OUTPUT|PREROUTING|POSTROUTING//"`' \
+                command gsed -ne "$chain" -e "s/OUTPUT|PREROUTING|POSTROUTING//"`' \
                     -- "$cur"))
             elif [[ $table == "-t mangle" ]]; then
                 COMPREPLY=($(compgen -W 'ACCEPT DROP LOG ULOG REJECT MARK TOS
-                `"$1" $table -nL 2>/dev/null | command sed -ne "$chain" \
+                `"$1" $table -nL 2>/dev/null | command gsed -ne "$chain" \
                 -e "s/INPUT|OUTPUT|FORWARD|PREROUTING|POSTROUTING//"`' -- \
                     "$cur"))
             fi
@@ -39,7 +39,7 @@ _iptables()
         *)
             if [[ $cur == -* ]]; then
                 COMPREPLY=($(compgen -W '$("$1" --help 2>&1 |
-                command sed -e "s/^\[\!\]//" | _parse_help -)' -- "$cur"))
+                command gsed -e "s/^\[\!\]//" | _parse_help -)' -- "$cur"))
                 [[ ${COMPREPLY-} == *= ]] && compopt -o nospace
             fi
             ;;
diff -pruN bash-completion-2.11.old/completions/ipv6calc bash-completion-2.11/completions/ipv6calc
--- bash-completion-2.11.old/completions/ipv6calc	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/ipv6calc	2021-03-31 13:40:32.803332737 +0000
@@ -10,9 +10,9 @@ _ipv6calc()
             return
             ;;
         --in | --out | --action | -!(-*)[IOA])
-            # With ipv6calc < 0.73.0, -m does nothing here, so use sed instead.
+            # With ipv6calc < 0.73.0, -m does nothing here, so use gsed instead.
             COMPREPLY=($(compgen -W "$($1 "$prev" -h 2>&1 |
-                command sed -ne 's/^[[:space:]]\{1,\}\([^[:space:]:]\{1,\}\)[[:space:]]*:.*/\1/p')" \
+                command gsed -ne 's/^[[:space:]]\{1,\}\([^[:space:]:]\{1,\}\)[[:space:]]*:.*/\1/p')" \
                 -- "$cur"))
             return
             ;;
@@ -29,7 +29,7 @@ _ipv6calc()
 
     if [[ $cur == -* ]]; then
         COMPREPLY=($(compgen -W '$("$1" -h 2>&1 |
-            command sed -e "s/[][]//g" | _parse_help -)' -- "$cur"))
+            command gsed -e "s/[][]//g" | _parse_help -)' -- "$cur"))
     fi
 
 } &&
diff -pruN bash-completion-2.11.old/completions/isql bash-completion-2.11/completions/isql
--- bash-completion-2.11.old/completions/isql	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/isql	2021-03-31 13:40:32.775012074 +0000
@@ -7,7 +7,7 @@ _isql()
     _init_completion || return
 
     [[ -f $ODBCINI ]] &&
-        COMPREPLY=($(command grep "\[$cur" "$ODBCINI" | tr -d \[\]))
+        COMPREPLY=($(command ggrep "\[$cur" "$ODBCINI" | tr -d \[\]))
 } &&
     complete -F _isql isql
 
diff -pruN bash-completion-2.11.old/completions/iwconfig bash-completion-2.11/completions/iwconfig
--- bash-completion-2.11.old/completions/iwconfig	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/iwconfig	2021-03-31 13:40:32.448104981 +0000
@@ -16,7 +16,7 @@ _iwconfig()
             if [[ -n ${COMP_IWLIST_SCAN:-} ]]; then
                 COMPREPLY+=($(compgen -W \
                     "$(iwlist ${words[1]} scan |
-                        awk -F'\"' '/ESSID/ {print $2}')" -- "$cur"))
+                        nawk -F'\"' '/ESSID/ {print $2}')" -- "$cur"))
             fi
             return
             ;;
@@ -26,13 +26,13 @@ _iwconfig()
             ;;
         channel)
             COMPREPLY=($(compgen -W "$(iwlist ${words[1]} channel |
-                awk '/^[ \t]*Channel/ {print $2}')" -- "$cur"))
+                nawk '/^[ \t]*Channel/ {print $2}')" -- "$cur"))
             return
             ;;
 
         freq)
             COMPREPLY=($(compgen -W "$(iwlist ${words[1]} channel |
-                awk '/^[ \t]*Channel/ {print $4"G"}')" -- "$cur"))
+                nawk '/^[ \t]*Channel/ {print $4"G"}')" -- "$cur"))
             return
             ;;
         ap)
@@ -40,7 +40,7 @@ _iwconfig()
             if [[ -n ${COMP_IWLIST_SCAN:-} ]]; then
                 COMPREPLY+=($(compgen -W \
                     "$(iwlist ${words[1]} scan |
-                        awk -F ': ' '/Address/ {print $2}')" -- "$cur"))
+                        nawk -F ': ' '/Address/ {print $2}')" -- "$cur"))
             fi
             return
             ;;
@@ -48,7 +48,7 @@ _iwconfig()
             COMPREPLY=($(compgen -W 'auto fixed' -- "$cur"))
             COMPREPLY+=($(compgen -W \
                 "$(iwlist ${words[1]} rate |
-                    awk '/^[ \t]*[0-9]/ {print $1"M"}')" -- "$cur"))
+                    nawk '/^[ \t]*[0-9]/ {print $1"M"}')" -- "$cur"))
             return
             ;;
         rts | frag)
diff -pruN bash-completion-2.11.old/completions/java bash-completion-2.11/completions/java
--- bash-completion-2.11.old/completions/java	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/java	2021-03-31 13:40:32.334844488 +0000
@@ -63,22 +63,22 @@ _java_classes()
         if [[ $i == *.@(jar|zip) && -r $i ]]; then
             if type zipinfo &>/dev/null; then
                 COMPREPLY+=($(zipinfo -1 "$i" "$cur*" 2>/dev/null |
-                    command grep '^[^$]*\.class$'))
+                    command ggrep '^[^$]*\.class$'))
             elif type unzip &>/dev/null; then
                 # Last column, between entries consisting entirely of dashes
                 COMPREPLY+=($(unzip -lq "$i" "$cur*" 2>/dev/null |
-                    awk '$NF ~ /^-+$/ { flag=!flag; next };
+                    nawk '$NF ~ /^-+$/ { flag=!flag; next };
                          flag && $NF ~ /^[^$]*\.class/ { print $NF }'))
             elif type jar &>/dev/null; then
                 COMPREPLY+=($(jar tf "$i" "$cur" |
-                    command grep '^[^$]*\.class$'))
+                    command ggrep '^[^$]*\.class$'))
             fi
 
         elif [[ -d $i ]]; then
             COMPREPLY+=(
-                $(compgen -d -- "$i/$cur" | command sed -e "s|^$i/\(.*\)|\1.|")
+                $(compgen -d -- "$i/$cur" | command gsed -e "s|^$i/\(.*\)|\1.|")
                 $(compgen -f -X '!*.class' -- "$i/$cur" |
-                    command sed -e '/\$/d' -e "s|^$i/||")
+                    command gsed -e '/\$/d' -e "s|^$i/||")
             )
             [[ ${COMPREPLY-} == *.class ]] || compopt -o nospace
 
@@ -110,12 +110,12 @@ _java_packages()
     for i in ${sourcepath//:/ }; do
         if [[ -d $i ]]; then
             COMPREPLY+=($(command ls -F -d $i/$cur* 2>/dev/null |
-                command sed -e 's|^'$i'/||'))
+                command gsed -e 's|^'$i'/||'))
         fi
     done
     if ((${#COMPREPLY[@]} != 0)); then
         # keep only packages
-        COMPREPLY=($(tr " " "\n" <<<"${COMPREPLY[@]}" | command grep "/$"))
+        COMPREPLY=($(tr " " "\n" <<<"${COMPREPLY[@]}" | command ggrep "/$"))
         # remove packages extension
         COMPREPLY=(${COMPREPLY[@]%/})
         # convert path syntax to package syntax
@@ -309,7 +309,7 @@ _javac()
         # For some reason there may be -g:none AND -g:{lines,source,vars};
         # convert the none case to the curly brace format so it parses like
         # the others.
-        local opts=$("$1" $helpopt 2>&1 | command sed -e 's/-g:none/-g:{none}/' -ne \
+        local opts=$("$1" $helpopt 2>&1 | command gsed -e 's/-g:none/-g:{none}/' -ne \
             "s/^[[:space:]]*${cur%%:*}:{\([^}]\{1,\}\)}.*/\1/p")
         COMPREPLY=($(compgen -W "${opts//,/ }" -- "${cur#*:}"))
         return
diff -pruN bash-completion-2.11.old/completions/koji bash-completion-2.11/completions/koji
--- bash-completion-2.11.old/completions/koji	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/koji	2021-03-31 13:40:33.203549041 +0000
@@ -29,7 +29,7 @@ _koji_tag()
 _koji_target()
 {
     COMPREPLY+=($(compgen -W '$("$1" -q list-targets 2>/dev/null |
-        awk "{ print \$1 }")' -- "$cur"))
+        nawk "{ print \$1 }")' -- "$cur"))
 }
 
 _koji()
@@ -237,7 +237,7 @@ _koji()
         [[ ${COMPREPLY-} == *= ]] && compopt -o nospace
     elif [[ ! -v command ]]; then
         COMPREPLY=($(compgen -W '$("$1" --help-commands 2>/dev/null | \
-            awk "/^(  +|\t)/ { print \$1 }")' -- "$cur"))
+            nawk "/^(  +|\t)/ { print \$1 }")' -- "$cur"))
     fi
 } &&
     complete -F _koji koji arm-koji ppc-koji s390-koji sparc-koji
diff -pruN bash-completion-2.11.old/completions/ktutil bash-completion-2.11/completions/ktutil
--- bash-completion-2.11.old/completions/ktutil	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/ktutil	2021-03-31 13:40:32.915355029 +0000
@@ -3,13 +3,13 @@
 _heimdal_principals()
 {
     COMPREPLY=($(compgen -W "$(kadmin -l dump 2>/dev/null |
-        awk '{print $1}')" -- "$cur"))
+        nawk '{print $1}')" -- "$cur"))
 }
 
 _heimdal_realms()
 {
     COMPREPLY=($(compgen -W "$(kadmin -l dump 2>/dev/null |
-        awk '{print $1}' | awk -F@ '{print $2}')" -- "$cur"))
+        nawk '{print $1}' | nawk -F@ '{print $2}')" -- "$cur"))
 }
 
 _heimdal_encodings()
diff -pruN bash-completion-2.11.old/completions/lilo bash-completion-2.11/completions/lilo
--- bash-completion-2.11.old/completions/lilo	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/lilo	2021-03-31 13:40:33.057552107 +0000
@@ -2,8 +2,8 @@
 
 _lilo_labels()
 {
-    COMPREPLY=($(compgen -W "$(awk -F= '$1 ~ /^[ \t]*label$/ {print $2}' \
-        ${1:-/etc/lilo.conf} 2>/dev/null | command sed -e 's/\"//g')" \
+    COMPREPLY=($(compgen -W "$(nawk -F= '$1 ~ /^[ \t]*label$/ {print $2}' \
+        ${1:-/etc/lilo.conf} 2>/dev/null | command gsed -e 's/\"//g')" \
         -- "$cur"))
 }
 
diff -pruN bash-completion-2.11.old/completions/links bash-completion-2.11/completions/links
--- bash-completion-2.11.old/completions/links	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/links	2021-03-31 13:40:32.894165944 +0000
@@ -28,7 +28,7 @@ _links()
             ;;
         -driver)
             local drivers=$("$1" -driver foo 2>&1 |
-                command sed -ne '$!d' -e '/^[a-z0-9, ]\{1,\}$/s/,/ /gp')
+                command gsed -ne '$!d' -e '/^[a-z0-9, ]\{1,\}$/s/,/ /gp')
             [[ $drivers ]] || drivers='x svgalib fb directfb pmshell atheos'
             COMPREPLY=($(compgen -W "$drivers" -- "$cur"))
             return
@@ -81,7 +81,7 @@ _links()
 
     if [[ $cur == -* ]]; then
         COMPREPLY=($(compgen -W '$(_parse_help "$1" |
-            command grep -vF -- "->")' -- "$cur"))
+            command ggrep -vF -- "->")' -- "$cur"))
         return
     fi
 
diff -pruN bash-completion-2.11.old/completions/lintian bash-completion-2.11/completions/lintian
--- bash-completion-2.11.old/completions/lintian	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/lintian	2021-03-31 13:40:32.390603439 +0000
@@ -4,13 +4,13 @@ _lintian_tags()
 {
     local match search tags
 
-    tags=$(awk '/^Tag/ { print $2 }' /usr/share/lintian/checks/*.desc)
+    tags=$(nawk '/^Tag/ { print $2 }' /usr/share/lintian/checks/*.desc)
     if [[ $cur == *, ]]; then
         search=${cur//,/ }
         for item in $search; do
-            match=$(command grep -nE "^Tag: $item$" \
+            match=$(command ggrep -nE "^Tag: $item$" \
                 /usr/share/lintian/checks/*.desc | cut -d: -f1)
-            tags=$(command sed -e "s/\<$item\>//g" <<<$tags)
+            tags=$(command gsed -e "s/\<$item\>//g" <<<$tags)
         done
         COMPREPLY+=($(compgen -W "$tags"))
     elif [[ $cur == *,* ]]; then
@@ -24,16 +24,16 @@ _lintian_checks()
 {
     local match search todisable checks
 
-    checks=$(awk '/^(Check-Script|Abbrev)/ { print $2 }' \
+    checks=$(nawk '/^(Check-Script|Abbrev)/ { print $2 }' \
         /usr/share/lintian/checks/*.desc)
     if [[ $cur == *, ]]; then
         search=${cur//,/ }
         for item in $search; do
-            match=$(command grep -nE "^(Check-Script|Abbrev): $item$" \
+            match=$(command ggrep -nE "^(Check-Script|Abbrev): $item$" \
                 /usr/share/lintian/checks/*.desc | cut -d: -f1)
-            todisable=$(awk '/^(Check-Script|Abbrev)/ { print $2 }' $match)
+            todisable=$(nawk '/^(Check-Script|Abbrev)/ { print $2 }' $match)
             for name in $todisable; do
-                checks=$(command sed -e "s/\<$name\>//g" <<<$checks)
+                checks=$(command gsed -e "s/\<$name\>//g" <<<$checks)
             done
         done
         COMPREPLY+=($(compgen -W "$checks"))
@@ -48,14 +48,14 @@ _lintian_infos()
 {
     local match search infos
 
-    infos=$(awk '/^Collector/ { print $2 }' \
+    infos=$(nawk '/^Collector/ { print $2 }' \
         /usr/share/lintian/collection/*.desc)
     if [[ $cur == *, ]]; then
         search=${cur//,/ }
         for item in $search; do
-            match=$(command grep -nE "^Collector: $item$" \
+            match=$(command ggrep -nE "^Collector: $item$" \
                 /usr/share/lintian/collection/*.desc | cut -d: -f1)
-            infos=$(command sed -e "s/\<$item\>//g" <<<$infos)
+            infos=$(command gsed -e "s/\<$item\>//g" <<<$infos)
         done
         COMPREPLY+=($(compgen -W "$infos"))
     elif [[ $cur == *,* ]]; then
diff -pruN bash-completion-2.11.old/completions/locale-gen bash-completion-2.11/completions/locale-gen
--- bash-completion-2.11.old/completions/locale-gen	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/locale-gen	2021-03-31 13:40:32.913691350 +0000
@@ -24,7 +24,7 @@ _locale_gen()
     fi
 
     COMPREPLY=($(compgen -W \
-        '$(awk "{ print \$1 }" /usr/share/i18n/SUPPORTED 2>/dev/null)' \
+        '$(nawk "{ print \$1 }" /usr/share/i18n/SUPPORTED 2>/dev/null)' \
         -- "$cur"))
 } &&
     complete -F _locale_gen locale-gen
diff -pruN bash-completion-2.11.old/completions/lvm bash-completion-2.11/completions/lvm
--- bash-completion-2.11.old/completions/lvm	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/lvm	2021-03-31 13:40:33.134293932 +0000
@@ -9,25 +9,25 @@ _lvm_filedir()
 _lvm_volumegroups()
 {
     COMPREPLY=($(compgen -W "$(vgscan 2>/dev/null |
-        command sed -n -e 's|.*Found.*"\(.*\)".*$|\1|p')" -- "$cur"))
+        command gsed -n -e 's|.*Found.*"\(.*\)".*$|\1|p')" -- "$cur"))
 }
 
 _lvm_physicalvolumes_all()
 {
     COMPREPLY=($(compgen -W "$(pvscan 2>/dev/null |
-        command sed -n -e 's|^.*PV \([^ ]*\) .*|\1|p')" -- "$cur"))
+        command gsed -n -e 's|^.*PV \([^ ]*\) .*|\1|p')" -- "$cur"))
 }
 
 _lvm_physicalvolumes()
 {
     COMPREPLY=($(compgen -W "$(pvscan 2>/dev/null |
-        command sed -n -e 's|^.*PV \(.*\) VG.*$|\1|p')" -- "$cur"))
+        command gsed -n -e 's|^.*PV \(.*\) VG.*$|\1|p')" -- "$cur"))
 }
 
 _lvm_logicalvolumes()
 {
     COMPREPLY=($(compgen -W "$(lvscan 2>/dev/null |
-        command sed -n -e "s|^.*'\(.*\)'.*$|\1|p")" -- "$cur"))
+        command gsed -n -e "s|^.*'\(.*\)'.*$|\1|p")" -- "$cur"))
     if [[ $cur == /dev/mapper/* ]]; then
         _filedir
         local i
diff -pruN bash-completion-2.11.old/completions/make bash-completion-2.11/completions/make
--- bash-completion-2.11.old/completions/make	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/make	2021-03-31 13:40:32.771033073 +0000
@@ -6,7 +6,7 @@ _make_target_extract_script()
     shift
 
     local prefix="$1"
-    local prefix_pat=$(command sed 's/[][\,.*^$(){}?+|/]/\\&/g' <<<"$prefix")
+    local prefix_pat=$(command gsed 's/[][\,.*^$(){}?+|/]/\\&/g' <<<"$prefix")
     local basename=${prefix##*/}
     local dirname_len=$((${#prefix} - ${#basename}))
 
@@ -155,7 +155,7 @@ _make()
         COMPREPLY=($(LC_ALL=C \
             $1 -npq __BASH_MAKE_COMPLETION__=1 \
             ${makef+"${makef[@]}"} "${makef_dir[@]}" .DEFAULT 2>/dev/null |
-            command sed -ne "$script"))
+            command gsed -ne "$script"))
 
         if [[ $mode != -d ]]; then
             # Completion will occur if there is only one suggestion
diff -pruN bash-completion-2.11.old/completions/Makefile.in bash-completion-2.11/completions/Makefile.in
--- bash-completion-2.11.old/completions/Makefile.in	2020-07-25 13:50:01.000000000 +0000
+++ bash-completion-2.11/completions/Makefile.in	2021-03-31 13:40:33.212778025 +0000
@@ -42,13 +42,13 @@ am__make_running_with_option = \
       *\\[\ \	]*) \
         bs=\\; \
         sane_makeflags=`printf '%s\n' "$$MAKEFLAGS" \
-          | sed "s/$$bs$$bs[$$bs $$bs	]*//g"`;; \
+          | gsed "s/$$bs$$bs[$$bs $$bs	]*//g"`;; \
     esac; \
   fi; \
   skip_next=no; \
   strip_trailopt () \
   { \
-    flg=`printf '%s\n' "$$flg" | sed "s/$$1.*$$//"`; \
+    flg=`printf '%s\n' "$$flg" | gsed "s/$$1.*$$//"`; \
   }; \
   for flg in $$sane_makeflags; do \
     test $$skip_next = yes && { skip_next=no; continue; }; \
@@ -114,27 +114,27 @@ am__can_run_installinfo = \
     n|no|NO) false;; \
     *) (install-info --version) >/dev/null 2>&1;; \
   esac
-am__vpath_adj_setup = srcdirstrip=`echo "$(srcdir)" | sed 's|.|.|g'`;
+am__vpath_adj_setup = srcdirstrip=`echo "$(srcdir)" | gsed 's|.|.|g'`;
 am__vpath_adj = case $$p in \
-    $(srcdir)/*) f=`echo "$$p" | sed "s|^$$srcdirstrip/||"`;; \
+    $(srcdir)/*) f=`echo "$$p" | gsed "s|^$$srcdirstrip/||"`;; \
     *) f=$$p;; \
   esac;
-am__strip_dir = f=`echo $$p | sed -e 's|^.*/||'`;
+am__strip_dir = f=`echo $$p | gsed -e 's|^.*/||'`;
 am__install_max = 40
 am__nobase_strip_setup = \
-  srcdirstrip=`echo "$(srcdir)" | sed 's/[].[^$$\\*|]/\\\\&/g'`
+  srcdirstrip=`echo "$(srcdir)" | gsed 's/[].[^$$\\*|]/\\\\&/g'`
 am__nobase_strip = \
-  for p in $$list; do echo "$$p"; done | sed -e "s|$$srcdirstrip/||"
+  for p in $$list; do echo "$$p"; done | gsed -e "s|$$srcdirstrip/||"
 am__nobase_list = $(am__nobase_strip_setup); \
   for p in $$list; do echo "$$p $$p"; done | \
-  sed "s| $$srcdirstrip/| |;"' / .*\//!s/ .*/ ./; s,\( .*\)/[^/]*$$,\1,' | \
+  gsed "s| $$srcdirstrip/| |;"' / .*\//!s/ .*/ ./; s,\( .*\)/[^/]*$$,\1,' | \
   $(AWK) 'BEGIN { files["."] = "" } { files[$$2] = files[$$2] " " $$1; \
     if (++n[$$2] == $(am__install_max)) \
       { print $$2, files[$$2]; n[$$2] = 0; files[$$2] = "" } } \
     END { for (dir in files) print dir, files[dir] }'
 am__base_list = \
-  sed '$$!N;$$!N;$$!N;$$!N;$$!N;$$!N;$$!N;s/\n/ /g' | \
-  sed '$$!N;$$!N;$$!N;$$!N;s/\n/ /g'
+  gsed '$$!N;$$!N;$$!N;$$!N;$$!N;$$!N;$$!N;s/\n/ /g' | \
+  gsed '$$!N;$$!N;$$!N;$$!N;s/\n/ /g'
 am__uninstall_files_from_dir = { \
   test -z "$$files" \
     || { test ! -d "$$dir" && test ! -f "$$dir" && test ! -r "$$dir"; } \
@@ -1018,7 +1018,7 @@ install-bashcompDATA: $(bashcomp_DATA)
 uninstall-bashcompDATA:
 	@$(NORMAL_UNINSTALL)
 	@list='$(bashcomp_DATA)'; test -n "$(bashcompdir)" || list=; \
-	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
+	files=`for p in $$list; do echo $$p; done | gsed -e 's|^.*/||'`; \
 	dir='$(DESTDIR)$(bashcompdir)'; $(am__uninstall_files_from_dir)
 tags TAGS:
 
@@ -1031,21 +1031,21 @@ distdir: $(BUILT_SOURCES)
 	$(MAKE) $(AM_MAKEFLAGS) distdir-am
 
 distdir-am: $(DISTFILES)
-	@srcdirstrip=`echo "$(srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
-	topsrcdirstrip=`echo "$(top_srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
+	@srcdirstrip=`echo "$(srcdir)" | gsed 's/[].[^$$\\*]/\\\\&/g'`; \
+	topsrcdirstrip=`echo "$(top_srcdir)" | gsed 's/[].[^$$\\*]/\\\\&/g'`; \
 	list='$(DISTFILES)'; \
 	  dist_files=`for file in $$list; do echo $$file; done | \
-	  sed -e "s|^$$srcdirstrip/||;t" \
+	  gsed -e "s|^$$srcdirstrip/||;t" \
 	      -e "s|^$$topsrcdirstrip/|$(top_builddir)/|;t"`; \
 	case $$dist_files in \
 	  */*) $(MKDIR_P) `echo "$$dist_files" | \
-			   sed '/\//!d;s|^|$(distdir)/|;s,/[^/]*$$,,' | \
+			   gsed '/\//!d;s|^|$(distdir)/|;s,/[^/]*$$,,' | \
 			   sort -u` ;; \
 	esac; \
 	for file in $$dist_files; do \
 	  if test -f $$file || test -d $$file; then d=.; else d=$(srcdir); fi; \
 	  if test -d $$d/$$file; then \
-	    dir=`echo "/$$file" | sed -e 's,/[^/]*$$,,'`; \
+	    dir=`echo "/$$file" | gsed -e 's,/[^/]*$$,,'`; \
 	    if test -d "$(distdir)/$$file"; then \
 	      find "$(distdir)/$$file" -type d ! -perm -700 -exec chmod u+rwx {} \;; \
 	    fi; \
diff -pruN bash-completion-2.11.old/completions/makepkg bash-completion-2.11/completions/makepkg
--- bash-completion-2.11.old/completions/makepkg	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/makepkg	2021-03-31 13:40:33.005026350 +0000
@@ -15,7 +15,7 @@ _makepkg_slackware()
 
     if [[ $cur == -* ]]; then
         COMPREPLY=($(compgen -W \
-            '$($1 | command sed -e "s/^options://" | _parse_help -)' \
+            '$($1 | command gsed -e "s/^options://" | _parse_help -)' \
             -- "$cur"))
         return
     fi
diff -pruN bash-completion-2.11.old/completions/mcrypt bash-completion-2.11/completions/mcrypt
--- bash-completion-2.11.old/completions/mcrypt	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/mcrypt	2021-03-31 13:40:33.061811639 +0000
@@ -23,12 +23,12 @@ _mcrypt()
             ;;
         -a | --algorithm)
             COMPREPLY=($(compgen -W "$($1 --list 2>/dev/null |
-                awk '{print $1}')" -- "$cur"))
+                nawk '{print $1}')" -- "$cur"))
             return
             ;;
         -h | --hash)
             COMPREPLY=($(compgen -W '$($1 --list-hash 2>/dev/null | \
-                command sed -e 1d)' -- "$cur"))
+                command gsed -e 1d)' -- "$cur"))
             return
             ;;
         -k | -s | --key | --keysize)
diff -pruN bash-completion-2.11.old/completions/medusa bash-completion-2.11/completions/medusa
--- bash-completion-2.11.old/completions/medusa	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/medusa	2021-03-31 13:40:32.521688262 +0000
@@ -15,8 +15,8 @@ _medusa()
             return
             ;;
         -*M)
-            COMPREPLY=($(compgen -W "$($1 -d | awk '/^ +\+/ {print $2}' |
-                command sed -e 's/\.mod$//')"))
+            COMPREPLY=($(compgen -W "$($1 -d | nawk '/^ +\+/ {print $2}' |
+                command gsed -e 's/\.mod$//')"))
             return
             ;;
     esac
diff -pruN bash-completion-2.11.old/completions/minicom bash-completion-2.11/completions/minicom
--- bash-completion-2.11.old/completions/minicom	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/minicom	2021-03-31 13:40:33.128992811 +0000
@@ -32,8 +32,8 @@ _minicom()
 
     COMPREPLY=(
         $(printf '%s\n' /etc/minirc.* /etc/minicom/minirc.* ~/.minirc.* |
-            command sed -e '/\*$/d' -e 's/^.*minirc\.//' |
-            command grep "^${cur}"))
+            command gsed -e '/\*$/d' -e 's/^.*minirc\.//' |
+            command ggrep "^${cur}"))
 } &&
     complete -F _minicom -o default minicom
 
diff -pruN bash-completion-2.11.old/completions/modprobe bash-completion-2.11/completions/modprobe
--- bash-completion-2.11.old/completions/modprobe	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/modprobe	2021-03-31 13:40:32.986195634 +0000
@@ -87,7 +87,7 @@ _modprobe()
                     prev=${cur%%=*}
                     cur=${cur#*=}
                     if PATH="$PATH:/sbin" modinfo -p "$module" 2>/dev/null |
-                        command grep -q "^$prev:.*(bool)"; then
+                        command ggrep -q "^$prev:.*(bool)"; then
                         local choices="on off"
                         [[ $cur ]] && choices="1 0 y Y n N on off"
                         COMPREPLY=($(compgen -W "$choices" -- "$cur"))
@@ -95,7 +95,7 @@ _modprobe()
                 else
                     COMPREPLY=($(compgen -S = -W "$(PATH="$PATH:/sbin" \
                         modinfo -p "$module" 2>/dev/null |
-                        awk -F: '!/^[ \t]/ { print $1 }')" -- "$cur"))
+                        nawk -F: '!/^[ \t]/ { print $1 }')" -- "$cur"))
                     [[ ${COMPREPLY-} == *= ]] && compopt -o nospace
                 fi
             else
diff -pruN bash-completion-2.11.old/completions/mplayer bash-completion-2.11/completions/mplayer
--- bash-completion-2.11.old/completions/mplayer	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/mplayer	2021-03-31 13:40:32.634199440 +0000
@@ -4,8 +4,8 @@ _mplayer_options_list()
 {
     cur=${cur%\\}
     COMPREPLY=($(compgen -W "$($1 -noconfig all $2 help 2>/dev/null |
-        command sed -e '/^Available/,/^$/!d' -e '/^Available/d' | awk '{print $1}' |
-        command sed -e 's/:$//' -e 's/^'${2#-}'$//' -e 's/<.*//')" -- "$cur"))
+        command gsed -e '/^Available/,/^$/!d' -e '/^Available/d' | nawk '{print $1}' |
+        command gsed -e 's/:$//' -e 's/^'${2#-}'$//' -e 's/<.*//')" -- "$cur"))
 }
 
 _mplayer()
@@ -57,7 +57,7 @@ _mplayer()
             ;;
         -subcp | -msgcharset)
             local cp
-            cp=($(iconv --list 2>/dev/null | command sed -e "s@//@@;" 2>/dev/null))
+            cp=($(iconv --list 2>/dev/null | command gsed -e "s@//@@;" 2>/dev/null))
             if [[ $cur == "${cur,,}" ]]; then
                 COMPREPLY=($(compgen -W '${cp[@],,}' -- "$cur"))
             else
@@ -272,7 +272,7 @@ _mplayer()
     case $cur in
         -*)
             COMPREPLY=($(compgen -W '$($cmd -noconfig all -list-options 2>/dev/null | \
-                command sed -ne "1,/^[[:space:]]*Name/d" \
+                command gsed -ne "1,/^[[:space:]]*Name/d" \
                     -e "s/^[[:space:]]*/-/" -e "s/[[:space:]:].*//" \
                     -e "/^-\(Total\|.*\*\)\{0,1\}$/!p")' -- "$cur"))
             ;;
diff -pruN bash-completion-2.11.old/completions/msynctool bash-completion-2.11/completions/msynctool
--- bash-completion-2.11.old/completions/msynctool	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/msynctool	2021-03-31 13:40:32.399345675 +0000
@@ -8,13 +8,13 @@ _msynctool()
     case $words in
         --configure)
             COMPREPLY=($(compgen -W "$($1 --showgroup \
-                $prev | awk '/^Member/ {print $2}' | command sed \
+                $prev | nawk '/^Member/ {print $2}' | command gsed \
                 -e 's/:$//')" -- "$cur"))
             return
             ;;
         --addmember)
             COMPREPLY=($(compgen -W '$($1 --listplugins \
-                | command sed -e 1d)' -- "$cur"))
+                | command gsed -e 1d)' -- "$cur"))
             return
             ;;
     esac
@@ -22,12 +22,12 @@ _msynctool()
     case $prev in
         --configure | --addgroup | --delgroup | --showgroup | --sync | --addmember)
             COMPREPLY=($(compgen -W '$($1 --listgroups \
-                | command sed -e 1d)' -- "$cur"))
+                | command gsed -e 1d)' -- "$cur"))
             return
             ;;
         --showformats | --filter-objtype | --slow-sync)
             COMPREPLY=($(compgen -W '$($1 --listobjects \
-                | command sed -e 1d)' -- "$cur"))
+                | command gsed -e 1d)' -- "$cur"))
             return
             ;;
     esac
diff -pruN bash-completion-2.11.old/completions/mtx bash-completion-2.11/completions/mtx
--- bash-completion-2.11.old/completions/mtx	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/mtx	2021-03-31 13:40:33.033455363 +0000
@@ -12,11 +12,11 @@ _mtx()
         inventory status load unload eepos first last next"
 
     tapes=$(mtx status 2>/dev/null |
-        awk '/Storage Element [0-9]+:Full/ { printf "%s ", $3 }')
+        nawk '/Storage Element [0-9]+:Full/ { printf "%s ", $3 }')
     tapes=${tapes//:Full/}
 
     drives=$(mtx status 2>/dev/null |
-        awk '/Data Transfer Element [0-9]+:(Full|Empty)/ { printf "%s ", $4 }')
+        nawk '/Data Transfer Element [0-9]+:(Full|Empty)/ { printf "%s ", $4 }')
     drives=${drives//:Full/}
     drives=${drives//:Empty/}
 
diff -pruN bash-completion-2.11.old/completions/mutt bash-completion-2.11/completions/mutt
--- bash-completion-2.11.old/completions/mutt	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/mutt	2021-03-31 13:40:32.701900216 +0000
@@ -53,7 +53,7 @@ _muttconffiles()
     sofar=" $1 "
     shift
     while [[ ${1-} ]]; do
-        newconffiles=($(command sed -n 's|^source[[:space:]]\{1,\}\([^[:space:]]\{1,\}\).*$|\1|p' "$(eval printf %s $1)"))
+        newconffiles=($(command gsed -n 's|^source[[:space:]]\{1,\}\([^[:space:]]\{1,\}\).*$|\1|p' "$(eval printf %s $1)"))
         for file in ${newconffiles+"${newconffiles[@]}"}; do
             __expand_tilde_by_ref file
             [[ ! -f $file || $sofar == *\ $file\ * ]] && continue
@@ -76,7 +76,7 @@ _muttaliases()
 
     conffiles=($(eval _muttconffiles $muttrc $muttrc))
     # shellcheck disable=SC2046
-    aliases=("$(command sed -n 's|^alias[[:space:]]\{1,\}\([^[:space:]]\{1,\}\).*$|\1|p' \
+    aliases=("$(command gsed -n 's|^alias[[:space:]]\{1,\}\([^[:space:]]\{1,\}\).*$|\1|p' \
         $(eval echo "${conffiles[@]}"))")
     COMPREPLY+=($(compgen -W "${aliases[*]}" -- "$cur"))
 }
@@ -87,13 +87,13 @@ _muttquery()
     local cur=$1 querycmd muttcmd=${words[0]}
     local -a queryresults
 
-    querycmd="$($muttcmd -Q query_command 2>/dev/null | command sed -e 's|^query_command=\"\(.*\)\"$|\1|' -e 's|%s|'$cur'|')"
+    querycmd="$($muttcmd -Q query_command 2>/dev/null | command gsed -e 's|^query_command=\"\(.*\)\"$|\1|' -e 's|%s|'$cur'|')"
     if [[ -z $cur || -z $querycmd ]]; then
         queryresults=()
     else
         __expand_tilde_by_ref querycmd
         queryresults=($($querycmd |
-            command sed -n '2,$s|^\([^[:space:]]\{1,\}\).*|\1|p'))
+            command gsed -n '2,$s|^\([^[:space:]]\{1,\}\).*|\1|p'))
     fi
 
     COMPREPLY+=($(compgen -W "${queryresults[*]}" -- "$cur"))
@@ -106,7 +106,7 @@ _muttfiledir()
 
     muttrc=$(_muttrc)
     if [[ $cur == [=+]* ]]; then
-        folder="$($muttcmd -F "$muttrc" -Q folder 2>/dev/null | command sed -e 's|^folder=\"\(.*\)\"$|\1|')"
+        folder="$($muttcmd -F "$muttrc" -Q folder 2>/dev/null | command gsed -e 's|^folder=\"\(.*\)\"$|\1|')"
         : folder:=~/Mail
 
         # Match any file in $folder beginning with $cur
@@ -117,7 +117,7 @@ _muttfiledir()
         return
     elif [[ $cur == !* ]]; then
         spoolfile="$($muttcmd -F "$muttrc" -Q spoolfile 2>/dev/null |
-            command sed -e 's|^spoolfile=\"\(.*\)\"$|\1|')"
+            command gsed -e 's|^spoolfile=\"\(.*\)\"$|\1|')"
         [[ -n $spoolfile ]] && eval cur="${cur/^!/$spoolfile}"
     fi
     _filedir
diff -pruN bash-completion-2.11.old/completions/mysql bash-completion-2.11/completions/mysql
--- bash-completion-2.11.old/completions/mysql	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/mysql	2021-03-31 13:40:33.120157151 +0000
@@ -22,7 +22,7 @@ _mysql()
             return
             ;;
         --database | -!(-*)D)
-            COMPREPLY=($(compgen -W "$(mysqlshow 2>/dev/null | command sed -ne '2d' -e 's/^|.\([^|]*\)|.*/\1/p')" -- "$cur"))
+            COMPREPLY=($(compgen -W "$(mysqlshow 2>/dev/null | command gsed -ne '2d' -e 's/^|.\([^|]*\)|.*/\1/p')" -- "$cur"))
             return
             ;;
 
@@ -91,7 +91,7 @@ _mysql()
     esac
 
     COMPREPLY=($(compgen -W \
-        "$(mysqlshow 2>/dev/null | command sed -ne '2d' -e 's/^|.\([^|]*\)|.*/\1/p')" \
+        "$(mysqlshow 2>/dev/null | command gsed -ne '2d' -e 's/^|.\([^|]*\)|.*/\1/p')" \
         -- "$cur"))
 } &&
     complete -F _mysql mysql
diff -pruN bash-completion-2.11.old/completions/ncftp bash-completion-2.11/completions/ncftp
--- bash-completion-2.11.old/completions/ncftp	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/ncftp	2021-03-31 13:40:33.164477006 +0000
@@ -17,7 +17,7 @@ _ncftp()
     fi
 
     if [[ $cword -eq 1 && -f ~/.ncftp/bookmarks ]]; then
-        COMPREPLY=($(compgen -W '$(command sed -ne "s/^\([^,]\{1,\}\),.*$/\1/p" \
+        COMPREPLY=($(compgen -W '$(command gsed -ne "s/^\([^,]\{1,\}\),.*$/\1/p" \
             ~/.ncftp/bookmarks)' -- "$cur"))
     fi
 
diff -pruN bash-completion-2.11.old/completions/nmap bash-completion-2.11/completions/nmap
--- bash-completion-2.11.old/completions/nmap	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/nmap	2021-03-31 13:40:32.452252403 +0000
@@ -36,7 +36,7 @@ _nmap()
         # strip everything following a non-option name or = char
         # TODO: should expand -T<0-5> to -T0 ... -T5
         COMPREPLY=($(compgen -W "$(
-            "$1" --help 2>&1 | command sed \
+            "$1" --help 2>&1 | command gsed \
                 -e "s/:.*$//" \
                 -e "s/=.*$/=/" \
                 -e "s/;[[:space:]]*-/ -/g" \
diff -pruN bash-completion-2.11.old/completions/op bash-completion-2.11/completions/op
--- bash-completion-2.11.old/completions/op	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/op	2021-03-31 13:40:33.214499734 +0000
@@ -3,7 +3,7 @@
 _op_commands()
 {
     "$@" --help 2>/dev/null |
-        awk '/^(Available |Sub)commands/{flag=1;next}/^ /&&flag{print $1}'
+        nawk '/^(Available |Sub)commands/{flag=1;next}/^ /&&flag{print $1}'
 }
 
 _op_command_options()
diff -pruN bash-completion-2.11.old/completions/openssl bash-completion-2.11/completions/openssl
--- bash-completion-2.11.old/completions/openssl	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/openssl	2021-03-31 13:40:33.037188164 +0000
@@ -22,16 +22,16 @@ _openssl_sections()
 
     [[ ! -f $config ]] && return
 
-    COMPREPLY=($(compgen -W "$(awk '/\[.*\]/ {print $2}' $config)" -- "$cur"))
+    COMPREPLY=($(compgen -W "$(nawk '/\[.*\]/ {print $2}' $config)" -- "$cur"))
 }
 
 _openssl_digests()
 {
     "$1" dgst -h 2>&1 |
-        awk '/^-.*[ \t]to use the .* message digest algorithm/ { print $1 }'
+        nawk '/^-.*[ \t]to use the .* message digest algorithm/ { print $1 }'
     local -a digests=($("$1" help 2>&1 |
-        command sed -ne '/^Message Digest commands/,/^[[:space:]]*$/p' |
-        command sed -e 1d))
+        command gsed -ne '/^Message Digest commands/,/^[[:space:]]*$/p' |
+        command gsed -e 1d))
     printf "%s\n" "${digests[@]/#/-}"
 }
 
diff -pruN bash-completion-2.11.old/completions/p4 bash-completion-2.11/completions/p4
--- bash-completion-2.11.old/completions/p4	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/p4	2021-03-31 13:40:33.176604318 +0000
@@ -9,7 +9,7 @@ _p4()
     local p4commands p4filetypes
 
     # rename isn't really a command
-    p4commands="$(p4 help commands 2>/dev/null | awk 'NF>3 {print $1}')"
+    p4commands="$(p4 help commands 2>/dev/null | nawk 'NF>3 {print $1}')"
     p4filetypes="ctext cxtext ktext kxtext ltext tempobj ubinary \
         uresource uxbinary xbinary xltext xtempobj xtext \
         text binary resource"
diff -pruN bash-completion-2.11.old/completions/pdftotext bash-completion-2.11/completions/pdftotext
--- bash-completion-2.11.old/completions/pdftotext	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/pdftotext	2021-03-31 13:40:32.409646354 +0000
@@ -11,7 +11,7 @@ _pdftotext()
             ;;
         -enc)
             COMPREPLY=($(compgen -W '$("$1" -listenc 2>/dev/null |
-                command sed -e 1d)' -- "$cur"))
+                command gsed -e 1d)' -- "$cur"))
             return
             ;;
         -eol)
diff -pruN bash-completion-2.11.old/completions/perl bash-completion-2.11/completions/perl
--- bash-completion-2.11.old/completions/perl	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/perl	2021-03-31 13:40:32.824727301 +0000
@@ -133,8 +133,8 @@ _perldoc()
             if [[ $cur == p* ]]; then
                 COMPREPLY+=($(compgen -W \
                     '$(PERLDOC_PAGER=cat "$1" -u perl |  \
-                       command sed -ne "/perl.*Perl overview/,/perlwin32/p" | \
-                       awk "\$NF=2 && \$1 ~ /^perl/ { print \$1 }")' \
+                       command gsed -ne "/perl.*Perl overview/,/perlwin32/p" | \
+                       nawk "\$NF=2 && \$1 ~ /^perl/ { print \$1 }")' \
                     -- "$cur"))
             fi
         fi
diff -pruN bash-completion-2.11.old/completions/pfexec bash-completion-2.11/completions/pfexec
--- bash-completion-2.11.old/completions/pfexec	2020-01-11 19:58:32.000000000 +0000
+++ bash-completion-2.11/completions/pfexec	2021-03-31 13:40:32.930202950 +0000
@@ -19,7 +19,7 @@ _pfexec()
 
   # Get profile names from profile description database, see prof_attr(4)
   if [[ "${prev}" == "-P" ]]; then
-    COMPREPLY=( $(compgen -W "$(awk -F: '/^[A-Z]/ {print $1}' /etc/security/prof_attr)" -P \' -S \' -- "${cur}") )
+    COMPREPLY=( $(compgen -W "$(nawk -F: '/^[A-Z]/ {print $1}' /etc/security/prof_attr)" -P \' -S \' -- "${cur}") )
   fi
   if [[ "${cur}" == -* ]]; then
     COMPREPLY=( "-P" )
diff -pruN bash-completion-2.11.old/completions/pgrep bash-completion-2.11/completions/pgrep
--- bash-completion-2.11.old/completions/pgrep	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/pgrep	2021-03-31 13:40:32.663255741 +0000
@@ -38,7 +38,7 @@ _pgrep()
             [[ $cur == *,* ]] && prefix="${cur%,*},"
             COMPREPLY=(
                 $(IFS="$IFS," compgen -W '$($1 --help 2>&1 |
-                    command sed -ne "s/^[[:space:]]*Available namespaces://p")' \
+                    command gsed -ne "s/^[[:space:]]*Available namespaces://p")' \
                     -- "${cur##*,}"))
             ((${#COMPREPLY[@]} == 1)) && COMPREPLY=(${COMPREPLY/#/$prefix})
             return
@@ -48,7 +48,7 @@ _pgrep()
     if [[ $cur == -* ]]; then
         local help=$(_parse_help "$1")
         [[ $help ]] || help='$("$1" --usage 2>&1 |
-            command sed -e "s/\[-signal\]//" -e "s/\[-SIGNAL\]//" |
+            command gsed -e "s/\[-signal\]//" -e "s/\[-SIGNAL\]//" |
             _parse_usage -)'
         COMPREPLY=($(compgen -W "$help" -- "$cur"))
         [[ $cword -eq 1 && $1 == *pkill ]] && _signals -
diff -pruN bash-completion-2.11.old/completions/pine bash-completion-2.11/completions/pine
--- bash-completion-2.11.old/completions/pine	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/pine	2021-03-31 13:40:32.957819170 +0000
@@ -23,7 +23,7 @@ _pine()
     if [[ $cur == -* ]]; then
         COMPREPLY=($(compgen -W '$(_parse_help "$1" -h)' -- "$cur"))
     else
-        COMPREPLY=($(compgen -W '$(awk "{print \$1}" ~/.addressbook \
+        COMPREPLY=($(compgen -W '$(nawk "{print \$1}" ~/.addressbook \
             2>/dev/null)' -- "$cur"))
     fi
 } &&
diff -pruN bash-completion-2.11.old/completions/pkg bash-completion-2.11/completions/pkg
--- bash-completion-2.11.old/completions/pkg	2020-01-11 19:58:32.000000000 +0000
+++ bash-completion-2.11/completions/pkg	2021-03-31 13:40:32.301868253 +0000
@@ -10,13 +10,13 @@ COMP_WORDBREAKS="${COMP_WORDBREAKS//,},"
 _get_installed_packages()
 {
   # XXX: Lists only last part of package's FMRI, path walker is warranted
-  COMPREPLY=( $(compgen -W "$(pkg list -H $* | awk '{print $1}' | awk -F/ '{print $NF}')" -- "${cur}") )
+  COMPREPLY=( $(compgen -W "$(pkg list -H $* | nawk '{print $1}' | nawk -F/ '{print $NF}')" -- "${cur}") )
 }
 
 _get_values()
 {
   local command="$1"
-  COMPREPLY=( $(compgen -W "$(pkg $command -H | awk '{print $1}')" -- "${cur}") )
+  COMPREPLY=( $(compgen -W "$(pkg $command -H | nawk '{print $1}')" -- "${cur}") )
 }
 
 _get_alternative_output_format()
diff -pruN bash-completion-2.11.old/completions/pkg-config bash-completion-2.11/completions/pkg-config
--- bash-completion-2.11.old/completions/pkg-config	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/pkg-config	2021-03-31 13:40:33.077577733 +0000
@@ -36,7 +36,7 @@ _pkg_config()
         [[ ${COMPREPLY-} == *= ]] && compopt -o nospace
     else
         COMPREPLY=($(compgen -W "$($1 --list-all \
-            2>/dev/null | awk '{print $1}')" -- "$cur"))
+            2>/dev/null | nawk '{print $1}')" -- "$cur"))
         _filedir pc
     fi
 } &&
diff -pruN bash-completion-2.11.old/completions/pkg-get bash-completion-2.11/completions/pkg-get
--- bash-completion-2.11.old/completions/pkg-get	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/pkg-get	2021-03-31 13:40:32.450221960 +0000
@@ -16,7 +16,7 @@ _pkg_get_get_catalog_file()
     conffile="${conffile:-/opt/csw/etc/pkg-get.conf}"
 
     if [[ -z $url ]]; then
-        url=$(awk -F= ' $1=="url" { print $2 }' $conffile)
+        url=$(nawk -F= ' $1=="url" { print $2 }' $conffile)
     fi
 
     catalog_file="${url##*//}"
@@ -50,7 +50,7 @@ _pkg_get_get_catalog_file()
             if [[ $command == @(-[Ddi]|describe|download|install) ]]; then
                 catalog_file=$(_pkg_get_get_catalog_file "$url")
                 if [[ -f $catalog_file ]]; then
-                    local packages_list=$(awk ' $0 ~ /BEGIN PGP SIGNATURE/ { exit } $1 ~ /^Hash:/ || $1 ~ /^ *(-|#|$)/ { next } { print $1 }' $catalog_file)
+                    local packages_list=$(nawk ' $0 ~ /BEGIN PGP SIGNATURE/ { exit } $1 ~ /^Hash:/ || $1 ~ /^ *(-|#|$)/ { next } { print $1 }' $catalog_file)
                     COMPREPLY=($(compgen -W "${packages_list}" -- ${cur}))
                 fi
             fi
diff -pruN bash-completion-2.11.old/completions/pkgadd bash-completion-2.11/completions/pkgadd
--- bash-completion-2.11.old/completions/pkgadd	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/pkgadd	2021-03-31 13:40:32.826707709 +0000
@@ -50,7 +50,7 @@ _pkgadd()
                     pkginst_list="${tmplist[*]}"
                 else
                     pkginst_list="$(strings "$(dequote $device)" |
-                        command grep ^PKG= | sort -u | cut -d= -f2)"
+                        command ggrep ^PKG= | sort -u | cut -d= -f2)"
                 fi
                 COMPREPLY=($(compgen -W "$pkginst_list" -- ${cur}))
             fi
diff -pruN bash-completion-2.11.old/completions/pkgutil bash-completion-2.11/completions/pkgutil
--- bash-completion-2.11.old/completions/pkgutil	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/pkgutil	2021-03-31 13:40:33.205376808 +0000
@@ -66,7 +66,7 @@ _pkgutil()
     if [[ -v command && $cur != -* ]]; then
 
         local mirrors mirror_url
-        mirrors=$(awk -F= ' $1 ~ /^ *mirror *$/ { print $2 }' "${configuration_files[@]}")
+        mirrors=$(nawk -F= ' $1 ~ /^ *mirror *$/ { print $2 }' "${configuration_files[@]}")
         mirrors=${mirrors:-http://mirror.opencsw.org/opencsw/testing}
         for mirror_url in $mirrors; do
             local catalog=$(_pkgutil_url2catalog "$mirror_url")
@@ -74,11 +74,11 @@ _pkgutil()
         done
 
         if [[ $command == -@([dius]|-download|-install|-upgrade|-stream) ]]; then
-            local packages_list=$(awk ' $0 ~ /BEGIN PGP SIGNATURE/ { exit } $1 ~ /^Hash:/ || $1 ~ /^ *(-|#|$)/ { next } { print $1 }' "${catalog_files[@]}")
+            local packages_list=$(nawk ' $0 ~ /BEGIN PGP SIGNATURE/ { exit } $1 ~ /^Hash:/ || $1 ~ /^ *(-|#|$)/ { next } { print $1 }' "${catalog_files[@]}")
             COMPREPLY=($(compgen -W "${packages_list}" -- $cur))
 
         elif [[ $command == @(-r|--remove) ]]; then
-            local packages_list=$(pkginfo | awk ' $2 ~ /^CSW/ { printf ("%s|",$2) }')
+            local packages_list=$(pkginfo | nawk ' $2 ~ /^CSW/ { printf ("%s|",$2) }')
             packages_list=${packages_list%|}
             packages_list=$(nawk " \$3 ~ /^$packages_list\$/ { print \$1 }" "${catalog_files[@]}")
             COMPREPLY=($(compgen -W "${packages_list}" -- $cur))
diff -pruN bash-completion-2.11.old/completions/pngfix bash-completion-2.11/completions/pngfix
--- bash-completion-2.11.old/completions/pngfix	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/pngfix	2021-03-31 13:40:33.143497074 +0000
@@ -15,7 +15,7 @@ _pngfix()
             ;;
         --strip)
             COMPREPLY=($(IFS='|' compgen -W '$("$1" --help 2>&1 |
-                command sed -ne "s/.*--strip=\[\([^]]*\)\].*/\1/p")' \
+                command gsed -ne "s/.*--strip=\[\([^]]*\)\].*/\1/p")' \
                 -- "$cur"))
             return
             ;;
diff -pruN bash-completion-2.11.old/completions/portinstall bash-completion-2.11/completions/portinstall
--- bash-completion-2.11.old/completions/portinstall	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/portinstall	2021-03-31 13:40:32.618661005 +0000
@@ -18,9 +18,9 @@ _portinstall()
 
     [[ $prev == -l || $prev == -L || $prev == -o ]] && return
 
-    COMPREPLY=($(command grep -E "^$cur" 2>/dev/null <$indexfile |
+    COMPREPLY=($(command ggrep -E "^$cur" 2>/dev/null <$indexfile |
         cut -d'|' -f1))
-    COMPREPLY2=($(command grep -E "^[^\|]+\|$portsdir$cur" 2>/dev/null \
+    COMPREPLY2=($(command ggrep -E "^[^\|]+\|$portsdir$cur" 2>/dev/null \
         <$indexfile | cut -d'|' -f2))
     COMPREPLY2=(${COMPREPLY2[@]#$portsdir})
     COMPREPLY+=("${COMPREPLY2[@]}")
diff -pruN bash-completion-2.11.old/completions/postcat bash-completion-2.11/completions/postcat
--- bash-completion-2.11.old/completions/postcat	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/postcat	2021-03-31 13:40:32.547675902 +0000
@@ -24,7 +24,7 @@ _postcat()
     if ((qfile == 1)); then
         local len=${#cur} pval
         for pval in $(mailq 2>/dev/null |
-            command sed -e '1d; $d; /^[^0-9A-Z]/d; /^$/d; s/[* !].*$//'); do
+            command gsed -e '1d; $d; /^[^0-9A-Z]/d; /^$/d; s/[* !].*$//'); do
             if [[ $cur == "${pval:0:len}" ]]; then
                 COMPREPLY+=($pval)
             fi
diff -pruN bash-completion-2.11.old/completions/postsuper bash-completion-2.11/completions/postsuper
--- bash-completion-2.11.old/completions/postsuper	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/postsuper	2021-03-31 13:40:32.488280626 +0000
@@ -15,7 +15,7 @@ _postsuper()
         -[dr])
             len=${#cur}
             for pval in ALL $(mailq 2>/dev/null |
-                command sed -e '1d; $d; /^[^0-9A-Z]/d; /^$/d; s/[* !].*$//'); do
+                command gsed -e '1d; $d; /^[^0-9A-Z]/d; /^$/d; s/[* !].*$//'); do
                 if [[ $cur == "${pval:0:len}" ]]; then
                     COMPREPLY+=($pval)
                 fi
@@ -25,7 +25,7 @@ _postsuper()
         -h)
             len=${#cur}
             for pval in ALL $(mailq 2>/dev/null |
-                command sed -e '1d; $d; /^[^0-9A-Z]/d; /^$/d; s/[* ].*$//; /!$/d'); do
+                command gsed -e '1d; $d; /^[^0-9A-Z]/d; /^$/d; s/[* ].*$//; /!$/d'); do
                 if [[ $cur == "${pval:0:len}" ]]; then
                     COMPREPLY+=($pval)
                 fi
@@ -35,7 +35,7 @@ _postsuper()
         -H)
             len=${#cur}
             for pval in ALL $(mailq 2>/dev/null |
-                command sed -e '1d; $d; /^[^0-9A-Z]/d; /^$/d; /^[0-9A-Z]*[* ]/d; s/!.*$//'); do
+                command gsed -e '1d; $d; /^[^0-9A-Z]/d; /^$/d; /^[0-9A-Z]*[* ]/d; s/!.*$//'); do
                 if [[ $cur == "${pval:0:len}" ]]; then
                     COMPREPLY+=($pval)
                 fi
diff -pruN bash-completion-2.11.old/completions/povray bash-completion-2.11/completions/povray
--- bash-completion-2.11.old/completions/povray	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/povray	2021-03-31 13:40:32.631812521 +0000
@@ -23,7 +23,7 @@ _povray()
             # guess what output file type user may want
             case $(
                 IFS=$'\n'
-                command grep '^[-+]F' <<<"${words[*]}"
+                command ggrep '^[-+]F' <<<"${words[*]}"
             ) in
                 [-+]FN) oext=png ;;
                 [-+]FP) oext=ppm ;;
@@ -33,7 +33,7 @@ _povray()
             # complete filename corresponding to previously specified +I
             COMPREPLY=($(
                 IFS=$'\n'
-                command grep '^[-+]I' <<<"${words[*]}"
+                command ggrep '^[-+]I' <<<"${words[*]}"
             ))
             COMPREPLY=(${COMPREPLY[@]#[-+]I})
             COMPREPLY=(${COMPREPLY[@]/%.pov/.$oext})
@@ -47,7 +47,7 @@ _povray()
             cur="${povcur#*\[}"
             pfx="${povcur%\["$cur"}" # prefix == filename
             [[ -r $pfx ]] || return
-            COMPREPLY=($(command sed -e 's/^[[:space:]]*\[\('"$cur"'[^]]*\]\).*$/\1/' \
+            COMPREPLY=($(command gsed -e 's/^[[:space:]]*\[\('"$cur"'[^]]*\]\).*$/\1/' \
                 -e 't' -e 'd' -- "$pfx"))
             # to prevent [bar] expand to nothing.  can be done more easily?
             COMPREPLY=("${COMPREPLY[@]/#/${pfx}[}")
diff -pruN bash-completion-2.11.old/completions/psql bash-completion-2.11/completions/psql
--- bash-completion-2.11.old/completions/psql	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/psql	2021-03-31 13:40:32.465922149 +0000
@@ -5,7 +5,7 @@ _pg_databases()
     # -w was introduced in 8.4, https://launchpad.net/bugs/164772
     # "Access privileges" in output may contain linefeeds, hence the NF > 1
     COMPREPLY=($(compgen -W "$(psql -XAtqwlF $'\t' 2>/dev/null |
-        awk 'NF > 1 { print $1 }')" -- "$cur"))
+        nawk 'NF > 1 { print $1 }')" -- "$cur"))
 }
 
 _pg_users()
diff -pruN bash-completion-2.11.old/completions/puppet bash-completion-2.11/completions/puppet
--- bash-completion-2.11.old/completions/puppet	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/puppet	2021-03-31 13:40:33.172709269 +0000
@@ -22,7 +22,7 @@ _puppet_certs()
         puppetca=puppetca
 
     if [[ $1 == --all ]]; then
-        cert_list=$($puppetca --list --all | command sed -e 's/^[+-]\{0,1\}\s*\(\S\+\)\s\+.*$/\1/')
+        cert_list=$($puppetca --list --all | command gsed -e 's/^[+-]\{0,1\}\s*\(\S\+\)\s\+.*$/\1/')
     else
         cert_list=$($puppetca --list)
     fi
@@ -31,7 +31,7 @@ _puppet_certs()
 
 _puppet_types()
 {
-    puppet_types=$(puppet describe --list | command sed -e 's/^\(\S\{1,\}\).*$/\1/')
+    puppet_types=$(puppet describe --list | command gsed -e 's/^\(\S\{1,\}\).*$/\1/')
     COMPREPLY+=($(compgen -W "$puppet_types" -- "$cur"))
 }
 
@@ -41,7 +41,7 @@ _puppet_references()
     PATH=$PATH:/sbin:/usr/sbin:/usr/local/sbin type puppetdoc &>/dev/null &&
         puppetdoc=puppetdoc
 
-    puppet_doc_list=$($puppetdoc --list | command sed -e 's/^\(\S\{1,\}\).*$/\1/')
+    puppet_doc_list=$($puppetdoc --list | command gsed -e 's/^\(\S\{1,\}\).*$/\1/')
     COMPREPLY+=($(compgen -W "$puppet_doc_list" -- "$cur"))
 }
 
diff -pruN bash-completion-2.11.old/completions/pydoc bash-completion-2.11/completions/pydoc
--- bash-completion-2.11.old/completions/pydoc	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/pydoc	2021-03-31 13:40:32.615375100 +0000
@@ -17,7 +17,7 @@ _pydoc()
 
     if [[ $cur == -* ]]; then
         COMPREPLY=($(compgen -W \
-            '$("$1" | command sed -e "s/^pydoc3\{0,1\} //" | _parse_help -)' \
+            '$("$1" | command gsed -e "s/^pydoc3\{0,1\} //" | _parse_help -)' \
             -- "$cur"))
         return
     fi
@@ -33,7 +33,7 @@ _pydoc()
     # Note that we don't do "pydoc modules" as it is known to hang on
     # some systems; _python_modules tends to work better and faster.
     COMPREPLY+=($(compgen -W \
-        '$($1 keywords topics | command sed -e /^Here/d)' -- "$cur"))
+        '$($1 keywords topics | command gsed -e /^Here/d)' -- "$cur"))
 
     _filedir py
 } &&
diff -pruN bash-completion-2.11.old/completions/pytest bash-completion-2.11/completions/pytest
--- bash-completion-2.11.old/completions/pytest	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/pytest	2021-03-31 13:40:32.708647299 +0000
@@ -76,7 +76,7 @@ _pytest()
             ;;
         --dist)
             local modes=$("$1" --dist=nonexistent-distmode 2>&1 |
-                command sed -e 's/[^[:space:][:alnum:]-]\{1,\}//g' \
+                command gsed -e 's/[^[:space:][:alnum:]-]\{1,\}//g' \
                     -ne 's/.*choose from //p')
             COMPREPLY=($(compgen -W '$modes' -- "$cur"))
             return
diff -pruN bash-completion-2.11.old/completions/qdbus bash-completion-2.11/completions/qdbus
--- bash-completion-2.11.old/completions/qdbus	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/qdbus	2021-03-31 13:40:32.937992014 +0000
@@ -7,7 +7,7 @@ _qdbus()
 
     [[ -n $cur ]] && unset "words[$((${#words[@]} - 1))]"
     COMPREPLY=($(compgen -W '$(command ${words[@]} 2>/dev/null | \
-        command sed "s/(.*)//")' -- "$cur"))
+        command gsed "s/(.*)//")' -- "$cur"))
 } &&
     complete -F _qdbus qdbus dcop
 
diff -pruN bash-completion-2.11.old/completions/qemu bash-completion-2.11/completions/qemu
--- bash-completion-2.11.old/completions/qemu	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/qemu	2021-03-31 13:40:32.996835139 +0000
@@ -26,17 +26,17 @@ _qemu()
             return
             ;;
         -soundhw)
-            COMPREPLY=($(compgen -W "$($1 -soundhw help | awk \
+            COMPREPLY=($(compgen -W "$($1 -soundhw help | nawk \
                 '/^[[:lower:]]/ {print $1}') all" -- "$cur"))
             return
             ;;
         -machine | -M)
-            COMPREPLY=($(compgen -W "$($1 $prev help | awk \
+            COMPREPLY=($(compgen -W "$($1 $prev help | nawk \
                 '/^[[:lower:]]/ {print $1}')" -- "$cur"))
             return
             ;;
         -cpu)
-            COMPREPLY=($(compgen -W "$($1 -cpu help | awk '{print $2}')" \
+            COMPREPLY=($(compgen -W "$($1 -cpu help | nawk '{print $2}')" \
                 -- "$cur"))
             return
             ;;
@@ -82,7 +82,7 @@ _qemu()
             ;;
         -watchdog)
             COMPREPLY=($(compgen -W "$($1 -watchdog help 2>&1 |
-                awk '{print $1}')" -- "$cur"))
+                nawk '{print $1}')" -- "$cur"))
             return
             ;;
         -watchdog-action)
diff -pruN bash-completion-2.11.old/completions/quota bash-completion-2.11/completions/quota
--- bash-completion-2.11.old/completions/quota	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/quota	2021-03-31 13:40:32.806886398 +0000
@@ -32,7 +32,7 @@ _filesystems()
 {
     #  Only list filesystems starting with "/", otherwise we also get
     #+ "binfmt_misc", "proc", "tmpfs", ...
-    COMPREPLY=($(compgen -W "$(awk '/^\// {print $1}' /etc/mtab)" \
+    COMPREPLY=($(compgen -W "$(nawk '/^\// {print $1}' /etc/mtab)" \
         -- "$cur"))
 }
 
diff -pruN bash-completion-2.11.old/completions/rdesktop bash-completion-2.11/completions/rdesktop
--- bash-completion-2.11.old/completions/rdesktop	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/rdesktop	2021-03-31 13:40:32.386731437 +0000
@@ -9,7 +9,7 @@ _rdesktop()
         -*k)
             COMPREPLY=($(command ls \
                 /usr/share/rdesktop/keymaps 2>/dev/null |
-                command grep -E -v '(common|modifiers)'))
+                command ggrep -E -v '(common|modifiers)'))
             COMPREPLY+=($(command ls $HOME/.rdesktop/keymaps 2>/dev/null))
             COMPREPLY+=($(command ls ./keymaps 2>/dev/null))
             COMPREPLY=($(compgen -W '${COMPREPLY[@]}' -- "$cur"))
diff -pruN bash-completion-2.11.old/completions/reportbug bash-completion-2.11/completions/reportbug
--- bash-completion-2.11.old/completions/reportbug	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/reportbug	2021-03-31 13:40:33.195368399 +0000
@@ -63,7 +63,7 @@ _reportbug()
             ;;
         --from-buildd)
             COMPREPLY=($(compgen -S "_" -W '$(apt-cache dumpavail | \
-                command grep "^Source: $cur" | sort -u | cut -f2 -d" ")'))
+                command ggrep "^Source: $cur" | sort -u | cut -f2 -d" ")'))
             return
             ;;
         --smtphost)
diff -pruN bash-completion-2.11.old/completions/rpcdebug bash-completion-2.11/completions/rpcdebug
--- bash-completion-2.11.old/completions/rpcdebug	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/rpcdebug	2021-03-31 13:40:32.629690145 +0000
@@ -13,7 +13,7 @@ _rpcdebug_flags()
 
     if [[ -n $module ]]; then
         COMPREPLY=($(compgen -W "$(rpcdebug -vh 2>&1 |
-            command sed -ne 's/^'$module'[[:space:]]\{1,\}//p')" -- "$cur"))
+            command gsed -ne 's/^'$module'[[:space:]]\{1,\}//p')" -- "$cur"))
     fi
 }
 
diff -pruN bash-completion-2.11.old/completions/rpm bash-completion-2.11/completions/rpm
--- bash-completion-2.11.old/completions/rpm	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/rpm	2021-03-31 13:40:32.950885290 +0000
@@ -7,7 +7,7 @@ _rpm_installed_packages()
     if [[ -r /var/log/rpmpkgs && \
         /var/log/rpmpkgs -nt /var/lib/rpm/Packages ]]; then
         # using RHL 7.2 or later - this is quicker than querying the DB
-        COMPREPLY=($(compgen -W "$(command sed -ne \
+        COMPREPLY=($(compgen -W "$(command gsed -ne \
             's|^\([^[:space:]]\{1,\}\)-[^[:space:]-]\{1,\}-[^[:space:]-]\{1,\}\.rpm$|\1|p' \
             /var/log/rpmpkgs)" -- "$cur"))
     elif type rpmqpack &>/dev/null; then
@@ -29,14 +29,14 @@ _rpm_groups()
 _rpm_macros()
 {
     # get a list of macros
-    COMPREPLY=($(compgen -W "$(${1:-rpm} --showrc | command sed -ne \
+    COMPREPLY=($(compgen -W "$(${1:-rpm} --showrc | command gsed -ne \
         's/^-\{0,1\}[0-9]\{1,\}[:=][[:space:]]\{1,\}\([^[:space:](]\{3,\}\).*/%\1/p')" \
         -- "$cur"))
 }
 
 _rpm_buildarchs()
 {
-    COMPREPLY=($(compgen -W "$(${1:-rpm} --showrc | command sed -ne \
+    COMPREPLY=($(compgen -W "$(${1:-rpm} --showrc | command gsed -ne \
         's/^\s*compatible\s\s*build\s\s*archs\s*:\s*\(.*\)/\1/ p')" \
         -- "$cur"))
 }
@@ -105,7 +105,7 @@ _rpm()
                 esac
                 COMPREPLY=($(compgen -W "$($1 -qa --nodigest --nosignature \
                     --queryformat=\"$fmt\\n\" 2>/dev/null |
-                    command grep -vF '(none)')" -- "$cur"))
+                    command ggrep -vF '(none)')" -- "$cur"))
             fi
             return
             ;;
@@ -261,7 +261,7 @@ _rpmbuild()
             local cfgdir=$($rpm --eval '%{_rpmconfigdir}' 2>/dev/null)
             if [[ $cfgdir ]]; then
                 COMPREPLY=($(compgen -W "$(command ls $cfgdir 2>/dev/null |
-                    command sed -ne 's/^brp-//p')" -- "$cur"))
+                    command gsed -ne 's/^brp-//p')" -- "$cur"))
             fi
             ;;
         --define | --with | --without | -!(-*)D)
diff -pruN bash-completion-2.11.old/completions/sbopkg bash-completion-2.11/completions/sbopkg
--- bash-completion-2.11.old/completions/sbopkg	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/sbopkg	2021-03-31 13:40:32.585348692 +0000
@@ -61,7 +61,7 @@ _sbopkg()
     done
     [[ -r $REPO_ROOT/$REPO_NAME/$REPO_BRANCH/SLACKBUILDS.TXT ]] || return
 
-    COMPREPLY=($(command sed -ne "/^SLACKBUILD NAME: $cur/{s/^SLACKBUILD NAME: //;p}" \
+    COMPREPLY=($(command gsed -ne "/^SLACKBUILD NAME: $cur/{s/^SLACKBUILD NAME: //;p}" \
         $REPO_ROOT/$REPO_NAME/$REPO_BRANCH/SLACKBUILDS.TXT)
     $(
         cd $QUEUEDIR
diff -pruN bash-completion-2.11.old/completions/screen bash-completion-2.11/completions/screen
--- bash-completion-2.11.old/completions/screen	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/screen	2021-03-31 13:40:33.147375531 +0000
@@ -2,7 +2,7 @@
 
 _screen_sessions()
 {
-    local sessions=($(command screen -ls | command sed -ne \
+    local sessions=($(command screen -ls | command gsed -ne \
         's|^\t\{1,\}\([0-9]\{1,\}\.[^\t]\{1,\}\).*'"$1"'.*$|\1|p'))
     if [[ $cur == +([0-9])?(.*) ]]; then
         # Complete sessions including pid prefixes
diff -pruN bash-completion-2.11.old/completions/scrub bash-completion-2.11/completions/scrub
--- bash-completion-2.11.old/completions/scrub	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/scrub	2021-03-31 13:40:32.610280017 +0000
@@ -11,7 +11,7 @@ _scrub()
             ;;
         --pattern | -!(-*)p)
             COMPREPLY=($(compgen -W '$("$1" --help 2>&1 |
-                awk "/^Available/{flag=1;next}/^ /&&flag{print \$1}")' \
+                nawk "/^Available/{flag=1;next}/^ /&&flag{print \$1}")' \
                 -- "$cur"))
             return
             ;;
diff -pruN bash-completion-2.11.old/completions/shellcheck bash-completion-2.11/completions/shellcheck
--- bash-completion-2.11.old/completions/shellcheck	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/shellcheck	2021-03-31 13:40:32.563859380 +0000
@@ -3,7 +3,7 @@
 _shellcheck_optarg()
 {
     local args=$("$1" --help 2>&1 |
-        command sed -e 's/,/ /g' -ne 's/^.*'$2'\>.*(\([^)]*\)).*/\1/p')
+        command gsed -e 's/,/ /g' -ne 's/^.*'$2'\>.*(\([^)]*\)).*/\1/p')
     COMPREPLY+=($(compgen -W '$args' -- "$cur"))
 }
 
@@ -21,8 +21,8 @@ _shellcheck()
             ;;
         --format | -!(-*)f)
             local args=$("$1" --format=nonexistent-format /dev/null 2>&1 |
-                command sed -ne '/^Supported formats/,//p' |
-                command sed -ne '/^[[:space:]]/p')
+                command gsed -ne '/^Supported formats/,//p' |
+                command gsed -ne '/^[[:space:]]/p')
             COMPREPLY=($(compgen -W '$args' -- "$cur"))
             return
             ;;
diff -pruN bash-completion-2.11.old/completions/sitecopy bash-completion-2.11/completions/sitecopy
--- bash-completion-2.11.old/completions/sitecopy	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/sitecopy	2021-03-31 13:40:32.516307814 +0000
@@ -42,7 +42,7 @@ _sitecopy()
 
     if [[ -r ~/.sitecopyrc ]]; then
         COMPREPLY=($(compgen -W "$($1 -v |
-            command sed -n '/^Site:/s/Site: //p')" -- "$cur"))
+            command gsed -n '/^Site:/s/Site: //p')" -- "$cur"))
     fi
 } &&
     complete -F _sitecopy -o default sitecopy
diff -pruN bash-completion-2.11.old/completions/slackpkg bash-completion-2.11/completions/slackpkg
--- bash-completion-2.11.old/completions/slackpkg	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/slackpkg	2021-03-31 13:40:32.988378554 +0000
@@ -84,12 +84,12 @@ _slackpkg()
             COMPREPLY+=($(compgen -W 'a ap d e f k kde kdei l n t tcl x
                 xap xfce y' -- "$cur"))
             COMPREPLY+=($(cut -f 6 -d\  "${WORKDIR}/pkglist" 2>/dev/null |
-                command grep "^$cur"))
+                command ggrep "^$cur"))
             return
             ;;
         info)
             COMPREPLY=($(cut -f 6 -d\  "${WORKDIR}/pkglist" 2>/dev/null |
-                command grep "^$cur"))
+                command ggrep "^$cur"))
             return
             ;;
         update)
diff -pruN bash-completion-2.11.old/completions/slapt-get bash-completion-2.11/completions/slapt-get
--- bash-completion-2.11.old/completions/slapt-get	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/slapt-get	2021-03-31 13:40:32.467944661 +0000
@@ -59,12 +59,12 @@ _slapt_get()
             # it can search for names only
             local name=${cur%%-*}
             COMPREPLY=($(LC_ALL=C "$1" -c "$config" --search "^$name" \
-                2>/dev/null | LC_ALL=C command sed -ne "/^$cur/{s/ .*$//;p}"))
+                2>/dev/null | LC_ALL=C command gsed -ne "/^$cur/{s/ .*$//;p}"))
             return
             ;;
         avl) # --install|-i|
             COMPREPLY=($(LC_ALL=C "$1" -c "$config" --available \
-                2>/dev/null | LC_ALL=C command sed -ne "/^$cur/{s/ .*$//;p}"))
+                2>/dev/null | LC_ALL=C command gsed -ne "/^$cur/{s/ .*$//;p}"))
             return
             ;;
         ins) # --remove|--filelist
diff -pruN bash-completion-2.11.old/completions/slapt-src bash-completion-2.11/completions/slapt-src
--- bash-completion-2.11.old/completions/slapt-src	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/slapt-src	2021-03-31 13:40:32.557783279 +0000
@@ -56,11 +56,11 @@ _slapt_src()
     if [[ $cur == *:* ]]; then
         local name=${cur%:*}
         COMPREPLY=($(LC_ALL=C "$1" --config "$config" --search "^$name" \
-            2>/dev/null | LC_ALL=C command sed -ne \
+            2>/dev/null | LC_ALL=C command gsed -ne \
             "/^$cur/{s/^$name:\([^ ]*\) .*$/\1/;p}"))
     else
         COMPREPLY=($(LC_ALL=C "$1" --config "$config" --search "^$cur" \
-            2>/dev/null | LC_ALL=C command sed -ne "/^$cur/{s/ .*$//;p}"))
+            2>/dev/null | LC_ALL=C command gsed -ne "/^$cur/{s/ .*$//;p}"))
     fi
 } &&
     complete -F _slapt_src slapt-src
diff -pruN bash-completion-2.11.old/completions/smbclient bash-completion-2.11/completions/smbclient
--- bash-completion-2.11.old/completions/smbclient	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/smbclient	2021-03-31 13:40:33.048588017 +0000
@@ -17,7 +17,7 @@ _samba_hosts()
     if [[ -n ${COMP_SAMBA_SCAN:-} ]]; then
         COMPREPLY=($(compgen -W "$(
             smbtree -N -S |
-                command sed -ne 's/^[[:space:]]*\\\\*\([^[:space:]]*\).*/\1/p'
+                command gsed -ne 's/^[[:space:]]*\\\\*\([^[:space:]]*\).*/\1/p'
         )" -- "$cur"))
     fi
 }
diff -pruN bash-completion-2.11.old/completions/ss bash-completion-2.11/completions/ss
--- bash-completion-2.11.old/completions/ss	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/ss	2021-03-31 13:40:32.523616202 +0000
@@ -18,7 +18,7 @@ _ss()
             local prefix=
             [[ $cur == *,* ]] && prefix="${cur%,*},"
             COMPREPLY=($(compgen -W '$("$1" --help | \
-                command sed -e "s/|/ /g" -ne "s/.*QUERY := {\([^}]*\)}.*/\1/p")' \
+                command gsed -e "s/|/ /g" -ne "s/.*QUERY := {\([^}]*\)}.*/\1/p")' \
                 -- "${cur##*,}"))
             ((${#COMPREPLY[@]} == 1)) && COMPREPLY=(${COMPREPLY/#/$prefix})
             return
diff -pruN bash-completion-2.11.old/completions/ssh bash-completion-2.11/completions/ssh
--- bash-completion-2.11.old/completions/ssh	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/ssh	2021-03-31 13:40:33.007419741 +0000
@@ -416,7 +416,7 @@ _scp_remote_files()
 
     # unescape (3 backslashes to 1 for chars we escaped)
     # shellcheck disable=SC2090
-    path=$(command sed -e 's/\\\\\\\('$_scp_path_esc'\)/\\\1/g' <<<"$path")
+    path=$(command gsed -e 's/\\\\\\\('$_scp_path_esc'\)/\\\1/g' <<<"$path")
 
     # default to home dir of specified user on remote host
     if [[ -z $path ]]; then
@@ -429,14 +429,14 @@ _scp_remote_files()
         # shellcheck disable=SC2090
         files=$(ssh -o 'Batchmode yes' $userhost \
             command ls -aF1dL "$path*" 2>/dev/null |
-            command sed -e 's/'$_scp_path_esc'/\\\\\\&/g' -e '/[^\/]$/d')
+            command gsed -e 's/'$_scp_path_esc'/\\\\\\&/g' -e '/[^\/]$/d')
     else
         # escape problematic characters; remove executables, aliases, pipes
         # and sockets; add space at end of file names
         # shellcheck disable=SC2090
         files=$(ssh -o 'Batchmode yes' $userhost \
             command ls -aF1dL "$path*" 2>/dev/null |
-            command sed -e 's/'$_scp_path_esc'/\\\\\\&/g' -e 's/[*@|=]$//g' \
+            command gsed -e 's/'$_scp_path_esc'/\\\\\\&/g' -e 's/[*@|=]$//g' \
                 -e 's/[^\/]$/& /g')
     fi
     COMPREPLY+=($files)
@@ -458,10 +458,10 @@ _scp_local_files()
 
     if $dirsonly; then
         COMPREPLY+=($(command ls -aF1dL $cur* 2>/dev/null |
-            command sed -e "s/$_scp_path_esc/\\\\&/g" -e '/[^\/]$/d' -e "s/^/${1-}/"))
+            command gsed -e "s/$_scp_path_esc/\\\\&/g" -e '/[^\/]$/d' -e "s/^/${1-}/"))
     else
         COMPREPLY+=($(command ls -aF1dL $cur* 2>/dev/null |
-            command sed -e "s/$_scp_path_esc/\\\\&/g" -e 's/[*@|=]$//g' \
+            command gsed -e "s/$_scp_path_esc/\\\\&/g" -e 's/[*@|=]$//g' \
                 -e 's/[^\/]$/& /g' -e "s/^/${1-}/"))
     fi
 }
diff -pruN bash-completion-2.11.old/completions/strings bash-completion-2.11/completions/strings
--- bash-completion-2.11.old/completions/strings	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/strings	2021-03-31 13:40:32.873848608 +0000
@@ -15,7 +15,7 @@ _strings()
             ;;
         --target | -!(-*)T)
             COMPREPLY=($(compgen -W '$(LC_ALL=C "$1" --help 2>/dev/null | \
-                command sed -ne "s/: supported targets: \(.*\)/\1/p")' \
+                command gsed -ne "s/: supported targets: \(.*\)/\1/p")' \
                 -- "$cur"))
             return
             ;;
diff -pruN bash-completion-2.11.old/completions/svcadm bash-completion-2.11/completions/svcadm
--- bash-completion-2.11.old/completions/svcadm	2020-01-11 19:58:32.000000000 +0000
+++ bash-completion-2.11/completions/svcadm	2021-03-31 13:40:32.500987087 +0000
@@ -25,7 +25,7 @@
 _gen_zoneadm_list()
 {
     if [[ ${prev} =~ "-z" ]]; then
-        local zones="$(zoneadm list -c | grep -v '^global$')"
+        local zones="$(zoneadm list -c | ggrep -v '^global$')"
         COMPREPLY=( $(compgen -W "${zones}" -- ${cur}) )
     fi
 }
@@ -133,7 +133,7 @@ _svcadm()
         else
             # svcadm [-v] milestone [-d] milestone_FMRI
             if [[ ${line} =~ -S ]]; then
-                command_list="$(echo ${command_list} | sed -e 's/milestone//')"
+                command_list="$(echo ${command_list} | gsed -e 's/milestone//')"
             fi
             COMPREPLY=( $(compgen -W "${command_list}" -- ${cur}) )
         fi
diff -pruN bash-completion-2.11.old/completions/svk bash-completion-2.11/completions/svk
--- bash-completion-2.11.old/completions/svk	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/svk	2021-03-31 13:40:32.674098480 +0000
@@ -191,7 +191,7 @@ _svk()
                     ;;
                 sync)
                     COMPREPLY=($(compgen -W "$($1 mirror --list \
-                        2>/dev/null | awk '/^\//{print $1}')" -- "$cur"))
+                        2>/dev/null | nawk '/^\//{print $1}')" -- "$cur"))
                     ;;
                 co | checkout | push | pull)
                     if [[ $cur == //*/* ]]; then
@@ -200,7 +200,7 @@ _svk()
                         path=//
                     fi
                     COMPREPLY=($(compgen -W "$($1 list $path 2>/dev/null |
-                        command sed -e 's|\(.*\)|'$path'\1|')" -- "$cur"))
+                        command gsed -e 's|\(.*\)|'$path'\1|')" -- "$cur"))
                     ;;
                 *)
                     _filedir
diff -pruN bash-completion-2.11.old/completions/synclient bash-completion-2.11/completions/synclient
--- bash-completion-2.11.old/completions/synclient	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/synclient	2021-03-31 13:40:32.392749752 +0000
@@ -15,7 +15,7 @@ _synclient()
         COMPREPLY=($(compgen -W '$(_parse_usage "$1")' -- "$cur"))
     elif [[ $cur != *=?* ]]; then
         COMPREPLY=($(compgen -S = -W '$($1 -l 2>/dev/null | \
-            awk "/^[ \t]/ { print \$1 }")' -- "$cur"))
+            nawk "/^[ \t]/ { print \$1 }")' -- "$cur"))
         compopt -o nospace
     fi
 } &&
diff -pruN bash-completion-2.11.old/completions/sysbench bash-completion-2.11/completions/sysbench
--- bash-completion-2.11.old/completions/sysbench	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/sysbench	2021-03-31 13:40:32.304117174 +0000
@@ -78,7 +78,7 @@ _sysbench()
             ;;
         --db-driver)
             COMPREPLY=($(compgen -W "$($1 --test=oltp help 2>/dev/null |
-                command sed -e '/^.*database drivers:/,/^$/!d' \
+                command gsed -e '/^.*database drivers:/,/^$/!d' \
                     -ne 's/^  *\([^ ]*\) .*/\1/p')" -- "$cur"))
             return
             ;;
diff -pruN bash-completion-2.11.old/completions/tipc bash-completion-2.11/completions/tipc
--- bash-completion-2.11.old/completions/tipc	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/tipc	2021-03-31 13:40:32.344238291 +0000
@@ -42,7 +42,7 @@ _tipc_bearer()
     elif ((cword == optind + 3)); then
         case "$media" in
             "udp")
-                local names=$(tipc bearer list 2>/dev/null | awk -F: '/^udp:/ {print $2}')
+                local names=$(tipc bearer list 2>/dev/null | nawk -F: '/^udp:/ {print $2}')
                 COMPREPLY=($(compgen -W '$names' -- $cur))
                 ;;
             "eth")
@@ -66,9 +66,9 @@ _tipc_link()
     if ((cword == optind)); then
         COMPREPLY=($(compgen -W 'link' -- $cur))
     elif ((cword == optind + 1)); then
-        # awk drops link state and last trailing :
+        # nawk drops link state and last trailing :
         local links=$(tipc link list 2>/dev/null |
-            awk '{print substr($1, 0, length($1))}')
+            nawk '{print substr($1, 0, length($1))}')
         local -a exclude
         [[ $filter == peers ]] && exclude=(-X broadcast-link)
         COMPREPLY=($(compgen "${exclude[@]}" -W '$links' -- $cur))
diff -pruN bash-completion-2.11.old/completions/tox bash-completion-2.11/completions/tox
--- bash-completion-2.11.old/completions/tox	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/tox	2021-03-31 13:40:32.797583322 +0000
@@ -30,7 +30,7 @@ _tox()
                 } 2>/dev/null
             )
             [[ $envs ]] || envs=$(
-                command sed -e 's/,/ /g' -ne 's/^envlist[[:space:]]*=//p' \
+                command gsed -e 's/,/ /g' -ne 's/^envlist[[:space:]]*=//p' \
                     tox.ini 2>/dev/null
             )
             local prefix=""
diff -pruN bash-completion-2.11.old/completions/tshark bash-completion-2.11/completions/tshark
--- bash-completion-2.11.old/completions/tshark	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/tshark	2021-03-31 13:40:32.490099052 +0000
@@ -24,7 +24,7 @@ _tshark()
                 _filedir
             else
                 [[ -v _tshark_prefs ]] ||
-                    _tshark_prefs="$("$1" -G defaultprefs 2>/dev/null | command sed -ne 's/^#\{0,1\}\([a-z0-9_.-]\{1,\}:\).*/\1/p' |
+                    _tshark_prefs="$("$1" -G defaultprefs 2>/dev/null | command gsed -ne 's/^#\{0,1\}\([a-z0-9_.-]\{1,\}:\).*/\1/p' |
                         tr '\n' ' ')"
                 : ${prefix:=}
                 COMPREPLY=($(compgen -P "$prefix" -W "$_tshark_prefs" \
@@ -38,7 +38,7 @@ _tshark()
             ;;
         -*i)
             COMPREPLY=($(compgen -W \
-                "$("$1" -D 2>/dev/null | awk '{print $2}')" -- "$cur"))
+                "$("$1" -D 2>/dev/null | nawk '{print $2}')" -- "$cur"))
             return
             ;;
         -*y)
@@ -50,7 +50,7 @@ _tshark()
                 fi
             done
             COMPREPLY=($(compgen -W "$("$1" $opts -L 2>/dev/null |
-                awk '/^  / { print $1 }')" -- "$cur"))
+                nawk '/^  / { print $1 }')" -- "$cur"))
             return
             ;;
         -*[ab])
@@ -70,7 +70,7 @@ _tshark()
             ;;
         -*F)
             COMPREPLY=($(compgen -W "$("$1" -F 2>&1 |
-                awk '/^  / { print $1 }')" -- "$cur"))
+                nawk '/^  / { print $1 }')" -- "$cur"))
             return
             ;;
         -*O)
@@ -84,13 +84,13 @@ _tshark()
             return
             ;;
         -*T)
-            # Parse from: tshark -T . 2>&1 | awk -F \" '/^\t*"/ { print $2 }'
+            # Parse from: tshark -T . 2>&1 | nawk -F \" '/^\t*"/ { print $2 }'
             COMPREPLY=($(compgen -W \
                 'pdml ps psml json jsonraw ek tabs text fields' -- "$cur"))
             return
             ;;
         -*t)
-            # Parse from: tshark -t . 2>&1 | awk -F \" '/^\t*"/ { print $2 }'
+            # Parse from: tshark -t . 2>&1 | nawk -F \" '/^\t*"/ { print $2 }'
             COMPREPLY=($(compgen -W \
                 'a ad adoy d dd e r u ud udoy' -- "$cur"))
             return
@@ -117,7 +117,7 @@ _tshark()
             ;;
         -*G)
             COMPREPLY=($(compgen -W "$("$1" -G \? 2>/dev/null |
-                awk '/^[ \t]*-G / \
+                nawk '/^[ \t]*-G / \
                     { sub("^[[]","",$2); sub("[]]$","",$2); print $2 }')" \
                 -- "$cur"))
             return
diff -pruN bash-completion-2.11.old/completions/valgrind bash-completion-2.11/completions/valgrind
--- bash-completion-2.11.old/completions/valgrind	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/valgrind	2021-03-31 13:40:32.573444546 +0000
@@ -71,7 +71,7 @@ _valgrind()
             # generic cases parsed from --help output
         --+([-A-Za-z0-9_]))
             local value=$($1 --help-debug ${tool-} 2>/dev/null |
-                command sed -ne "s|^[[:blank:]]*$prev=\([^[:blank:]]\{1,\}\).*|\1|p")
+                command gsed -ne "s|^[[:blank:]]*$prev=\([^[:blank:]]\{1,\}\).*|\1|p")
             case $value in
                 \<file*\>)
                     _filedir
diff -pruN bash-completion-2.11.old/completions/wget bash-completion-2.11/completions/wget
--- bash-completion-2.11.old/completions/wget	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/wget	2021-03-31 13:40:32.407692103 +0000
@@ -118,7 +118,7 @@ _wget()
             return
             ;;
         --user | --http-user | --proxy-user | --ftp-user)
-            COMPREPLY=($(compgen -W "$(command sed -n \
+            COMPREPLY=($(compgen -W "$(command gsed -n \
                 '/^login/s/^[[:blank:]]*login[[:blank:]]//p' ~/.netrc \
                 2>/dev/null)" -- "$cur"))
             return
diff -pruN bash-completion-2.11.old/completions/wodim bash-completion-2.11/completions/wodim
--- bash-completion-2.11.old/completions/wodim	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/wodim	2021-03-31 13:40:32.532976857 +0000
@@ -45,7 +45,7 @@ _cdrecord()
                 ;;
             driver)
                 COMPREPLY=($(compgen -W "$($1 driver=help 2>&1 |
-                    awk 'NR > 1 { print $1 }') help" -- "$cur"))
+                    nawk 'NR > 1 { print $1 }') help" -- "$cur"))
                 ;;
             minbuf)
                 COMPREPLY=($(compgen -W '{25..95}' -- "$cur"))
diff -pruN bash-completion-2.11.old/completions/wol bash-completion-2.11/completions/wol
--- bash-completion-2.11.old/completions/wol	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/wol	2021-03-31 13:40:33.065359675 +0000
@@ -15,7 +15,7 @@ _wol()
             COMPREPLY=($({
                 ip addr show || ifconfig -a
             } 2>/dev/null |
-                command sed -ne 's/.*[[:space:]]Bcast:\([^[:space:]]*\).*/\1/p' -ne \
+                command gsed -ne 's/.*[[:space:]]Bcast:\([^[:space:]]*\).*/\1/p' -ne \
                     's/.*inet.*[[:space:]]brd[[:space:]]\([^[:space:]]*\).*/\1/p' -ne \
                     's/.*[[:space:]]broadcast[[:space:]]\{1,\}\([^[:space:]]*\).*/\1/p'))
             _known_hosts_real -- "$cur"
diff -pruN bash-completion-2.11.old/completions/wvdial bash-completion-2.11/completions/wvdial
--- bash-completion-2.11.old/completions/wvdial	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/wvdial	2021-03-31 13:40:32.536556778 +0000
@@ -33,8 +33,8 @@ _wvdial()
             done
             # parse config files for sections and
             # remove default section
-            COMPREPLY=($(command sed -ne "s|^\[Dialer \($cur.*\)\]$|\1|p" $config \
-                2>/dev/null | command grep -v '^Defaults$'))
+            COMPREPLY=($(command gsed -ne "s|^\[Dialer \($cur.*\)\]$|\1|p" $config \
+                2>/dev/null | command ggrep -v '^Defaults$'))
             # escape spaces
             COMPREPLY=(${COMPREPLY[@]// /\\ })
             ;;
diff -pruN bash-completion-2.11.old/completions/xdg-settings bash-completion-2.11/completions/xdg-settings
--- bash-completion-2.11.old/completions/xdg-settings	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/xdg-settings	2021-03-31 13:40:32.980108481 +0000
@@ -23,7 +23,7 @@ _xdg_settings()
         COMPREPLY=($(compgen -W "get check set" -- "$cur"))
     elif ((args == 2)); then
         COMPREPLY=($(compgen -W \
-            '$("$1" --list | awk "!/^Known/ { print \$1 }")' -- "$cur"))
+            '$("$1" --list | nawk "!/^Known/ { print \$1 }")' -- "$cur"))
     fi
 } &&
     complete -F _xdg_settings xdg-settings
diff -pruN bash-completion-2.11.old/completions/xfreerdp bash-completion-2.11/completions/xfreerdp
--- bash-completion-2.11.old/completions/xfreerdp	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/xfreerdp	2021-03-31 13:40:32.297114195 +0000
@@ -8,7 +8,7 @@ _xfreerdp()
     case $prev in # old/dash syntax
         -k)
             COMPREPLY=($(compgen -W '$("$1" --kbd-list |
-                awk "/^0x/ { print \$1 }")' -- "$cur"))
+                nawk "/^0x/ { print \$1 }")' -- "$cur"))
             return
             ;;
         -a)
@@ -28,7 +28,7 @@ _xfreerdp()
     case $cur in # new/slash syntax
         /kbd:*)
             COMPREPLY=($(compgen -W '$("$1" /kbd-list |
-                awk "/^0x/ { print \$1 }")' -- "${cur#/kbd:}"))
+                nawk "/^0x/ { print \$1 }")' -- "${cur#/kbd:}"))
             return
             ;;
         /bpp:*)
@@ -42,14 +42,14 @@ _xfreerdp()
 
     if [[ $cur == /* ]]; then
         COMPREPLY=($(compgen -W '$("$1" --help |
-            awk "\$1 ~ /^\\// && \$1 !~ /^.(flag\$|option:)/ { sub(\":.*\",\":\",\$1); print \$1 }")' \
+            nawk "\$1 ~ /^\\// && \$1 !~ /^.(flag\$|option:)/ { sub(\":.*\",\":\",\$1); print \$1 }")' \
             -- "$cur"))
         [[ ${COMPREPLY-} == *: ]] && compopt -o nospace
     elif [[ $cur == [+-]* ]]; then
         local char=${cur:0:1}
         local help="$($1 --help)"
         if [[ $help == */help* ]]; then # new/slash syntax
-            COMPREPLY=($(compgen -W '$(awk "
+            COMPREPLY=($(compgen -W '$(nawk "
                 \$1 ~ /^[+-]/ && \$1 !~ /^.toggle\$/ { sub(\"^.\",\"$char\",\$1); print \$1 }
             " <<<"$help")' -- "$cur"))
         else # old/dash syntax
@@ -57,7 +57,7 @@ _xfreerdp()
             COMPREPLY=($(compgen -W '${COMPREPLY[@]%:}' -- "$cur"))
         fi
     else
-        COMPREPLY=($(compgen -W "$(awk '{print $1}' ~/.freerdp/known_hosts \
+        COMPREPLY=($(compgen -W "$(nawk '{print $1}' ~/.freerdp/known_hosts \
             2>/dev/null)" -- "$cur"))
     fi
 
diff -pruN bash-completion-2.11.old/completions/xgamma bash-completion-2.11/completions/xgamma
--- bash-completion-2.11.old/completions/xgamma	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/xgamma	2021-03-31 13:40:32.599407914 +0000
@@ -7,7 +7,7 @@ _xgamma()
 
     case "$prev" in
         -screen)
-            local screens=$(xrandr --query 2>/dev/null | command sed -n \
+            local screens=$(xrandr --query 2>/dev/null | command gsed -n \
                 '/^Screen /s|^Screen \{1,\}\(.*\):.*$|\1|p' 2>/dev/null)
             COMPREPLY=($(compgen -W "$screens" -- "$cur"))
             return
@@ -30,7 +30,7 @@ _xgamma()
                 compopt -o nospace
             elif [[ $cur == :*.* ]]; then
                 # local screen numbers
-                local t screens=$(xrandr --query 2>/dev/null | command sed -ne \
+                local t screens=$(xrandr --query 2>/dev/null | command gsed -ne \
                     '/^Screen /s|^Screen \{1,\}\(.*\):.*$|\1|p' 2>/dev/null)
                 t="${cur#:}"
                 COMPREPLY=($(compgen -P "${t%.*}." -W "$screens" -- \
diff -pruN bash-completion-2.11.old/completions/xrandr bash-completion-2.11/completions/xrandr
--- bash-completion-2.11.old/completions/xrandr	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/xrandr	2021-03-31 13:40:32.731621379 +0000
@@ -12,7 +12,7 @@ _xrandr()
             return
             ;;
         --output | --left-of | --right-of | --above | --below | --same-as)
-            local outputs=$("$1" | awk '/connected/ {print $1}')
+            local outputs=$("$1" | nawk '/connected/ {print $1}')
             COMPREPLY=($(compgen -W "$outputs" -- "$cur"))
             return
             ;;
@@ -25,7 +25,7 @@ _xrandr()
                 fi
             done
             if [[ -v output ]]; then
-                local modes=$("$1" | command sed -e "1,/^$output / d" \
+                local modes=$("$1" | command gsed -e "1,/^$output / d" \
                     -e "/connected/,$ d" \
                     -e "s/\([^[:space:]]\)[[:space:]].*/\1/")
                 COMPREPLY=($(compgen -W "$modes" -- "$cur"))
@@ -47,7 +47,7 @@ _xrandr()
             ;;
         --setprovideroutputsource | --setprovideroffloadsink)
             local providers=$("$1" --listproviders 2>/dev/null |
-                command sed -ne 's/.* name:\([^ ]*\).*/\1/p')
+                command gsed -ne 's/.* name:\([^ ]*\).*/\1/p')
             COMPREPLY=($(compgen -W "$providers" -- "$cur"))
             # TODO 2nd arg needed, is that a provider as well?
             return
@@ -55,7 +55,7 @@ _xrandr()
     esac
 
     COMPREPLY=($(compgen -W '$("$1" -help 2>&1 |
-        command sed -e "s/ or / /g" -e "s/<[^>]*>]//g" | _parse_help -)' \
+        command gsed -e "s/ or / /g" -e "s/<[^>]*>]//g" | _parse_help -)' \
         -- "$cur"))
 } &&
     complete -F _xrandr xrandr
diff -pruN bash-completion-2.11.old/completions/xsltproc bash-completion-2.11/completions/xsltproc
--- bash-completion-2.11.old/completions/xsltproc	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/xsltproc	2021-03-31 13:40:32.882065034 +0000
@@ -17,7 +17,7 @@ _xsltproc()
         --encoding)
             # some aliases removed
             COMPREPLY=($(compgen -X '@(UTF[1378]|8859|ISO[0-9_])*' \
-                -W "$(iconv -l | command sed -e 's/\/.*//')" -- "$cur"))
+                -W "$(iconv -l | command gsed -e 's/\/.*//')" -- "$cur"))
             return
             ;;
         --param | --stringparam)
diff -pruN bash-completion-2.11.old/completions/zfs bash-completion-2.11/completions/zfs
--- bash-completion-2.11.old/completions/zfs	2020-01-11 19:58:32.000000000 +0000
+++ bash-completion-2.11/completions/zfs	2021-03-31 13:40:32.854269741 +0000
@@ -37,22 +37,22 @@ shopt -u hostcomplete
 
 __zfs_get_commands()
 {
-    $__ZFS_CMD 2>&1 | awk '/^\t[a-z]/ {print $1}' | cut -f1 -d '|' | uniq
+    $__ZFS_CMD 2>&1 | nawk '/^\t[a-z]/ {print $1}' | cut -f1 -d '|' | uniq
 }
 
 __zfs_get_properties()
 {
-    $__ZFS_CMD get 2>&1 | awk '$2 == "YES" || $2 == "NO" {print $1}'; echo all name space
+    $__ZFS_CMD get 2>&1 | nawk '$2 == "YES" || $2 == "NO" {print $1}'; echo all name space
 }
 
 __zfs_get_editable_properties()
 {
-    $__ZFS_CMD get 2>&1 | awk '$2 == "YES" {print $1"="}'
+    $__ZFS_CMD get 2>&1 | nawk '$2 == "YES" {print $1"="}'
 }
 
 __zfs_get_inheritable_properties()
 {
-    $__ZFS_CMD get 2>&1 | awk '$3 == "YES" {print $1}'
+    $__ZFS_CMD get 2>&1 | nawk '$3 == "YES" {print $1}'
 }
 
 __zfs_list_datasets()
@@ -100,7 +100,7 @@ __zfs_match_snapshot()
                     return 0
                 fi
             fi
-            echo "$datasets" | awk '{print $1"@"}'
+            echo "$datasets" | nawk '{print $1"@"}'
         fi
     fi
 }
@@ -141,7 +141,7 @@ __zfs_match_multiple_snapshots()
                 return 1
             fi
             local range_start=$(expr "$cur" : '\(.*%\)')
-            $__ZFS_CMD list -H -o name -s name -t snapshot -d 1 $base_dataset | sed 's$.*@$'$range_start'$g'
+            $__ZFS_CMD list -H -o name -s name -t snapshot -d 1 $base_dataset | gsed 's$.*@$'$range_start'$g'
         fi
     else
         __zfs_match_snapshot_or_bookmark
diff -pruN bash-completion-2.11.old/completions/zones bash-completion-2.11/completions/zones
--- bash-completion-2.11.old/completions/zones	2020-01-11 19:58:32.000000000 +0000
+++ bash-completion-2.11/completions/zones	2021-03-31 13:40:32.642746302 +0000
@@ -31,7 +31,7 @@ _zlogin()
         esac
     else
       # Provide running zone names
-      local zones=$(zoneadm list | grep -v '^global$')
+      local zones=$(zoneadm list | ggrep -v '^global$')
       COMPREPLY=( $(compgen -W "${zones}" -- ${cur}) )
     fi
 }
@@ -45,7 +45,7 @@ _dash_z_zone()
 
     if [[ ${prev} =~ "-z" ]]; then
       # Provide running zone names
-      local zones="$(zoneadm list -c | grep -v '^global$')"
+      local zones="$(zoneadm list -c | ggrep -v '^global$')"
       COMPREPLY=( $(compgen -W "${zones}" -- ${cur}) )
     fi
 }
diff -pruN bash-completion-2.11.old/completions/zopfli bash-completion-2.11/completions/zopfli
--- bash-completion-2.11.old/completions/zopfli	2020-07-25 13:49:49.000000000 +0000
+++ bash-completion-2.11/completions/zopfli	2021-03-31 13:40:32.355514166 +0000
@@ -13,7 +13,7 @@ _zopfli()
 
     if [[ $cur == -* ]]; then
         COMPREPLY=($(compgen -W \
-            '$(_parse_help "$1" -h | command sed -e "s/#$//")' -- "$cur"))
+            '$(_parse_help "$1" -h | command gsed -e "s/#$//")' -- "$cur"))
         [[ ${COMPREPLY-} == --i ]] && compopt -o nospace
         return
     fi
