https://github.com/nagios-plugins/nagios-plugins/pull/700

--- nagios-plugins-2.4.4/configure.orig
+++ nagios-plugins-2.4.4/configure
@@ -14323,7 +14323,7 @@
 
 # Also read in the version from it
 if test -f $srcdir/release; then
-	NP_RELEASE="$(<release)"
+	NP_RELEASE="$(<$srcdir/release)"
 else
 	NP_RELEASE="$PACKAGE_VERSION"
 fi
