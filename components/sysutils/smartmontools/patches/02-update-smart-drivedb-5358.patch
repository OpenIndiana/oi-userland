https://www.smartmontools.org/changeset/5358/

--- smartmontools/ChangeLog	(revision 5357)
+++ smartmontools/ChangeLog	(revision 5358)
@@ -2,4 +2,6 @@
 
 2022-04-24  Christian Franke  <franke@computer.org>
+
+	update-smart-drivedb.in: Don't use 'let' which is bash specific.
 
 	update-smart-drivedb.in: Don't use semicolon in sed scripts.
--- smartmontools/update-smart-drivedb.in	(revision 5357)
+++ smartmontools/update-smart-drivedb.in	(revision 5358)
@@ -528,5 +528,5 @@
   i=0
   while ! out=`rm -f -r "$gnupgtmp" 2>&1`; do
-    let ++i
+    i=$((i+1))
     if [ $i -ge 10 ]; then
       echo "$out" >&2; break
