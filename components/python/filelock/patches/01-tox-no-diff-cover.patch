We cannot compare against origin/main branch because we build from sdist, not git.

--- filelock-3.29.4/tox.toml.orig
+++ filelock-3.29.4/tox.toml
@@ -35,14 +35,6 @@
     ], extend = true },
     "tests",
   ],
-  { replace = "posargs", default = [
-    [
-      "diff-cover",
-      "--compare-branch",
-      { replace = "env", name = "DIFF_AGAINST", default = "origin/main" },
-      "{env_tmp_dir}{/}coverage.xml",
-    ],
-  ], extend = true },
 ]
 
 [env.coverage]
--- filelock-3.29.4/pyproject.toml.orig
+++ filelock-3.29.4/pyproject.toml
@@ -57,7 +57,6 @@
 ]
 test = [
   "covdefaults>=2.3",
-  "diff-cover>=10.2",
   "pytest>=9.0.3",
   "pytest-asyncio>=1.3",
   "pytest-cov>=7.1",
