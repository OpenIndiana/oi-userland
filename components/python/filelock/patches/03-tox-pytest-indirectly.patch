--- filelock-3.29.3/tox.toml.orig
+++ filelock-3.29.3/tox.toml
@@ -11,6 +11,8 @@
 set_env.COVERAGE_FILE = { replace = "env", name = "COVERAGE_FILE", default = "{work_dir}{/}.coverage.{env_name}" }
 commands = [
   [
+    "python",
+    "-m",
     "pytest",
     "--durations",
     "10",
