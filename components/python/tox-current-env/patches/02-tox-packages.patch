https://github.com/fedora-python/tox-current-env/issues/52

--- tox-current-env-0.0.10/tests/test_integration_tox3.py.orig
+++ tox-current-env-0.0.10/tests/test_integration_tox3.py
@@ -523,7 +523,7 @@
 
     # without a flag, the output must match tox defaults
     if not flag:
-        assert len(packages) == 3
+        assert len(packages) == 4
         assert packages[0].startswith("py==")
         assert packages[1].startswith("six==")
         assert packages[2].startswith(("test==", "test @ "))  # old and new pip
