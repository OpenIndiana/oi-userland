It is hard to normalize pytest-xdist test results

--- tox-current-env-0.0.10/tox.ini.orig
+++ tox-current-env-0.0.10/tox.ini
@@ -13,6 +13,3 @@
     tox4: tox >=4,< 5
 commands =
     pytest -v {posargs} tests
-
-[pytest]
-addopts = -nauto
