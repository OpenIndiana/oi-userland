--- pyproject_api-1.10.1/tox.toml.orig
+++ pyproject_api-1.10.1/tox.toml
@@ -12,6 +12,7 @@
 set_env.COVERAGE_PROCESS_START = "{tox_root}/pyproject.toml"
 commands = [
   [
+    "python", "-m",
     "pytest",
     { replace = "posargs", default = [
       "--junitxml",
