We do not need to check coverage for this project.

--- build-1.6.1/pyproject.toml.orig
+++ build-1.6.1/pyproject.toml
@@ -89,14 +89,12 @@
 ]
 test-core = [
   "pytest >= 6.2.5, != 9.1.0",
-  "pytest-cov >= 3.0.0",
   "pytest-mock >= 3.7.0",
   "pytest-rerunfailures >= 10.2",
   "pytest-xdist >= 2.4.0",
 ]
 test = [
   "filelock >= 3.20.1",
-  "covdefaults >= 2.3",
   "wheel >= 0.38.1",
   'setuptools >= 56.0.0; python_version == "3.10"',
   'setuptools >= 56.0.0; python_version == "3.11"',
--- build-1.6.1/tox.toml.orig
+++ build-1.6.1/tox.toml
@@ -16,10 +16,7 @@
 pass_env = ["LC_ALL", "PIP_*", "PYTEST_*", "TERM"]
 commands = [
     [
-        "pytest", "-ra", "--cov", "--cov-config", "pyproject.toml",
-        "--cov-report=html:{envdir}/htmlcov", "--cov-context=test",
-        "--cov-report=xml:{toxworkdir}/coverage.{envname}.xml",
-        "--cov-report", "term-missing",
+        "pytest", "-ra",
         { replace = "posargs", default = ["-n", "auto"], extend = true },
     ],
 ]
