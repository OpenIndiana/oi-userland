We are building from sdist and so check-manifest always fails.

--- pytest_run_parallel-0.6.0/pyproject.toml.orig
+++ pytest_run_parallel-0.6.0/pyproject.toml
@@ -72,7 +72,6 @@
     "pytest>=6.2.0",
     "pytest-cov",
     "pytest-order",
-    "check-manifest",
     "hypothesis>=6.135.33",
 ]
 commands = [
@@ -84,11 +83,6 @@
     "--cov", "tests",
     "{posargs:tests}"
     ],
-    [
-    "check-manifest",
-    "-u",
-    "-v",
-    ]
 ]
 
 [tool.tox.env.ruff]
