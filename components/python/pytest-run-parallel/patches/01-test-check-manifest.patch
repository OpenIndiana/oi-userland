We are building from sdist and so check-manifest always fails.

--- pytest_run_parallel-0.4.4/pyproject.toml.orig
+++ pytest_run_parallel-0.4.4/pyproject.toml
@@ -73,7 +73,6 @@
     "pytest>=6.2.0",
     "pytest-cov",
     "pytest-order",
-    "check-manifest",
     "hypothesis",
 ]
 commands = [
@@ -85,11 +84,6 @@
     "--cov", "tests",
     "{posargs:tests}"
     ],
-    [
-    "check-manifest",
-    "-u",
-    "-v",
-    ]
 ]
 
 [tool.tox.env.ruff]
