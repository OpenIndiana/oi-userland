#
# This file and its contents are supplied under the terms of the
# Common Development and Distribution License ("CDDL"), version 1.0.
# You may only use this file in accordance with the terms of version
# 1.0 of the CDDL.
#
# A full copy of the text of the CDDL should have accompanied this
# source.  A copy of the CDDL is also available via the Internet at
# http://www.illumos.org/license/CDDL.
#

#
# Copyright 2024 Marcel Telka
#

%patch% 01-no-shebang.patch
%patch% 02-setuptools.patch
%patch% 03-tox-requires.patch
%patch% 04-no-coverage_pyver_pragma.patch
%patch% 05-tox-no-pip-install.patch
%patch% 06-coverage.patch
%patch% 07-no-pandas.patch

%hook-begin%
# https://github.com/domdfcoding/domdf_python_tools/issues/114
DOWNLOAD_URL='https://github.com/domdfcoding/domdf_python_tools/archive/refs/tags/v$(HUMAN_VERSION).tar.gz'

# The project summary at PyPI contains strange characters that could cause
# issues so override it.
SUMMARY='Helpful functions for Python'

%include-2%
#
# Following dependency loops were detected (where 'A --> B' means 'A needs B to
# run tests', and 'A ==> B' means 'A needs B for build and/or run'):
#
# domdf_python_tools --> coincidence ==> whey ==> domdf_python_tools
#
# To break loops we need to allow this project to integrate without testing in
# a case the required projects are not available yet (bootstrap).
#
PYTHON_TEST_BOOTSTRAP = yes
%include-3%
# https://github.com/domdfcoding/domdf_python_tools/issues/82
PYTEST_ADDOPTS += --deselect tests/test_import_tools.py::test_discover_entry_points_by_name_object_match_func
PYTEST_ADDOPTS += --deselect tests/test_import_tools.py::test_discover_entry_points_by_name_name_match_func
PYTEST_ADDOPTS += --deselect tests/test_import_tools.py::test_discover_entry_points

# Normalize pointers
COMPONENT_TEST_TRANSFORMS += "-e 's/0x0\*[0-9a-f]\{12\}/\$$(POINTER)/g'"
