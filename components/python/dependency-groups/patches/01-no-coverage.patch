We do not need to check coverage for this project.

--- dependency_groups-1.3.1/tox.ini.orig
+++ dependency_groups-1.3.1/tox.ini
@@ -16,7 +16,7 @@
 package = wheel
 wheel_build_env = build_wheel
 dependency_groups = test
-commands = coverage run -m pytest -v {posargs}
+commands = pytest -v {posargs}
 
 depends =
     py{38,39,310,311,312},py: clean
--- dependency_groups-1.3.1/pyproject.toml.orig
+++ dependency_groups-1.3.1/pyproject.toml
@@ -4,7 +4,7 @@
 
 [dependency-groups]
 coverage = ["coverage[toml]"]
-test = ["pytest", {include-group = "coverage"}]
+test = ["pytest"]
 docs = ["sphinx>=8.1", "sphinx-issues>=5", "furo"]
 lint = ["pre-commit"]
 typing = ["mypy", "packaging"]
