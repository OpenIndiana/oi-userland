We do not have enrich and molecule packaged yet.

--- subprocess_tee-0.4.2/pyproject.toml.orig
+++ subprocess_tee-0.4.2/pyproject.toml
@@ -66,8 +66,6 @@
   "pymdown-extensions",
 ]
 test =[
-    "enrich>=1.2.6",
-    "molecule>=3.4.0",  # ansible is needed but no direct imports are made
     "pytest-cov>=2.12.1",
     "pytest-plus>=0.2",
     "pytest-xdist>=2.3.0",
