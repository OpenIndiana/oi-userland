We do not need to check coverage for this project.

--- zope_interface-8.6/pyproject.toml.orig
+++ zope_interface-8.6/pyproject.toml
@@ -41,7 +41,7 @@
 
 [project.optional-dependencies]
 docs = ["Sphinx", "repoze.sphinx.autointerface", "furo"]
-test = ["coverage[toml]", "zope.event", "zope.testing"]
+test = ["zope.event", "zope.testing"]
 testing = ["coverage[toml]", "zope.event", "zope.testing"]
 
 [project.urls]
--- zope_interface-8.6/tox.ini.orig
+++ zope_interface-8.6/tox.ini
@@ -28,7 +28,7 @@
     !pure-!pypy3: PURE_PYTHON=0
     ZOPE_INTERFACE_STRICT_IRO=1
 commands =
-    coverage run -p -m unittest discover -s src/zope {posargs}
+    python -m unittest discover -s src/zope {posargs}
 extras =
     test
 
