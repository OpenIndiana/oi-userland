We do not need to check sources, coverage, typing for this project.

--- keyrings_alt-5.0.2/pyproject.toml.orig
+++ keyrings_alt-5.0.2/pyproject.toml
@@ -30,11 +30,7 @@
 test = [
 	# upstream
 	"pytest >= 6, != 8.1.*",
-	"pytest-checkdocs >= 2.4",
-	"pytest-cov",
-	"pytest-mypy",
 	"pytest-enabler >= 2.2",
-	"pytest-ruff >= 0.2.1; sys_platform != 'cygwin'",
 
 	# local
 	"keyring >= 20",
