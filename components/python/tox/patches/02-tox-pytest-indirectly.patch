--- tox-4.35.0/tox.toml.orig
+++ tox-4.35.0/tox.toml
@@ -12,6 +12,8 @@
 set_env.COVERAGE_FILECOVERAGE_PROCESS_START = "{tox_root}{/}pyproject.toml"
 commands = [
   [
+    "python",
+    "-m",
     "pytest",
     { replace = "posargs", extend = true, default = [
       "--durations",
