We cannot compare against origin/main branch because we build from sdist, not git.

--- tox-4.50.2/tox.toml.orig
+++ tox-4.50.2/tox.toml
@@ -45,12 +45,6 @@
       "--run-integration",
     ] },
   ],
-  [
-    "diff-cover",
-    "--compare-branch",
-    { replace = "env", name = "DIFF_AGAINST", default = "origin/main" },
-    "{work_dir}{/}coverage.{env_name}.xml",
-  ],
 ]
 
 [env.fix]
--- tox-4.50.2/pyproject.toml.orig
+++ tox-4.50.2/pyproject.toml
@@ -82,7 +82,6 @@
   "covdefaults>=2.3",
   "coverage>=7.13.4",
   "detect-test-pollution>=1.2",
-  "diff-cover>=10.2",
   "distlib>=0.4",
   "docutils>=0.21",
   "flaky>=3.8.1",
