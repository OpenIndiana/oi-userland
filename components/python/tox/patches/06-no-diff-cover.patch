We cannot compare against origin/main branch because we build from sdist, not git.

--- tox-4.49.1/tox.toml.orig
+++ tox-4.49.1/tox.toml
@@ -45,12 +45,6 @@
       "--run-integration",
     ] },
   ],
-  [
-    "diff-cover",
-    "--compare-branch",
-    { replace = "env", name = "DIFF_AGAINST", default = "origin/main" },
-    "{work_dir}{/}coverage.{env_name}.xml",
-  ],
 ]
 
 [env.fix]
--- tox-4.49.1/pyproject.toml.orig
+++ tox-4.49.1/pyproject.toml
@@ -81,7 +81,6 @@
   "covdefaults>=2.3",
   "coverage>=7.13.4",
   "detect-test-pollution>=1.2",
-  "diff-cover>=10.2",
   "distlib>=0.4",
   "flaky>=3.8.1",
   "hatch-vcs>=0.5",
