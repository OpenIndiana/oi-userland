https://github.com/tox-dev/tox/issues/3602

--- tox-4.32.0/src/tox/config/source/discover.py.orig
+++ tox-4.32.0/src/tox/config/source/discover.py
@@ -19,10 +19,10 @@
 
 SOURCE_TYPES: tuple[type[Source], ...] = (
     ToxIni,
-    SetupCfg,
     LegacyToml,
     TomlPyProject,
     TomlTox,
+    SetupCfg,
 )
 
 
--- tox-4.32.0/tests/config/source/test_discover.py.orig
+++ tox-4.32.0/tests/config/source/test_discover.py
@@ -10,7 +10,7 @@
 
 def out_no_src(path: Path) -> str:
     return (
-        f"ROOT: No loadable tox.ini or setup.cfg or pyproject.toml or tox.toml found, assuming empty tox.ini at {path}"
+        f"ROOT: No loadable tox.ini or pyproject.toml or tox.toml or setup.cfg found, assuming empty tox.ini at {path}"
         f"\ndefault environments:\npy -> [no description]\n"
     )
 
--- tox-4.32.0/tests/session/cmd/test_legacy.py.orig
+++ tox-4.32.0/tests/session/cmd/test_legacy.py
@@ -66,7 +66,7 @@
     outcome = project.run("le", "-l")
     outcome.assert_success()
     out = (
-        "ROOT: No loadable tox.ini or setup.cfg or pyproject.toml or tox.toml found, assuming empty tox.ini at "
+        "ROOT: No loadable tox.ini or pyproject.toml or tox.toml or setup.cfg found, assuming empty tox.ini at "
         f"{project.path}\n"
     )
     assert not outcome.err
