--- virtualenv-20.35.3/tox.ini.orig
+++ virtualenv-20.35.3/tox.ini
@@ -35,12 +35,12 @@
     PYTHONWARNDEFAULTENCODING = 1
     _COVERAGE_SRC = virtualenv
 commands =
-    !graalpy: coverage erase
-    !graalpy: coverage run -m pytest {posargs:--junitxml "{toxworkdir}/junit.{envname}.xml" tests --int}
-    !graalpy: coverage combine
-    !graalpy: coverage report --skip-covered --show-missing
-    !graalpy: coverage xml -o "{toxworkdir}/coverage.{envname}.xml"
-    !graalpy: coverage html -d {envtmpdir}/htmlcov --show-contexts  --title virtualenv-{envname}-coverage
+    !graalpy: python -m coverage erase
+    !graalpy: python -m coverage run -m pytest {posargs:--junitxml "{toxworkdir}/junit.{envname}.xml" tests --int}
+    !graalpy: python -m coverage combine
+    !graalpy: python -m coverage report --skip-covered --show-missing
+    !graalpy: python -m coverage xml -o "{toxworkdir}/coverage.{envname}.xml"
+    !graalpy: python -m coverage html -d {envtmpdir}/htmlcov --show-contexts  --title virtualenv-{envname}-coverage
     graalpy: pytest {posargs:--junitxml "{toxworkdir}/junit.{envname}.xml" tests --skip-slow}
 uv_seed = true
 
