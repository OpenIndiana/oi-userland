pycmd is not needed to run tests

--- pytest-forked-1.6.0/tox.ini.orig
+++ pytest-forked-1.6.0/tox.ini
@@ -10,7 +10,6 @@
 
 [testenv]
 deps =
-  pycmd
   # to avoid .eggs
   setuptools_scm
   pytest310: pytest~=3.10
