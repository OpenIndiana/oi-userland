We do not have these (mostly optional) pytest plugins packaged yet.

--- pydantic-2.9.2/pyproject.toml.orig
+++ pydantic-2.9.2/pyproject.toml
@@ -131,12 +131,8 @@
     "eval-type-backport",
     "pytest>=8.2.2",
     "pytest-mock",
-    "pytest-pretty",
-    "pytest-examples",
     "faker>=18.13.0",
     "pytest-benchmark>=4.0.0",
-    # TODO: remove bound once pytest-codspeed supports 3.13, blocked by `python-cffi`
-    "pytest-codspeed~=2.2.0; python_version < '3.13'",
     "packaging>=23.2",
     "jsonschema>=4.23.0",
 ]
