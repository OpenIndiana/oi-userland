https://github.com/zestsoftware/zest.releaser/pull/445

--- zest.releaser-9.1.2/tox.ini.orig
+++ zest.releaser-9.1.2/tox.ini
@@ -4,8 +4,8 @@
 
 [testenv]
 usedevelop = true
-deps =
-    zest.releaser[test]
-    zest.releaser[recommended]
+extras =
+    test
+    recommended
 commands =
     zope-testrunner --test-path={toxinidir} -s zest.releaser --tests-pattern=^tests$ {posargs:-v -c}
