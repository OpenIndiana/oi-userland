Make sure this passes even when there is Cython installed.

--- setuptools-73.0.1/setuptools/command/build_ext.py.orig
+++ setuptools-73.0.1/setuptools/command/build_ext.py
@@ -18,7 +18,7 @@
 
 try:
     # Attempt to use Cython for building extensions, if available
-    from Cython.Distutils.build_ext import (  # type: ignore[import-not-found] # Cython not installed on CI tests
+    from Cython.Distutils.build_ext import (  # type: ignore # Cython not installed on CI tests
         build_ext as _build_ext,
     )
 
