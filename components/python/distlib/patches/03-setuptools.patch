--- distlib-0.4.2/pyproject.toml.orig
+++ distlib-0.4.2/pyproject.toml
@@ -1,6 +1,6 @@
 [build-system]
 requires = [
-    "setuptools >= 68,<75",
+    "setuptools >= 68",
     "wheel"
 ]
 build-backend = 'setuptools.build_meta'
