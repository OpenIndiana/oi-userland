tox-envlist is not needed to run tests.
tox version pin is too strict.

--- consolekit-1.7.2/tox.ini.orig
+++ consolekit-1.7.2/tox.ini
@@ -39,8 +39,6 @@
 isolated_build = True
 requires =
     pip>=21,!=22.2
-    tox-envlist>=0.2.1
-    tox~=3.0
     virtualenv!=20.16.0
 
 [envlists]
