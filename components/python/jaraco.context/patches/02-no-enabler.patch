pytest-enabler is not needed to run tests.

--- jaraco_context-6.0.1/pyproject.toml.orig
+++ jaraco_context-6.0.1/pyproject.toml
@@ -29,7 +29,6 @@
 test = [
 	# upstream
 	"pytest >= 6, != 8.1.*",
-	"pytest-enabler >= 2.2",
 
 	# local
 	"portend",
