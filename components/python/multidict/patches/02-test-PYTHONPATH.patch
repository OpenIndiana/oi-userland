We cannot ignore PYTHONPATH here.  Otherwise the test will try to use the
installed multidict.

--- multidict-6.2.0/tests/test_circular_imports.py.orig
+++ multidict-6.2.0/tests/test_circular_imports.py
@@ -84,7 +84,6 @@
     imp_cmd = (
         # fmt: off
         sys.executable,
-        "-I",
         "-W", "error",
         "-c", f"import {import_path!s}",
         # fmt: on
