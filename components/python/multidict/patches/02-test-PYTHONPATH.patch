We cannot ignore PYTHONPATH here.  Otherwise the test will try to use the
installed multidict.

--- multidict-6.7.0/tests/test_circular_imports.py.orig
+++ multidict-6.7.0/tests/test_circular_imports.py
@@ -81,7 +81,6 @@
     imp_cmd = (
         # fmt: off
         sys.executable,
-        "-I",
         "-W",
         "error",
         "-c",
@@ -100,7 +99,6 @@
     imp_cmd = (
         # fmt: off
         sys.executable,
-        "-I",
         "-W",
         "error",
         "-c",
