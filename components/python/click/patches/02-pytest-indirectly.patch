Needed for tox-current-env to work properly.

--- click-8.3.0/pyproject.toml.orig
+++ click-8.3.0/pyproject.toml
@@ -157,6 +157,7 @@
 use_frozen_constraints = true
 dependency_groups = ["tests"]
 commands = [[
+    "python", "-m",
     "pytest", "-v", "--tb=short", "--basetemp={env_tmp_dir}",
     {replace = "posargs", default = [], extend = true},
 ]]
