We do not have this optional dependency packaged yet.

--- anyio-4.12.1/pyproject.toml.orig
+++ anyio-4.12.1/pyproject.toml
@@ -56,7 +56,6 @@
     "pytest >= 7.0",
     "pytest-mock >= 3.14",
     "trustme >= 1.0.0",
-    "truststore >= 0.9.1; python_version >= '3.10'",
     """
     winloop >= 0.2.3; platform_python_implementation == 'CPython' \
     and platform_system == 'Windows' \
