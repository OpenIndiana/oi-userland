This is optional test dependency and we do not have it packaged yet.

--- anyio-4.2.0/pyproject.toml.orig
+++ anyio-4.2.0/pyproject.toml
@@ -50,7 +50,6 @@
     "pytest >= 7.0",
     "pytest-mock >= 3.6.1",
     "trustme",
-    "uvloop >= 0.17; platform_python_implementation == 'CPython' and platform_system != 'Windows'",
 ]
 doc = [
     "packaging",
