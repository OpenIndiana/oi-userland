--- zope_deferredimport-6.0/pyproject.toml.orig
+++ zope_deferredimport-6.0/pyproject.toml
@@ -3,7 +3,7 @@
 
 [build-system]
 requires = [
-    "setuptools == 78.1.1",
+    "setuptools",
     "wheel",
 ]
 build-backend = "setuptools.build_meta"
