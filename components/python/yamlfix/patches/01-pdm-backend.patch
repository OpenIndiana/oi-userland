https://github.com/lyz-code/yamlfix/issues/267

--- yamlfix-1.15.0/pyproject.toml.orig
+++ yamlfix-1.15.0/pyproject.toml
@@ -10,16 +10,14 @@
 [tool.autoimport.common_statements]
 factories = "from tests import factories"
 
-[tool.pdm]
-package-dir = "src"
-source-includes = [
-    "tests/",
-]
-
 [tool.pdm.version]
 from = "src/yamlfix/version.py"
 
 [tool.pdm.build]
+package-dir = "src"
+source-includes = [
+    "tests/",
+]
 editable-backend = "path"
 
 [tool.pdm.resolution.overrides]
@@ -351,6 +349,6 @@
 
 [build-system]
 requires = [
-    "pdm-pep517",
+    "pdm-backend",
 ]
-build-backend = "pdm.pep517.api"
+build-backend = "pdm.backend"
