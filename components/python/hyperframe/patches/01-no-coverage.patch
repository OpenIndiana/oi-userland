We do not need to check coverage for this project.

--- hyperframe-6.1.0/pyproject.toml.orig
+++ hyperframe-6.1.0/pyproject.toml
@@ -55,7 +55,6 @@
 
 testing = [
   "pytest>=8.3.3,<9",
-  "pytest-cov>=6.0.0,<7",
   "pytest-xdist>=3.6.1,<4",
 ]
 
@@ -166,7 +165,7 @@
 ]
 dependency_groups = ["testing"]
 commands = [
-  ["pytest", "--cov-report=xml", "--cov-report=term", "--cov=hyperframe", { replace = "posargs", extend = true }]
+  ["pytest", { replace = "posargs", extend = true }]
 ]
 
 [tool.tox.env.pypy3]
