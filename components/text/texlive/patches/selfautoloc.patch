diff --git a/texk/kpathsea/progname.c b/texk/kpathsea/progname.c
index a984eab..2b885c0 100644
--- a/texk/kpathsea/progname.c
+++ b/texk/kpathsea/progname.c
@@ -657,7 +657,7 @@ kpathsea_set_program_name (kpathsea kpse,  const_string argv0,
      the program_name, otherwise the PATH search inside kpse_selfdir will fail,
      since `prog' doesn't exists as a file, there's `prog.exe' instead.  */
 #ifndef WIN32
-  sdir = kpathsea_selfdir (kpse, kpse->invocation_name);
+  sdir = xstrdup("/usr/share/texlive");
 #endif
   /* SELFAUTODIR is actually the parent of the invocation directory,
      and SELFAUTOPARENT the grandparent.  This is how teTeX did it.  */
