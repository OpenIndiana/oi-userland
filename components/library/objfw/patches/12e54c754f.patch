Commit 12e54c754f from upstream.

--- objfw-1.0.3/src/OFSystemInfo.m
+++ objfw-1.0.3/src/OFSystemInfo.m
@@ -158,11 +158,11 @@
 	    initWithCString: _os_flavor
 		   encoding: OFStringEncodingASCII];
 #elif defined(HAVE_SYS_UTSNAME_H) && defined(HAVE_UNAME)
 	struct utsname name;
 
-	if (uname(&name) != 0)
+	if (uname(&name) == -1)
 		return;
 
 	operatingSystemName = [[OFString alloc]
 	    initWithCString: name.sysname
 		   encoding: [OFLocale encoding]];
@@ -255,11 +255,11 @@
     defined(OF_PSP)
 	/* Intentionally nothing */
 #elif defined(HAVE_SYS_UTSNAME_H) && defined(HAVE_UNAME)
 	struct utsname name;
 
-	if (uname(&name) != 0)
+	if (uname(&name) == -1)
 		return;
 
 	operatingSystemVersion = [[OFString alloc]
 	    initWithCString: name.release
 		   encoding: [OFLocale encoding]];

