# see https://bugreports.qt.io/browse/QTBUG-106964
--- qt-everywhere-src-5.15.18/qtdeclarative/src/3rdparty/masm/wtf/Assertions.h.orig
+++ qt-everywhere-src-5.15.18/qtdeclarative/src/3rdparty/masm/wtf/Assertions.h
@@ -346,7 +346,7 @@
 #ifndef COMPILE_ASSERT
 #if COMPILER_SUPPORTS(C_STATIC_ASSERT)
 /* Unlike static_assert below, this also works in plain C code. */
-#define COMPILE_ASSERT(exp, name) _Static_assert((exp), #name)
+#define COMPILE_ASSERT(exp, name) typedef int dummy##name [(exp) ? 1 : -1]
 #elif COMPILER_SUPPORTS(CXX_STATIC_ASSERT)
 #define COMPILE_ASSERT(exp, name) static_assert((exp), #name)
 #else
