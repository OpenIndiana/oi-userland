Disable for Solaris

Not suitable for upstream

--- webkitgtk-2.48.1/Source/WTF/wtf/PlatformUse.h.orig
+++ webkitgtk-2.48.1/Source/WTF/wtf/PlatformUse.h
@@ -87,8 +87,10 @@
 #endif
 
 #if PLATFORM(COCOA) || (PLATFORM(GTK) || PLATFORM(WPE))
+#if !OS(SOLARIS)
 #define USE_FILE_LOCK 1
 #endif
+#endif
 
 #if PLATFORM(COCOA)
 #define USE_FOUNDATION 1
