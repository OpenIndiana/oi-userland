Fix code.
--- heimdal-7.8.0/lib/hcrypto/rand-fortuna.c.old
+++ heimdal-7.8.0/lib/hcrypto/rand-fortuna.c
@@ -336,7 +336,7 @@
 	st->pool0_bytes += len;
 
     memset_s(hash, sizeof(hash), 0, sizeof(hash));
-    memset_s(&md, sizeof(hash), 0, sizeof(md));
+    memset_s(&md, sizeof(md), 0, sizeof(md));
 }
 
 /*
