--- libreoffice-26.8.0.0.alpha1/vcl/unx/generic/app/geninst.cxx.~1
+++ libreoffice-26.8.0.0.alpha1/vcl/unx/generic/app/geninst.cxx
@@ -22,7 +22,7 @@
 #if defined(LINUX)
 #  include <stdio.h>
 #endif
-#if defined(__FreeBSD__)
+#if defined(__FreeBSD__) || defined(__sun)
 #  include <sys/utsname.h>
 #endif
 #if defined(__EMSCRIPTEN__)
@@ -77,6 +77,11 @@
             break;
     }
     aKernelVer = OUString::createFromAscii(stName.sysname) + " " + aKernelVer.copy(0, nIndex);
+#elif defined(__sun)
+    struct utsname stName;
+    uname( &stName );
+
+    aKernelVer = OUString::createFromAscii( stName.sysname ) + " " + OUString::createFromAscii( stName.release )+ " " + OUString::createFromAscii( stName.version );
 #elif defined(__EMSCRIPTEN__)
 #if defined __EMSCRIPTEN_MAJOR__
     aKernelVer = u"Emscripten "_ustr + OUString::number(__EMSCRIPTEN_MAJOR__) + u"."_ustr
