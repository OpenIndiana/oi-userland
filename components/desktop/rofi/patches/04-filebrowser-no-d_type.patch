--- rofi-2.0.0/source/modes/filebrowser.c.orig	2025-09-01 20:22:18.000000000 +0200
+++ rofi-2.0.0/source/modes/filebrowser.c	2025-11-02 14:01:37.657415450 +0100
@@ -242,6 +242,7 @@
   DIR *dir = opendir(cdir);
   if (dir) {
     struct dirent *rd = NULL;
+    struct stat st;
     while ((rd = readdir(dir)) != NULL) {
       if (g_strcmp0(rd->d_name, "..") == 0) {
         fb_resize_array(pd);
@@ -264,39 +265,37 @@
         continue;
       }
 
-      switch (rd->d_type) {
-      case DT_BLK:
-      case DT_CHR:
-      case DT_FIFO:
-      case DT_UNKNOWN:
-      case DT_SOCK:
-      default:
-        break;
-      case DT_REG:
-      case DT_DIR:
-        fb_resize_array(pd);
-        // Rofi expects utf-8, so lets convert the filename.
-        pd->array[pd->array_length].name =
-            g_filename_to_utf8(rd->d_name, -1, NULL, NULL, NULL);
-        if (pd->array[pd->array_length].name == NULL) {
-          pd->array[pd->array_length].name = rofi_force_utf8(rd->d_name, -1);
+      if (stat(rd->d_name, &st) == 0) {
+        /* I don't know how to replace case DT_UNKNOWN */
+        if (S_ISBLK(st.st_mode) || S_ISCHR(st.st_mode) || S_ISFIFO(st.st_mode) || S_ISSOCK(st.st_mode)) {
+          // Nothing
         }
-        pd->array[pd->array_length].path =
-            g_build_filename(cdir, rd->d_name, NULL);
-        pd->array[pd->array_length].type =
-            (rd->d_type == DT_DIR) ? DIRECTORY : RFILE;
-        pd->array[pd->array_length].icon_fetch_uid = 0;
-        pd->array[pd->array_length].icon_fetch_size = 0;
-        pd->array[pd->array_length].icon_fetch_scale = 0;
-        pd->array[pd->array_length].link = FALSE;
+        if (S_ISREG(st.st_mode) || S_ISDIR(st.st_mode)) {
+          fb_resize_array(pd);
+          // Rofi expects utf-8, so lets convert the filename.
+          pd->array[pd->array_length].name =
+              g_filename_to_utf8(rd->d_name, -1, NULL, NULL, NULL);
+          if (pd->array[pd->array_length].name == NULL) {
+            pd->array[pd->array_length].name = rofi_force_utf8(rd->d_name, -1);
+          }
+          pd->array[pd->array_length].path =
+              g_build_filename(cdir, rd->d_name, NULL);
+          if (stat(rd->d_name, &st) == 0) {
+              pd->array[pd->array_length].type =
+              (S_ISDIR(st.st_mode)) ? DIRECTORY : RFILE;
+          }
+          pd->array[pd->array_length].icon_fetch_uid = 0;
+          pd->array[pd->array_length].icon_fetch_size = 0;
+          pd->array[pd->array_length].icon_fetch_scale = 0;
+          pd->array[pd->array_length].link = FALSE;
 
-        if (file_browser_config.sorting_method == FB_SORT_TIME) {
-          set_time(&pd->array[pd->array_length]);
-        }
+          if (file_browser_config.sorting_method == FB_SORT_TIME) {
+            set_time(&pd->array[pd->array_length]);
+          }
 
-        pd->array_length++;
-        break;
-      case DT_LNK:
+          pd->array_length++;
+        }  // if (S_ISREG() || S_ISDIR())
+      if (S_ISLNK(st.st_mode)) {
         fb_resize_array(pd);
         // Rofi expects utf-8, so lets convert the filename.
         pd->array[pd->array_length].name =
@@ -342,11 +341,11 @@
           }
         }
         pd->array_length++;
-        break;
-      }
-    }
+      }  // if (S_ISLNK()) 
+      }  // if (stat())
+    }  // if()
     closedir(dir);
-  }
+  }  // while ()
   g_free(cdir);
   g_qsort_with_data(pd->array, pd->array_length, sizeof(FBFile), compare, NULL);
 }
