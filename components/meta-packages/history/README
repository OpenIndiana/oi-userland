See $(WS)/doc/packaging.txt for information about the history file.

To sort the history file into the canonical ordering, use:
env LC_COLLATE=C sort -t @ -k 1,1 history

The other *.p5m here are history entries more complex than the simple
one-to-one mapping of the history file allows for.
