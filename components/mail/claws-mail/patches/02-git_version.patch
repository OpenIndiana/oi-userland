--- claws-mail-4.3.1/get-git-version.orig
+++ claws-mail-4.3.1/get-git-version
@@ -9,9 +9,9 @@
     exit 1
   fi
 elif test -s ./version; then
-  version=`sh -c ". ./version"`
+  version=`bash -c ". ./version"`
 else
   echo "Error: *** invalid tree. Neither '.git' nor 'version' were found"
   exit 1
 fi
-echo -n $version
+gecho -n $version
