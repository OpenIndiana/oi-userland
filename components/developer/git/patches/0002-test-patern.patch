https://lore.kernel.org/git/ZkW5ggOVlglfi64u@telcontar/

--- git-2.45.1/t/t0211-trace2-perf.sh.orig
+++ git-2.45.1/t/t0211-trace2-perf.sh
@@ -233,7 +233,7 @@
 
 	pattern="d0|${thread}|${event}||||${category}|name:${name} value:${value}" &&
 
-	grep "${patern}" ${file}
+	grep "${pattern}" ${file}
 }
 
 test_expect_success 'global counter test/test1' '
